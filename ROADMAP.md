# Roadmap

The build order for Provenance, sequenced so there's always something demoable at the end of each phase rather than a long stretch with nothing to show. Kept as a living checklist — update this file as items complete rather than letting it drift from reality: strike through a finished item (`~~…~~`) and append a `— **done**: …` note recording what actually shipped, including any deviation from the plan.

**Items 0.5–26 are done — Phases 1 through 7 are complete and Phase 8 is under way**, including Phase 4, the differentiator. Each item's `— **done**:` note below records what shipped; the reasoning is in its `docs/adr/` entry. **Item 27 (the injection arc) is next** — items 25 and 26 built both outer filters and measured both leaking on the same crafted payload, so the arc now has an untrusted-content path end to end and what remains is the claim it exists to make: the gateway holds at 11 when the filters leak. Hard deadline: **August 31, 2026, 5:00 PM PDT** (All Things Agentic Hackathon); the hosted URL and credentials must stay valid through **October 1** for judging.

See [`ARCHITECTURE.md`](./ARCHITECTURE.md) for what each item actually means — its §10 Testing Strategy table is the source of the `verify:` criteria below — and [`docs/adr/`](./docs/adr/) for the reasoning behind the decisions items depend on. Phase 4 (memory) is the differentiator and gets the most time; phases 12–14 are submission mechanics with their own deadline pressure.

---

## Build Order

**Conventions these notes assume**, stated once so each item's note carries only its own result:

- **Where the reasoning lives.** Design reasoning and rejected alternatives live in the item's `docs/adr/` entry. These notes carry what shipped, what was verified live, what deviated, and what was found by running it.
- **Two halves.** Each item's *offline* half runs in CI with no cloud credentials; its *live* half needs credentials, is not in CI, and reads its result back from the authoritative source rather than eyeballing it — item 2's Cloud Trace read-back, item 3's `/health` curl, item 4's document read-back — exiting non-zero on mismatch.
- **Mutation testing.** Claims are checked by mutating the code and confirming the suite goes red. `__pycache__` is cleared between mutations (item 4's finding: Python's `.pyc` invalidation misses a same-second edit that leaves the file the same size). **A mutation that survives is a claim about the mutation first** — items 7 and 8 both recorded a first attempt that was wrong about itself rather than about the tests.
- **Cost.** Unless an item says otherwise it added no paid resource, no idle-billing resource, and nothing outside the free tier; Cloud Run stays `--min-instances=0`. Only deviations from that are noted.

**Phase 1 — Foundations, deployed (nothing exists until it's on Cloud Run)**

0.5. ~~**Day-one dependency spike — before any code is written.** Three findings from auditing PortunusMCP as a dependency (see `docs/adr/ADR-004`), resolved in an hour, not discovered on Aug 25.~~
   — **done** (Aug 21): spike run against `portunusmcp==0.1.0` (PyPI) + `google-adk==2.7.1` in a Python 3.12 venv. All three findings resolved; `from services.gateway import signing, abac, decision` verified working alongside ADK, with an ECDSA sign/verify round-trip and an ABAC compile/evaluate round-trip.
   - **Package naming — confirmed.** The wheel ships a top-level package literally named `services`. **Provenance's own package is `provenance/`**, never `services/`. The `mypy` target in `CLAUDE.md` / `AGENTS.md` / `.cursor/rules/provenance.mdc` was updated to `provenance/` in this commit.
   - **Dependency resolution — `--no-deps`, and the failure mode is worse than a conflict.** `portunusmcp` pins `fastapi==0.115.6`; `google-adk` requires `fastapi>=0.133,<1`. A plain `pip install portunusmcp` does **not** error — it silently *downgrades* `fastapi` to 0.115.6 and `starlette` to 0.41.3, out of ADK's supported range, and pulls in `redis`, `sqlalchemy`, `asyncpg`, `alembic`, `mcp`, `uvloop`. Install is therefore two steps, and `portunusmcp` **cannot** go in `pyproject.toml`'s `dependencies`:
     ```
     pip install -e ".[dev]" && pip install --no-deps portunusmcp==0.1.0
     ```
     `cryptography` is declared as our own dependency (Portunus's `==49.0.0` pin is unnecessary — verified working on 50.0.0). `pip check` exits non-zero in the resulting venv by design; **CI must not gate on `pip check`**. Same two-step in the Dockerfile (Phase 1, item 3).
   - **Consumed surface — `signing`, `abac`, `decision`, and nothing else.** Verified per-module imports: `signing` needs only `cryptography` + stdlib; `abac` is pure stdlib; `decision` needs only `pydantic` (already an ADK transitive dep). `services/gateway/auth.py` — the identity broker — is **not consumed**: it requires `redis.asyncio`, `structlog`, `canonicaljson`, a YAML-loaded `PolicyEngine`, and a live Redis, none of which belong on Cloud Run for this. **Agent identity resolution is new code in this repo**: ECDSA-verify against the `public_key` on the Firestore registry record (item 5 stores it anyway) — the ADR-004-scored side of the boundary, not the pre-existing side.
   - **Short-lived credentials — confirmed absent; building the minting layer.** Portunus provides static API keys and HMAC-signed requests, no minting and no `expires_at` (the only TTLs are rate-limit counters). Decision: **build a thin minting layer** on top of Portunus's `signing` primitives — the registry mints an ECDSA-signed assertion `(agent_id, agent_version, issued_at, expires_at)`; the gateway verifies signature + expiry per request. New, scored work on the track's Agent Identity pillar; lands in **item 7**. `README.md` and `ARCHITECTURE.md` §2.1 keep their "short-lived per-agent credentials" wording — the claim becomes true rather than softened.
   - **Two constraints for item 7 to design around**, found in the wheel: `abac.ATTRIBUTE_ROOTS` is a fixed `{identity, tool, context, risk}` — Provenance conditions must live under those roots (`identity.standing`, `risk.score` map cleanly); and `decision.DecisionOutcome` is `allow / deny / challenge / human_approval_required`, so our approve/hold/deny vocabulary either maps `hold → human_approval_required` and ignores `challenge`, or we define our own typed decision. Not decided here.
   - **Also in the wheel, deliberately not imported:** `services/gateway/risk_engine.py` (per-call MCP risk scoring). It is a different thing from Provenance's deterministic action risk table, but a judge grepping the dependency will see "risk scoring" present in pre-existing code — the `README.md` disclosure row should say so explicitly before submission.
   - **Carried into item 1:** `tests/test_portunus_surface.py` holding the import + sign/verify + ABAC round-trip, so CI catches a `--no-deps` install that quietly stopped working or a Portunus API change.

1. ~~GCP project, ADK 2.0 project scaffold, Gemini 3.5 Pro/Flash access, Firestore provisioned, PortunusMCP added as a library dependency (per the item-0.5 resolution). Repo scaffold: `pyproject.toml`, lint/typecheck/test tooling, CI.~~ — verify: a fresh clone installs and `pytest` runs (zero tests is fine); CI is green.
   — **done** (Aug 21): both halves.
   - **Repo.** `pyproject.toml` (package `provenance/`; runtime deps `google-adk==2.7.1` + `cryptography` only), `provenance/__init__.py`, ruff / mypy-strict / pytest config, and `.github/workflows/ci.yml` running all four gates with no cloud credentials and no `pip check` gate. Verified: a fresh clone installs with the documented two-step command and `pytest` passes; dropping the `--no-deps` step turns the surface test red. CI green on push and PR ([#1](https://github.com/BashaarJavaid/Provenance/pull/1)).
   - **The item-0.5 carry-forward test** landed as `tests/test_portunus_surface.py`: sign/verify and ABAC compile/evaluate round-trips, plus `abac.ATTRIBUTE_ROOTS` and `decision.DecisionOutcome` pinned so an upstream change breaks the build instead of surprising item 7. Note for item 7: `signing.sign()` takes a hash **string**, not bytes.
   - **GCP.** `scripts/gcp_setup.sh` (idempotent, fails closed without linked billing) created `provenance-hackathon`, enabled `aiplatform` + `firestore`, and provisioned the Native-mode `(default)` Firestore database in `us-central1`. Re-running it is a no-op and exits 0.
   - **Deviation — Gemini 3.5 Pro does not exist.** The Vertex publisher catalog for this project lists `gemini-3-flash-preview`, `gemini-3.1-pro-preview`, `gemini-3.1-flash-*`, `gemini-3.5-flash`, `gemini-3.5-flash-lite`, and `gemini-3.6-flash` — no 3.5 Pro, and no GA Pro anywhere in the 3.x line (`gemini-3.1-pro`, `gemini-3.6-pro`, `gemini-3.5-pro-preview` all 404). Google shipped 3.x Flash-first; the only Pro-tier Gemini 3 is `gemini-3.1-pro-preview`, which does serve. This is not an allowlist, so there is nothing to request. Decision: stay on GA **`gemini-2.5-pro`** rather than a preview model that can change or be withdrawn during the Oct 1 judging window. **The four reasoning roles (Orchestrator, domain agents, Remediation Planner, Memory Analyst) run on `gemini-2.5-pro`**; verification stays on `gemini-3.5-flash`. `README.md` and `ARCHITECTURE.md` §5 were updated to name what is actually deployed. The model is a per-role config string, so swapping back is a one-line change if access appears.
   - **Also learned:** Gemini 3.x is served from the **`global`** endpoint, not a regional one — a regional probe 404s on models that are in fact available. The first version of the setup script got this wrong and reported a false negative on 3.5 Flash.
   - **Not pulled forward:** ADK is a dependency only — no `adk create` template, no root agent, no app (item 3 and Phase 3). Firestore, OpenTelemetry, and Vertex client libraries are not declared yet; each lands with the item that first imports it.

2. ~~OpenTelemetry-compliant trace schema defined **before any agent is written** — span shapes for authorization decisions, belief commits, verification outcomes, reasoning chains; exported to Cloud Trace/Cloud Logging. — verify: a hand-emitted span for each shape renders in Cloud Trace with trace IDs intact.~~
   — **done** (Aug 21): `provenance/telemetry.py` — four span shapes (`provenance.authorization.decision`, `provenance.belief.commit`, `provenance.verification.outcome`, `provenance.reasoning.chain`), their attribute vocabulary, and `configure_tracing()`. Written up in `ARCHITECTURE.md` §8.1; `§10` gained the tracing row this item's `verify:` line now derives from.
   - **Verified live.** `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/emit_trace_samples.py` emits one span of each shape under a shared parent, then **reads the trace back through the Cloud Trace v1 API** and asserts all four names are present on the one trace ID — the `verify:` line checked mechanically rather than by eye. Trace `9a0b237f4f576fc4da35e4e76bd0ee03` on `provenance-hackathon`. Indexing lag was ~15s on the successful run but **~2 minutes** on the first, so the poll budget is 4 minutes; a "not found" inside the first minute means nothing.
   - **Offline half.** `tests/test_telemetry_schema.py` (16 tests, in-memory exporter, no credentials) pins each shape's span name and *exact* attribute set, the trace-ID/parent relationship between nested shapes, and the fail-closed behaviour. Attribute keys are asserted as literal strings, not via the module's constants, so a rename fails the build. Mutation-checked: dropping a required attribute, renaming a key, and adding a `rationale` attribute each turn it red.
   - **Design decisions** (all confirmed before implementation): attributes are `provenance.<object>.<field>`, grouped by the §3 canonical object; span names carry the `provenance.` prefix so a Cloud Trace filter separates them from ADK's own auto-instrumented spans in Phase 3; `COMMIT`/`REJECT`/`RETRACT` share one belief span rather than three, mirroring §2.1's treatment of denials; **no root/incident span** — that arrives with the Orchestrator in item 9. No ADR: OTel → Cloud Trace was already decided in §8 and the README stack table, with no alternative rejected here.
   - **The redaction rule is now a checked property, not a wish.** Span attributes carry identifiers, hashes, enums and numbers only. A test walks every attribute of all four shapes and rejects any key containing `prompt`/`rationale`/`payload`/`text`/`content`/`message`/`body`. Item 26's "raw inbound text never appears in the trace" has something to be checked against.
   - **Risk arithmetic is validated at emit.** `set_risk()` raises unless `score == base + criticality + blast + irreversibility`. The gateway ledger and item 31's approval card both render this breakdown from this stream, so it has to add up. `set_risk()` is deliberately separate from `set_outcome()`: a proposal denied at the identity or registry stage never reaches the risk table and its span correctly carries no score.
   - **Deviation — Cloud Logging is not wired.** The item says "Cloud Trace/Cloud Logging"; only Trace is exported. Nothing logs yet, and on Cloud Run stdout reaches Cloud Logging with no exporter. `ARCHITECTURE.md` §8.1 records this.
   - **Finding — `CloudTraceSpanExporter` is deprecated upstream.** It emits a `DeprecationWarning` pointing at the OTLP → `telemetry.googleapis.com` migration. Kept anyway: it works, and `google-adk`'s own `gcp` extra still pins `opentelemetry-exporter-gcp-trace>=1.9,<2`. Revisit only if it breaks before Oct 1; the migration would add `opentelemetry-exporter-otlp-proto-http` plus auth-header plumbing for no gain here.
   - **Dependency:** `opentelemetry-exporter-gcp-trace>=1.9,<2` only. `opentelemetry-api`/`-sdk` already arrive via `google-adk==2.7.1` and are deliberately not re-pinned. `google-adk[gcp]` would have pulled four more packages we don't emit to. `scripts/gcp_setup.sh` now enables `cloudtrace.googleapis.com`; ingestion is free to 2.5M spans/month and nothing here bills while idle.
3. ~~A deployed Cloud Run service on day one (skeleton gateway + UI shell), plus deploy scripts. — verify: the service URL responds publicly; deploy is one command from a clean checkout.~~
   — **done** (Aug 21): live and public at **https://provenance-808273007560.us-central1.run.app**. `./scripts/deploy.sh` is the one command; it curls `/health` on the deployed URL and exits non-zero if it isn't 200, so the `verify:` line is checked mechanically rather than by eye.
   - **Design recorded in [`ADR-008`](./docs/adr/ADR-008-one-cloud-run-service.md):** one service rather than four (§11's "gateway, orchestrator, sweeper, and UI as services" is the eventual shape, not a Phase 1 requirement), and why not `adk deploy cloud_run`, not `get_fast_api_app`, and not a Node toolchain — with the revisit conditions for each. `provenance/app.py` is a plain FastAPI app serving `GET /health` and `GET /` (the shell).
   - **Our own Dockerfile, because ADK's cannot express the install.** ADK's `_DOCKERFILE_TEMPLATE` is `python:3.11-slim` (we pin 3.12), agent-folder-shaped, installs `google-adk` from PyPI rather than this repo, and has no hook for the mandatory `--no-deps portunusmcp` second step. Verified in the built image: `fastapi 0.141.1`, `starlette 1.6.0`, and none of redis/sqlalchemy/asyncpg/alembic/uvloop — the item-0.5 ordering held.
   - **Finding — Cloud Run's frontend swallows `/healthz`.** The route was `/healthz` and returned Google's own HTML 404 in production while `/` and `/docs` served fine and the app's own 404s were JSON: the request never reached the container. Renamed to **`/health`**. Worth knowing before any later item picks a path by habit.
   - **Finding — `status.url` reports the hostname Google is retiring.** `gcloud run services describe --format='value(status.url)'` returns the legacy `*-uc.a.run.app` form; the canonical project-number URL is first in the `run.googleapis.com/urls` annotation. Both serve. The script reads the annotation so the README URL is the canonical one.
   - **Deviation — `gcp_setup.sh` grew an IAM grant.** `gcloud run deploy --source` builds as the Compute Engine default service account, which on a project created after mid-2024 has no build permissions and fails *after* the source upload with an opaque `PERMISSION_DENIED`. The setup script now grants `roles/cloudbuild.builds.builder` to it, and enables `run` / `cloudbuild` / `artifactregistry` alongside the existing three APIs. Still idempotent, still exits 0 on a re-run.
   - **The shell is one static file** (`provenance/web/index.html`, inline CSS/JS, no build step) framing the six `ARCHITECTURE.md` §8.2 surfaces as labelled empty regions, each naming the item that fills it. Later items fill regions rather than restructuring. A health badge polls `/health`.
   - **Tracing is wired at startup** and reports `true` in production — `configure_tracing()` runs in the FastAPI lifespan and `GOOGLE_CLOUD_PROJECT` is set by the deploy. The item-2 stream is proven live in the deployed environment now, rather than first discovered broken in Phase 3. It returns `False` and no-ops without the env var, so CI needs no credentials.
   - **Offline half.** `tests/test_app.py` asserts both routes and pins the six surface names as literal strings. **Mutation-checked:** renaming a health key and renaming a panel each turn it red.
   - **Dependencies:** `fastapi>=0.133,<1` and `uvicorn>=0.34,<1` declared with ADK's own ranges (both already arrived transitively; we import them, so we declare them). `httpx` for `TestClient` is already present. Note `starlette` warns that `TestClient` wants `httpx2`; harmless, revisit only if it breaks.
   - **Cost.** `--min-instances=0 --max-instances=3 --memory=512Mi --cpu=1`, unauthenticated. $0 while idle; Cloud Build minutes and egress sit inside the free tier at this volume.
4. ~~Synthetic company base: `google/adk-samples` Customer Service entity model imported; `inventory-api` with config versions (known-good v41), 2–3 suppliers, two extra tier-2 services that never appear in an incident, and the fault-injection switch (ARCHITECTURE §9). — verify: a seed script populates Firestore idempotently; entities render in the UI shell.~~
   — **done** (Aug 21): **26 documents live in Firestore on `provenance-hackathon`.** `provenance/synthetic/company.py` holds the cast as frozen dataclasses; `scripts/seed_firestore.py` writes it and reads every document back, exiting non-zero on any mismatch. `ARCHITECTURE.md` §9 gained an "as built" table.
   - **Design recorded in [`ADR-009`](./docs/adr/ADR-009-synthetic-company-collections.md):** typed top-level collections rather than one polymorphic `entities` collection; config history as a `services/{id}/config_versions/{version}` subcollection; and the fault switch as a `fault_injection/{target_id}` **document read at request time** rather than deploy config, so a fault stays flippable mid-incident.
   - **The cast.** Cymbal Home & Garden, after the sample it derives from. `inventory-api` (tier2, v42 deployed over known-good v41 — the gap incidents #1/#2 need), `pricing-api` (tier2, deliberately no config history and no beliefs, so item 24's class belief carries the diagnosis alone), `checkout-api` (tier2, never in an incident). `SUP-042` "Verdant Supply Co." (**tier1**), `SUP-017`, `SUP-093`. Dana Ruiz, Store Operations Manager. Plus the retail base: 3 customers, 6 products, 5 orders, every foreign key resolving.
   - **Resolved — `pricing-api` *is* one of §9's "two additional tier-2 services".** §9 says they "never appear in an incident" while the demo runs incident #3 on `pricing-api`. Read as: never in incidents #1/#2. Incident #3 landing on a member of that population is the class-belief payoff, not a contradiction. Two extra services, not three.
   - **The two tiers item 7 depends on are now guarded.** `inventory-api` is tier2 and `SUP-042` is tier1, which is the only route §4.2's worked examples have to `criticality` +1 and +2 — i.e. to the frozen scores 2 and 11. `tests/test_synthetic_company.py` asserts both by name, so item 7's tests can't be broken from here silently.
   - **No entity carries a status, and a test enforces it.** The tempting shape is `suppliers/SUP-042 { status: AT_RISK }` because that is what the demo shows. It would be a belief with no evidence, no computed confidence, no signature, in a place recall never looks and nothing can supersede. `SUP-042` becomes AT_RISK in item 17 through the belief store or not at all; a test rejects any `status`/`confidence`/`belief` field on the entity dataclasses.
   - **Finding — a naive read-back false-alarms mid-rehearsal.** The first version compared *every* document against the fixture after writing. Correct on a fresh seed; wrong on a re-run after a demo take, where `inventory-api` has legitimately been rolled back to v41 with its error rate spiked — the script skipped every document as already-present, did its job, and then exited non-zero reporting the demo working as a seeding fault. Now: existence is asserted for all 26 (the seed's actual claim), content only for documents *this run wrote*. `--reset` rewrites everything, so its read-back does check all 26. Both failure branches were exercised directly against Firestore.
   - **Offline half.** `tests/test_synthetic_company.py` — 11 tests, no credentials, no cloud import: the two frozen tiers, tier vocabulary, id uniqueness across the entity model (§3.1 looks a target up by one id), `inventory-api`'s single known-good v41 with v42 current, `pricing-api`'s empty history, every fault switch off at baseline, the no-status rule, and retail referential integrity. **Mutation-checked:** flipping `inventory-api` to tier1, adding a defaulted `status` to `Supplier`, and giving `pricing-api` a config version each turn it red.
   - **Deviation — "entities render in the UI shell" is not done.** None of the shell's six §8.2 surfaces is an entity panel, `tests/test_app.py` pins those six names as literal strings, and item 3's convention is that later items fill regions rather than restructure. Rendering entities would need a seventh region, a Firestore read inside `app.py` (whose docstring states it deliberately has none), a runtime dependency and a service-account IAM grant — for a panel no later item claims. The script's read-back proves the seed instead. Revisit only if a UI surface later genuinely needs the entity list.
   - **Dependency:** `google-cloud-firestore>=2.21,<3`, and deliberately in **dev** extras, not runtime: the only importer is `scripts/seed_firestore.py`, and `scripts/` is in `.gcloudignore` so none of it ships in the Cloud Run image. It moves to `[project] dependencies` in item 5. Not a `google-adk` transitive dep — it was not installed at all.
   - **Cost.** 26 documents, inside Firestore's free tier (1 GiB, 20k writes/day).
   - **Also learned:** Python's `.pyc` invalidation misses a same-second edit that leaves the file the same size (`"v7"` and `None` are both four characters), so a mutation check can report a stale result. Clear `__pycache__` between mutations.
5. ~~Agent Registry: the typed record (id, version, public_key, tool_scope, memory_domains, standing, rejection_window), stored in Firestore, with a request-time read API — never cached at boot. — verify: ARCHITECTURE §10's **Gateway** and **Standing** rows — flip standing mid-run and the next read reflects it.~~
   — **done** (Aug 21): **3 agents live in Firestore on `provenance-hackathon`.** `provenance/registry.py` is the record, the read API and the single standing writer; `scripts/seed_registry.py` writes the fleet and `scripts/verify_registry.py` performs the flip. `ARCHITECTURE.md` §3.4 gained the storage shape and the window's actual numbers.
   - **Design recorded in [`ADR-010`](./docs/adr/ADR-010-agent-registry-record.md):** one flat document per agent at `agents/{id}` with `version` as a field; **stored standing authoritative, nothing derived on read** (`SUSPENDED` is not derivable from rejections at any count, and "restoration requires explicit human reinstatement" needs something a human can set); raising rather than returning `None` as fail-closed; and **no `--reset`**, deliberately unlike `seed_firestore.py` — rewriting the fixture's `standing: GOOD` over a stored `DEGRADED` is exactly the quiet forgiveness §3.4 forbids, wearing the costume of a routine re-seed. An existing record is skipped whole, so each keypair is generated **once** and a re-run cannot invalidate a credential item 7 minted; `--rotate <agent-id>` is the one deliberate path to a new key and bumps the version with it.
   - **Correction to this item's own `verify:` line.** It pointed at an "ARCHITECTURE §10 registry row" that does not exist. The two rows that cover item 5 are **Gateway** (`registry read at request time`) and **Standing** (`earned and lost, never quietly restored`); the strikethrough above now names them.
   - **The flip is checked live, in one process.** `scripts/verify_registry.py` reads `sre-infra-agent` (GOOD), calls `set_standing(DEGRADED)`, re-reads through the **same client without restarting anything**, asserts the new value, and restores. Verified both ways: clean it exits 0; with a one-line dict cache added to `get_agent` it printed `FAIL: the re-read still says 'GOOD'; the registry is being cached.`
   - **The window had no size anywhere, and now has one.** §3.4, §10, `THREAT_MODEL.md`, the spec's §9 and item 28 all say "the rolling window" and none says how long. It is now **3 rejections inside 24 hours** (`REJECTION_THRESHOLD` / `REJECTION_WINDOW_HOURS`), named in §3.4 so items 14 and 28 point at a number instead of a phrase. A `RejectionEntry` carries `rejected_at` + `reason` — §2.2's "standing counter incremented" is the list's length, and the `reason` is what item 28's panel renders. `degraded_by_window()` ships with the constants but is **not** called by `get_agent`: it is the arithmetic item 14 applies before it writes.
   - **Fail-closed is structural, not documentary.** `RegistryUnavailable`, `AgentNotRegistered` and a bare `RegistryError` (malformed record) share one base class item 7 catches once and maps to `DENY(stage="registry")`. **No function returns `Agent | None`** — a test walks the module and rejects any that does, because the failure mode of a forgotten `if agent:` is an authorization granted with no standing read at all.
   - **Real P-256 keys, private halves never on disk.** The seeder generates the pair, stores the public PEM, and prints the private PEM once with a "not shown again" banner; item 7 decides how a signing agent receives it rather than this item inventing a key store it would have to defend. A test signs with a generated private half and verifies with the public one, so what is stored is a usable `SubjectPublicKeyInfo` PEM rather than a plausible string.
   - **The cast, and what is deliberately empty.** `sre-infra-agent` (domain `infrastructure`), `supply-chain-agent` (domain `supply-chain`), `remediation-planner` (no memory domain — it proposes actions, never beliefs). `tool_scope` holds only §4.2's two named action classes and sits on the Planner alone; the domain agents hold none, because §2.1 has the Planner emit the Action. The tool registry is item 6, and any further string would be a guess item 6 must honour or delete.
   - **Offline half.** `tests/test_registry.py` — 16 tests, no credentials, a ~30-line dict fake rather than a mock framework, because the point of the first test is that a value changing *between two reads* is visible. **Mutation-checked five ways:** memoizing `get_agent`, `REJECTION_THRESHOLD = 4`, returning `None` on an absent agent, recomputing standing from the window, and adding an invented action class to `tool_scope` each turn it red.
   - **No telemetry, no route.** A registry read is a data access, not a decision; item 7 emits `authorization.decision` carrying the standing it read. `provenance/app.py` gains no endpoint — §8.2's registry panel is item 11's — but its docstring's "any Firestore read" exclusion was reworded, since item 5 is the first thing to read Firestore from inside the package.
   - **Dependency and IAM.** `google-cloud-firestore>=2.21,<3` moved from `[dev]` to `[project] dependencies` exactly as item 4 promised, so it ships in the Cloud Run image. `scripts/gcp_setup.sh` now also grants `roles/datastore.user` to the Compute Engine default service account — the Cloud Run **runtime** identity — because without it the registry read fails closed in production and every proposal is denied. Still idempotent, still exits 0 on a re-run.
   - **Cost.** 3 documents and a handful of reads, inside Firestore's free tier.
6. ~~Typed Action schema + mechanical validation: `action_class` must exist in the tool registry, target in the entity model, declared fields validated against the tool schema; malformed → rejected once, escalated on the second. — verify: fabricated tool, nonexistent target, and free-form text all die before the gateway; second malformed emission escalates.~~
   — **done** (Aug 21): `provenance/tools.py` is the tool registry, `provenance/action.py` is §3.1's Action and `validate()`, and `tests/test_action.py` (26 tests) is the entire `verify:` line. `ARCHITECTURE.md` §3.1 gained an as-built note, §7.1 a reconciling sentence and §10 a tightened row; `THREAT_MODEL.md`'s hallucinated-action row became a tested claim.
   - **Design recorded in [`ADR-011`](./docs/adr/ADR-011-tool-registry-and-action-validation.md):** `TOOLS` as a frozen in-code constant rather than a Firestore collection — §1.1's fourth property exists because standing changes mid-run, and nothing analogous is true of a tool, since `DISABLE_COMPLIANCE_CHECKS` is irreversible as a fact about the world; a `Tool`'s four fields (`action_class`, `target_kind`, and the authoritative `reversible` and `blast_radius`) carrying **no `base[action_class]`**, because every risk component belongs together in item 7's table; and **no `params` field**, §3.1 staying at eight, because a ninth open field would be a validated-but-arbitrary channel onto the one object the determinism boundary is a pure function of. A test asserts the four-field set and another asserts `params`' absence.
   - **The first item with no live half, and that is the finding.** Items 2–5 each shipped a `scripts/` counterpart because each touched Cloud Trace, Cloud Run or Firestore. Item 6 touches nothing: the tool registry and the entity model are both in-code constants, so there is no state a script could read back that the test suite does not already assert. No script, no credentials, no cost — and `pytest` alone is the check, which is *why* the mutation testing below matters more here than usual.
   - **`validate()` accepts `object`, and that is what makes "free-form text" a real test rather than a claim.** §10 names three cases; the third only dies here if a bare string can actually be handed in. A `str`, `None`, a list, a dict with a missing key and a dict with an *extra* key all enter the same door and leave through one `ActionError` hierarchy — `NotATypedAction` / `UnknownTool` / `UnknownTarget` / `FieldMismatch`, which item 7 catches once and maps to `DENY(stage="schema")`. **No function returns an optional `Action` or `Tool`**; the same reflection test item 5 used guards it, because a forgotten `if action:` at stage 1 means the gateway scores something never validated and no later stage would catch it.
   - **§3.1's "not vibes" sentence is now three tests.** A Planner declaring `DISABLE_COMPLIANCE_CHECKS` reversible, or claiming `single-service` blast radius, or understating `SUP-042` as tier3, each raises `FieldMismatch` — and each of those lies is worth −3, −2 and −2 on the item-7 score, i.e. the difference between HOLD and auto-approve on the score-11 action. A fourth: a target of the *wrong kind* (`SUP-042` under `ROLLBACK_CONFIG`) is `UnknownTarget` even though the entity exists, which is what ADR-009 meant by "a tool schema that names which it expects".
   - **Deviation — the docs contradicted themselves and were reconciled, not picked between.** §2.1 lists schema validation as gateway **stage 1**, while §7.1, §10 and `THREAT_MODEL.md` all say it dies "before the gateway ever sees it". Both describe one design: validation is a standalone pure function run first, whose rejection reaches no identity check, no registry read, no ABAC and no risk score. §7.1 now says so in one sentence; §2.1's ordering is unchanged.
   - **ADR-010's loose end is closed.** Item 5 seeded `remediation-planner.tool_scope` with two bare strings and noted that "any further string would be a guess item 6 must honour or delete". A test now asserts every `tool_scope` string across `registry.AGENTS` names a real `TOOLS` entry, so a fourth agent seeded with a fictional tool fails the build instead of failing at item 7's ABAC stage.
   - **New shared code, in the two places it belongs.** `provenance/synthetic/company.py` gained `service(id)` / `supplier(id)` — the dict-comprehension pattern the tests were rebuilding per caller, landed once next to the data, both raising `KeyError`; items 7, 9 and 10 all need it. `provenance/telemetry.py` gained `TargetKind = Literal["service", "supplier"]` in the vocabulary block. **No span shape changed.**
   - **Stateless escalation, because §7.1 says the loop owns the count.** `MALFORMED_RETRY_BUDGET = 1` and `outcome_for(attempts)` → `REJECT` then `ESCALATE`; item 9's control loop keeps the count next to its `REFUTED` retry budget. Shipped and tested now so item 9 inherits a checked rule, the same way `registry.degraded_by_window()` shipped ahead of item 14.
   - **Sync, not async.** The project convention is async throughout; `registry.py` is async because Firestore is. `validate()` reads two frozen tuples, so `await` at every call site and `asyncio.run()` in every test would buy nothing and would imply an I/O boundary at the one stage that deliberately has none.
   - **Mutation-checked four ways**: a third `TOOLS` entry, `DISABLE_COMPLIANCE_CHECKS` flipped to reversible, the field-vs-authority check removed, and `MALFORMED_RETRY_BUDGET = 2` each turn the suite red. 76 tests pass on restore.
   - **Also learned — a generic enum helper does not survive mypy-strict.** `def _enum[E: str](...) -> E` fails with "a function returning TypeVar should receive at least one argument containing the same TypeVar": the Literal has to be named at the call site to narrow. Two monomorphic checkers (`_check_tier`, `_check_blast_radius`) in `registry._check_standing`'s shape are the working form.
   - **Cost.** None.
7. ~~Agent Gateway: identity (PortunusMCP broker, short-lived per-agent credentials) → RBAC/ABAC → deterministic risk table (§4.2) → ECDSA-sign → approve / hold / deny. Every outcome, including denials, signed into the audit stream. — verify: table-driven tests over action_class × tier × blast × reversibility; the two worked examples score exactly 2 and 11.~~
   — **done** (Aug 21): `provenance/risk.py` (§4.2's table), `provenance/credentials.py` (stage 2's assertion), `provenance/gateway.py` (the pipeline); `tests/test_risk.py`, `tests/test_credentials.py`, `tests/test_gateway.py` (52 + 23 + 45); live `scripts/verify_gateway.py`. 198 tests pass. `ARCHITECTURE.md` §2.1, §4.2, §8.1 and §10 gained as-built sections; `THREAT_MODEL.md` gained two assumptions and one tested claim.
   - **Eight design questions this item settled are recorded in [`ADR-012`](./docs/adr/ADR-012-the-gateway.md)**, and only there: the agent signing its own assertion (§2.1's identity stage contradicted itself), the registry read physically preceding the identity check, binding the credential to the action via `proposed_by`, splitting RBAC from ABAC because Portunus's grammar has no `in`, a scored DEGRADED hold beside an unscored SUSPENDED denial, our own `Decision` rather than Portunus's (closing item 0.5's open question), the `subject` field that binds the signature to what it decided, and the ephemeral per-process signing key. §8.1's span shape widened rather than a fifth shape appearing (`_opt_enum()` passes `None` through and `_set_attributes` already omitted it, so **absent means omitted, never emitted empty**; out-of-vocabulary values still raise) — module, §8.1 and `tests/test_telemetry_schema.py` moved in one commit, item 2's standing condition.
   - **Live half — verified.** Trace `fc1cb8c7b3de26ab8edd142b050c32b6` on `provenance-hackathon`: both worked examples authorized against the **live** Firestore registry — APPROVE at exactly 2, HOLD at exactly 11 — an unregistered id denied at `stage=registry`, all three signatures verifying, and all three spans read back from Cloud Trace with `1+1+0+0=2` and `4+2+2+3=11`. It mutates nothing. The three denial paths were also checked against live Firestore earlier in the item with a throwaway key.
   - **Offline half.** 120 new tests, no credentials. `tests/test_risk.py` sweeps all 2 × 3 × 3 × 2 = 36 combinations (§10's Risk-table row) — a lookup with a hole is a crash at authorization time, not a wrong number. `tests/test_gateway.py` reuses item 5's dict fake, because §10's Gateway row needs a store whose value can change *between two reads*. All nine `DecisionReason` values asserted reachable with the right stage.
   - **Mutation-checked seven ways:** `BASE["ROLLBACK_CONFIG"] = 2` (23 red), `CRITICALITY["tier2"] = 0` (17), the notify ceiling 6→7 (1), a real memoizing cache on `get_agent` (19, including the standing-flip tests in both files), skipping the credential expiry check (3), DEGRADED falling through to the risk band (2), dropping `set_outcome` (4), the `proposed_by` binding removed (1), and `subject` dropped from the signed hash (2). **A first attempt at the memoization mutation survived and was wrong, not the tests** — it read a cache it never populated. A mutation that passes is a claim about the mutation first.
   - **Registry state changed: `remediation-planner` is now `v2`.** `--rotate` was run once, because item 5 printed the original private half exactly once and it no longer existed. Rotation preserves standing and `rejection_window` and swaps only the key and version. Item 8 rotated again for the same reason, so the planner is now at **v3**; item 9 mints against whatever the record says rather than a hardcoded version, and `proposed_by` must match it or the gateway denies at `stage="identity"`.
   - **New in CI config:** `[[tool.mypy.overrides]] module = "services.*"` — `portunusmcp` ships no `py.typed` and `credentials.py` is the first module inside the package to import it; scoped to that one distribution, everything we write stays `--strict`. Also `tests/conftest.py`, holding the one global `TracerProvider`: OpenTelemetry allows it to be set once, and two test modules now emit spans, so the second `set_tracer_provider()` was being ignored and that module's exporter silently received nothing.
   - **Cost.** None.
8. ~~**Denial-by-registry, proven before any agent exists**: a scripted proposal from a SUSPENDED identity is denied, and a DEGRADED identity's low-risk proposal is held for human approval regardless of score. — verify: both denials appear in the signed ledger citing the registry entry as cause. *(This is the demo's registry beat — a denial that happens because of a registry entry.)*~~
   — **done** (Aug 21): one new file, `scripts/verify_denial_by_registry.py`, and no change under `provenance/` at all — `git diff --stat provenance/` is empty. **Verified live:** traces `c156252d5e73a61d694609d1eb5a8adc` and `cd2c7a2a64efabd7f3f197ea1866d934` on `provenance-hackathon`. No ADR: item 8 rejects no alternative and fixes no design that ADR-012 has not already recorded.
   - **One proposal, three standings — the whole point.** `ROLLBACK_CONFIG(inventory-api)`, §4.2's first worked example, is authorized three times against the live registry while the *only* thing that changes between runs is the stored `standing`: `GOOD` → APPROVE at 2, `SUSPENDED` → DENY, `DEGRADED` → HOLD. The GOOD control run is what makes it a proof rather than an assertion — without it, "denied" and "held" could be anything about the action. Nothing about the action ever moves; the same `dict` object is passed all three times.
   - **The `verify:` line is the span read-back, not the decision object.** "The signed ledger" is the signed `provenance.authorization.decision` stream item 7 wired, so the script polls Cloud Trace and asserts on what actually landed there: each span's `decision.stage`, `decision.reason`, `agent.standing` and a non-empty `decision.signature`. "Citing the registry entry as cause" is `stage="registry"` plus the standing that decided it, both read back off the wire. Rendering it on a screen is item 11's; §8.2's ledger surface is untouched.
   - **The absent risk block is checked, not assumed.** ADR-012's "a DEGRADED hold is scored; a SUSPENDED denial is not" was a decision with offline tests behind it; it is now checked against a real exported span. The `STANDING_DEGRADED` span carries `1 + 1 + 0 + 0 = 2` — held *despite* scoring 2, which is §3.4's "regardless of risk score" and the sentence item 31's card needs — and the `STANDING_SUSPENDED` span carries **no risk attributes at all**, confirming §8.1's "absent means omitted, never emitted empty" survives the round trip through Cloud Trace.
   - **The first script that writes to live state, so the restore is a real `try/finally`.** `scripts/verify_registry.py` restores with an unconditional line and no `try/finally`; item 8 deliberately departs, because `seed_registry.py` has no `--reset` and can never forgive a stranded `SUSPENDED` (ADR-010) — a crash between the flip and the restore would leave the fleet's only tool-scoped agent permanently denied, repairable by hand alone. **Exercised, not assumed:** a simulated Ctrl-C injected between the two flips exited 130 and left the record at `GOOD`. Worth backporting to `verify_registry.py` if that script ever grows a step between its flip and its restore.
   - **It refuses to run against a non-GOOD record.** If `remediation-planner` is stored as anything but `GOOD`, the script exits 1 naming `registry.set_standing` rather than proceeding: the control run would prove nothing, and the restore would write the stranded value back as though it were the baseline. The one assumption the script makes, checked instead of trusted.
   - **No new offline tests, deliberately.** Item 7 already ships `test_a_suspended_agent_is_denied_even_at_risk_2`, `test_a_standing_flip_between_two_authorizations_holds_the_second` and `test_a_degraded_hold_carries_the_arithmetic_a_suspended_denial_does_not`, all against the dict fake. A second offline module asserting the same three facts would be a second thing to keep true, not a second proof. 198 tests, unchanged.
   - **Mutation-checked**: letting DEGRADED fall through to the risk band in `gateway.py` turned the offline suite red (2 tests) **and** the live script red three ways — the decision assertion, the missing `STANDING_DEGRADED` span, and the control span coming back with `agent.standing="DEGRADED"`. **A first attempt at this mutation never applied** — the replaced string had the wrong indentation and the `assert` in the patch script caught it, so the "everything still passes" run that followed proved nothing. Item 7 recorded the same lesson: a mutation that survives is a claim about the mutation first.
   - **Registry state changed: `remediation-planner` is now `v3`.** `seed_registry.py --rotate remediation-planner` was run once, for exactly the reason item 7 ran it — the v2 private half was printed once during item 7 and no longer existed. Rotation preserves standing and `rejection_window`; only the key and version move. The script reads `agent.version` off the record rather than hardcoding it, so a future rotation costs nothing.
   - **Cost.** None. A handful of Firestore reads/writes and three spans per run, both inside the free tier; nothing bills while idle.

**Phase 3 — Incident #1 end to end (the fleet acts)**

9. ~~Trigger stream + Orchestrator (classify → recall → route, wake-on-event) + SRE/Infra Agent + Remediation Planner emitting one typed Action with a pre-declared success predicate. — verify: the injected `inventory-api` error-rate spike produces exactly one typed `ROLLBACK_CONFIG` proposal, risk 2, auto-approved.~~
   — **done** (Aug 21): **the first agent code in the repo, and the `verify:` line passes live against real Gemini.** `provenance/models.py` (per-role model strings), `provenance/incident.py` (the `Trigger`, the ADK graph, the control loop), `provenance/agents/` (`orchestrator.py`, `sre_infra.py`, `planner.py`, `_reasoning.py`), `POST /trigger` on `provenance/app.py`, a fifth span shape, `scripts/inject_fault.py` and `scripts/verify_incident_one.py`. 218 tests pass. `ARCHITECTURE.md` §5.3, §5.5, §7.1, §8.1 and §10 gained as-built sections and two new rows; `THREAT_MODEL.md` gained two assumptions.
   - **Design recorded in [`ADR-013`](./docs/adr/ADR-013-trigger-stream-and-incident-graph.md):** the trigger stream as a shared-secret-guarded `POST /trigger` rather than a Firestore listener (which needs an always-warm instance the cost ceiling forbids) and failing closed when the token is unset; the incident loop as a real ADK `Workflow` with §7.1's re-plan as an actual edge, the cycle checked in a throwaway spike before committing to it; and `Trigger` as a non-canonical object. (ADK 2.7.1's `google.adk.workflow.Workflow` is the Graph Runtime; `LlmAgent` is a `BaseNode` subclass, so agents drop straight into `edges`, a node sets `ctx.route` to pick a conditional edge, and the graph runs under `Runner(node=…)`.) The Orchestrator gets **no registry record** — it proposes no action and writes no belief, so §3.4 has nothing to record (`ARCHITECTURE.md` §5.3); its span's `agent.id` is `orchestrator@v1`, answering who reasoned, which is a different question from who may act. The fifth span shape `provenance.incident` and `predicate_id = sha256(success_predicate)[:16]` are §8.1, shipped under item 2's standing condition — module, §8.1 and `tests/test_telemetry_schema.py` in one commit.
   - **Verified live, four consecutive clean runs.** `scripts/verify_incident_one.py` injects the fault, wakes the fleet against live Firestore and live `gemini-2.5-pro`, and asserts one typed `ROLLBACK_CONFIG(inventory-api)`, `1+1+0+0 = 2`, APPROVE at `stage=risk`, a verifying signature — then reads the spans back from Cloud Trace. Trace `7104189bb5520f4fd66bdcfb14b12d1e`. **Also live on the deployed service:** one authenticated `POST /trigger` returned `AUTHORIZED` with the same `1+1+0+0 = 2`, with no IAM change needed.
   - **"Exactly one" is counted, not assumed.** The `verify:` line's first clause only means something if a second proposal would fail it, so the script asserts exactly one `provenance.authorization.decision` span on the trace and zero malformed re-plans. Two would mean §7.1's budget had been spent silently.
   - **Finding — naming a domain is not a vocabulary a model can aim at.** The first live run ended `UNROUTABLE`: with one domain registered and the bare word `infrastructure` to aim at, the Orchestrator reasonably decided a service error rate was an application concern and named something else. Fixed by giving each domain a scope sentence (`sre_infra.DOMAIN_SCOPE`) and making the escape hatch explicitly a last resort. The routing table now carries the scope with the agent id, because the Orchestrator's vocabulary and the routing table have to be the same list — a domain it can name but not reach is a silently unroutable incident.
   - **Finding — a poll loop that waits for success takes four minutes to report a failure.** `read_back` originally waited for the spans a *successful* incident emits, so the `UNROUTABLE` run above polled the full budget and then reported "never reached Cloud Trace" — the wrong finding, four minutes late. It now waits for the trace to stop growing and lets `check_spans` say whether that is the right set. Worth knowing for every later live script.
   - **The determinism boundary is where item 6 left it, and item 9 is the first thing that could have crossed it.** Three real model calls now sit between the trigger and the score. What they contribute is *which* action class and target; `action.validate()` still overrules the declared tier against the entity model and reversibility and blast radius against the tool registry, and `risk.score()` reads only the validated result. `tests/test_incident.py::test_a_planner_understating_the_tier_is_rejected_before_the_gateway` is the test that would still matter if every other guarantee held.
   - **Offline half: 25 new tests, no credentials.** A ~20-line fake `BaseLlm` (the same idea as item 5's dict `FakeFirestore`) answers from a queue, which is the only way to express a *sequence* of emissions — a real model cannot be asked to misbehave on cue. It covers what the live run cannot: the malformed re-plan, the second-strike escalation with the Planner's third reply left unconsumed, the rejection reason reaching the re-plan prompt, `UNROUTABLE`, and a DEGRADED planner held at the same score 2.
   - **Mutation-checked five ways:** `MALFORMED_RETRY_BUDGET = 2`, defaulting an unroutable domain to the SRE agent, `BASE["ROLLBACK_CONFIG"] = 2`, dropping the incident span's `set_outcome`, and detaching the reasoning span's `after_agent_callback` each turned the suite red. 218 pass on restore.
   - **Deviation — `--min-instances=0` costs the demo about a minute.** Three sequential `gemini-2.5-pro` calls take ~60s wall clock end to end, before any cold start. That is inside the demo's 3:40 only if the incident is already running when the beat starts; item 37 should measure it on camera rather than assume. `gemini-3.5-flash` for the classify step is the fallback if it drags — one line in `provenance/models.py`, which is what that file is for.
   - **Cost.** Three `gemini-2.5-pro` calls per incident, cents each; roughly a dozen live runs during this item. The shared-secret guard is the actual control on the public URL, not the budget alert.
10. ~~Execution path + Verification Agent (`CONFIRMED` path only for now) + first belief committed by a stub Policy Engine (full engine is Phase 4). — verify: rollback executes on the synthetic service, error rate drops, verification returns CONFIRMED against the predicate declared before execution.~~
   — **done** (Aug 22): **incident #1 finishes, and the fleet learns something.** `provenance/executor.py`, `provenance/policy.py`, `provenance/agents/verification.py`; two nodes and three edges appended to `provenance/incident.py`; `RESOLVED` on `telemetry.IncidentOutcome`; `scripts/verify_incident_one.py` extended rather than duplicated. 251 tests pass. `ARCHITECTURE.md` gained §5.8a (the executor, named for the first time), as-built sections on §5.8 and §5.10, two §7.3 rows and an §8.1 note; `THREAT_MODEL.md` gained two assumptions and two amended rows.
   - **Design recorded in [`ADR-014`](./docs/adr/ADR-014-execution-and-the-stub-policy-engine.md):** the executor re-verifying the signed decision rather than trusting its own branch (the first consumer of item 7's `subject` binding), `known_good_version` coming off the entity model rather than the Action (what ADR-011 removed `params` for — a test hands the Planner a predicate naming a *different* version and asserts v41 still lands), the stub Policy Engine answering `REJECT(SUPERSESSION_UNSUPPORTED)` on an existing v1 rather than overwriting or writing an unlinked v2, and `resolve` emitting the verification span because `belief_written` is not known until the commit has been attempted.
   - **Verified live, clean on the first run.** Fault injected, fleet woken against live Firestore and live Gemini, `ROLLBACK_CONFIG(inventory-api)` at `1+1+0+0 = 2` auto-approved, executed v42 → v41 with the error rate back to 0.01, `CONFIRMED` against the predicate `8e5abb9eae58789a` the incident span carried *before* execution, and `belief-inventory-api-1` committed at `0.6000` (`1 − (1 − 0.60)`). All five span kinds read back from Cloud Trace; trace `b6a5dbbe4a2ea0e15cdfe072730f82a0`. Baseline confirmed restored afterwards — v42, 0.01, `healthy`, fault off, belief deleted.
   - **Finding — ADK re-raises a failed node out of the root workflow.** `_node_runner` records the error and returns, but `Runner` then raises `DynamicNodeFailError`, so a Verification Agent that errors meant `resolve` never ran and an *executed* action reached the end of the loop with no verification span — reading as an incident nobody checked. §7.3 says such a failure is `INCONCLUSIVE`, so the control loop now catches it and emits the span itself. Same rule as §7.1's retry count: no agent owns its own failure semantics either.
   - **The offline suite covers what a live run cannot force.** A cooperative model will not answer `INCONCLUSIVE` on cue, so the fourth queued reply is the only way to express it; likewise `REFUTED` (asserted now as "writes nothing yet", so item 19 changes a red assertion rather than filling an unrecorded gap), a held incident proving it executed nothing, an execution failure, and an agent that holds the action authority but not the memory domain — the case where the two pipelines legitimately disagree.
   - **Mutation-checked five ways:** dropping the executor's signature check (3 red), assuming `rollback_fails` false without reading it (1), `base_weight["verified_system_observation"] = 0.90` (7), committing on `INCONCLUSIVE` (2), and removing `resolve`'s `set_outcome` (3). 251 pass on restore.
   - **Cost.** One `gemini-3.5-flash` call per incident on top of item 9's three Pro calls — seconds and cents. No new dependency, no new GCP resource, no idle billing.
11. ~~Trace UI renders the incident as it runs: live fleet view + gateway ledger reading the one OpenTelemetry stream. — verify: a cold browser session can watch incident #1 end to end without console access.~~
   — **done** (Aug 22): `GET /trace` in `provenance/app.py` over a span buffer in `provenance/telemetry.py`; the two surfaces filled in `provenance/web/index.html`. Offline halves `tests/test_app.py` + `tests/test_telemetry_schema.py`; live half is `scripts/verify_incident_one.py`, extended a second time rather than duplicated. **Three files under `provenance/`, no new dependency, no new GCP resource.**
   - **Design recorded in [`ADR-015`](./docs/adr/ADR-015-the-trace-ui.md):** the UI reading the **same span objects** Cloud Trace gets rather than a copy (a second `SpanProcessor` on the same provider holds them in a bounded deque, so §8's "the UI reads that same stream" is literal — Cloud Trace read-back was the obvious alternative and is the one thing that cannot work here, since item 2 measured 15s–2min indexing lag and the item says *as it runs*); capture at **span start**, because §8.2 says "current state" and a `reasoning.chain` span stays open for the whole ~20s model call; polling rather than SSE; and staying one static file with no build step — which answers ADR-008's revisit clause, re-deferred to item 30, the first surface that writes.
   - **`GET /trace` is unauthenticated on purpose.** The verify line says a *cold* browser, and item 36 says a judge with a README. A read spends nothing, which is the entire reason `/trigger` is guarded and this is not. What bounds it is §8.1's redaction rule, now checked three times: at emit, on the serialized payload in `tests/test_app.py`, and on the deployed bytes in the live script. `THREAT_MODEL.md` states exactly what is published rather than letting "read-only" imply harmlessness.
   - **Deviation — `configure_tracing()` is no longer a no-op without a project.** It always builds the provider and attaches the buffer; only the Cloud Trace exporter is conditional, and the return value narrows to mean "Cloud Trace export is wired", which is what `/health`'s `tracing` field always reported. Without this the UI is blank locally and in CI and the item has no offline half. `tests/conftest.py` attaches the same buffer for the reason that file exists: `TestClient` runs the lifespan, and OTel ignores the second `set_tracer_provider()`.
   - **Deviation — `--max-instances` dropped 3 → 1.** The buffer is per process, so a ~60s blocking `POST /trigger` and the 1/s polls beside it have to reach the same instance. `--min-instances=0` is unchanged, so this *lowers* the ceiling. It is also why the Firestore-mirror alternative was rejected: it solves a problem one flag solves for free.
   - **`POST /trigger` was not touched.** Backgrounding it would have changed item 9's documented response shape and broken the live script's assertions on its body; the UI simply does not await the fetch before it starts polling. Likewise the endpoint serves the whole buffer each poll rather than an `?after=` delta — a span mutates after it is first served, so a cursor needs all of the snapshot's de-duplication *plus* a sequence counter.
   - **The Executor gets no row, deliberately**, because it emits no span (ADR-014). Inferring one would have the UI assert state the trace cannot prove. The other four surfaces keep their placeholders; `tests/test_app.py` still pins all six names as literal strings.
   - **Finding — the buffer catches ADK's spans too, so it filters by name.** ADK auto-instruments roughly twenty spans per incident onto the same provider (`agent …`, `call_llm …`, one per graph node). They belong in Cloud Trace and reach it through the other processor, but carrying them into the UI buffer quadrupled the polled payload and cut its history to a quarter, so `on_start` keeps only names with the `provenance.` prefix — which is exactly what §8.1 says that prefix is for. The UI filtered them out by name anyway; this makes `BUFFER_SIZE` mean what it says.
   - **Fixed while verifying — the deployed half was mutating live state and never restoring it.** `check_deployed_route()` fired a real `POST /trigger` *after* `run()`'s `try/finally` had unwound, so a clean run left `inventory-api` at v41 with a belief present, which the next run's own precondition check would refuse over. It now takes the identical posture — `refuse_if_dirty()` (extracted from `run()`, one copy of the messages), `inject()`, `try/finally` `restore()` — and because it injects, it wakes the fleet against a *spiked* service instead of a nominal one: the deployed half went from asserting two span shapes on an ESCALATED incident to five on a `RESOLVED` one, for the same four model calls it was already spending. **One honest limit, verified with a SIGINT 25s into the trigger rather than assumed:** the incident runs in the *deployed* process, so the local `finally` restores correctly and the remote rollback can still land afterwards. Nothing local can order those two, so it prints a warning naming the remedy instead of reporting a clean exit.
   - **Mutation-checked four ways**: capturing on `on_end` instead of `on_start` (3 red), serializing an empty attribute map (3), guarding `/trace` with the trigger token (2), and renaming one surface heading (1). 258 pass on restore, up from 251.
   - **Verified live against a paced fake incident:** RUNNING rows appear mid-flight and settle to DONE.
   - **Cost.** No paid resource, no new dependency (`SpanProcessor` is `opentelemetry-sdk`, already transitive via `google-adk`), and a lower instance ceiling than before. The buffer is 500 spans of process memory in a 512Mi container — about seventy incidents, once ADK's own spans are filtered out of it.
11.5. ~~**The Planner must not write a predicate a successful rollback cannot satisfy.** Seed the target's nominal error rate into session state and require the declared threshold to sit strictly above it. — verify: ten consecutive incident #1 runs all reach `RESOLVED`, and no run's `success_predicate` names a threshold at or below the service's nominal error rate.~~
   — **done** (Aug 22): **two lines of behaviour change, and the ten runs that found the second defect.** One key in `provenance/incident.py`'s `_seed_state()` and one clause in `provenance/agents/planner.py`; offline `tests/test_incident.py::test_the_planner_is_told_what_healthy_looks_like`; live `scripts/verify_incident_one.py`, extended a **third** time with `--runs N`. 259 tests pass. `ARCHITECTURE.md` §5.5 gained a predicate-floor paragraph. **No ADR, no new dependency, no new GCP resource, nothing under `provenance/` but the Planner's prompt and the state it reads.**
   - **Found live, not designed — and verification is not the bug.** Three runs on Aug 22 produced two `CONFIRMED` and one `REFUTED`. The refuted run's predicate was *"the error rate for inventory-api is less than 1%"* and the post-rollback state was exactly `0.01`: the rollback worked and the predicate was still false. `REFUTED` was the honest answer, and §7.2's three-valued rule working is the whole reason the fleet is trustworthy — loosening the Verification Agent to accept a boundary miss would trade the property Phase 5 exists to demonstrate for a greener demo. The defect is upstream: `provenance/agents/planner.py` asks for "a concrete numeric threshold" while `provenance/incident.py`'s `_initial_state()` never tells the Planner what healthy looks like, so `1%` is a reasonable guess landing exactly on the baseline. **The fix is one state key and one instruction clause** — `nominal_error_rate` off `company.service(target).error_rate` (the frozen fixture value, not the spiked live one), and a sentence requiring the threshold to sit strictly above it. **No §3.1 change:** the Action stays at eight fields and `success_predicate` stays free text, because a typed threshold is exactly the `params` field ADR-011 removed, and judging a natural-language claim is the Verification Agent's job.
   - **Why this blocked Phase 4 rather than waiting for item 37.** Incidents #2 (item 18) and #3 (item 24) run this same Planner down this same path, and item 32's counterfactual A/B compares two runs of it. A one-in-three chance of a spurious `REFUTED` contaminates every measurement built on top of it, and item 20's REFUTED retry budget cannot be exercised honestly while REFUTED also fires by accident.
   - **Verified live, 10/10 `RESOLVED`.** `scripts/verify_incident_one.py --runs 10`. Trace `c7e4a8a0c8ef59df4edc14efeb3f4e09` (the tenth run; the read-back runs on the last one only). Baseline confirmed restored on all twenty-two live incidents this item spent. Thresholds named: `0.02` ×5, `0.015` ×2, `2%` ×2, `0.05` ×1 — **lowest `0.015` against a nominal `0.01`**, and every one parsed (no `WARN`). Before the fix, one run in three landed exactly on `1%`. The ten sentences:
       1. "The error rate for inventory-api is below 0.02 within 5 minutes, and the deployed config version is v41."
       2. "The deployed config version for inventory-api is v41 and its error rate is below 0.02 within 5 minutes."
       3. "Within 5 minutes, the error rate on inventory-api is below 0.015 and the deployed config version is v41."
       4. "Within 5 minutes, the error rate for inventory-api is below 0.02 and its deployed config version is v41."
       5. "The error rate for inventory-api is below 2% and the deployed config version is v41, observed over a 5-minute window."
       6. "The inventory-api service's error rate is less than 0.02 and its deployed config version is v41, measured over a 5 minute window."
       7. "Within 5 minutes, the error rate for inventory-api is below 2% and the deployed config version is v41."
       8. "Within 5 minutes, the error rate for inventory-api is below 0.015 and the deployed config version is v41."
       9. "Within 5 minutes, the error rate for inventory-api is less than 0.02 and the deployed config version is v41."
       10. "Within 5 minutes, the deployed config version for inventory-api is v41 and its error rate is below 0.05."
   - **Finding — the ten-run verification found a second defect, and it took a second clause.** The first ten-run came back **9/10**: run 1 declared *"the error rate … will be less than 0.02 **and the deployed config version will match the last known-good version**"* and got `INCONCLUSIVE`. The threshold was fine. The Verification Agent is shown the error rate before, the error rate now and the deployed version now — it is **never told what known-good refers to**, so it could not settle the second conjunct. A predicate can therefore fail two distinct ways: *unsatisfiable* (the floor) and *uncheckable* (a reference to a fact the judge is not given). The second clause requires literal values: write `"is v41"`, never `"matches the last known-good version"`. Ten clean runs after it, all naming the version literally. **It also exposed a contradiction:** the instruction already ended "Do not carry a version number in any field", which now said the opposite. Narrowed to "**Outside the success predicate**, do not carry a version number in any field … so a version in your sentence is something to be checked and never something to be obeyed." ADR-011's intent is untouched — the executor still reads `known_good_version` off the entity model, and `tests/test_executor.py` still hands a predicate naming a different version and asserts v41 lands.
   - **Also verified on the deployed service, which needed a redeploy to be true.** The hosted revision predated this item, so the public URL was still serving the Planner without the floor. Redeployed as `provenance-00011-fbm`; `PROVENANCE_TRIGGER_TOKEN` was recovered from the previous revision's environment rather than reminted. The deployed half then passed whole: 403 unauthenticated, 200 with the header returning `RESOLVED`, and `GET /trace` served to a cold client with five span shapes, risk components summing to 2, and no content-bearing attribute key. **Worth carrying forward: merging an agent-prompt fix does not fix the demo** — "no redeploy required" was true of the code and false of the thing judges use.
   - **The threshold check is assert-only, and in the script rather than in `provenance/`.** `check_result()` scrapes the rates a sentence names — percentages ÷ 100 and bare decimals below 1, so *"below 0.5% within 10 minutes … v41"* yields `[0.005]` and not `[0.005, 10, 41]` — and fails on any at or below nominal. Nothing parsed is a `WARN`, not a failure: a script that hard-fails on its own regex turns a green fleet run red for the wrong reason. Putting the same regex in `validate()` was rejected — it would spend §7.1's single retry budget on a failure that is not a schema failure, and natural-language parsing is exactly what the determinism boundary exists to keep out of the decision path.
   - **`--runs N` extends the existing script rather than adding a fifth.** Per-run inject/restore is required, not stylistic: the restore is what deletes the belief, and without it run 2 is refused `SUPERSESSION_UNSUPPORTED` by the stub Policy Engine. The loop finishes all N after a failure and tallies — "1 of 10" and "7 of 10" are different findings, and both defects here were intermittent. Read-back is on the last run only. `--runs 1` is the default and behaves exactly as before.
   - **Mutation-checked four ways:** sourcing `nominal_error_rate` from `trigger.observed_value` (the offline store holds the *spiked* 0.38, so this is the regression the test exists for), deleting the floor clause, dropping the percent form from the prompt, and deleting the literal-values clause. Each turned the new test red; 259 pass on restore.
   - **Cost.** Twenty-two live incidents, not ten — a two-run smoke test, the ten that found the second defect, and the ten that passed. About 66 `gemini-2.5-pro` plus 22 `gemini-3.5-flash` calls, roughly half an hour and low single-digit dollars.

**Phase 4 — Institutional memory (the differentiator — most time lives here)**
12. ~~Typed Evidence (§3.3) + the versioned belief store in Firestore: append-only versions, supersession links, full history, entity-keyed reads. — verify: committing a superseding belief leaves the old version intact and linked; nothing is ever deleted.~~
   — **done** (Aug 22): `provenance/beliefs.py` — §3.3's `Evidence` (moved out of `policy.py`), the `BeliefVersion` document, and `append`/`current`/`history`. `policy.py` keeps §2.2's pipeline and now writes a real superseding version; `SUPERSESSION_UNSUPPORTED` is gone from `CommitReason` rather than left unused. Offline half `tests/test_beliefs.py`, live half `scripts/verify_belief_store.py`.
   - **Design recorded in [`ADR-016`](./docs/adr/ADR-016-the-versioned-belief-store.md)**, which also answers ADR-014's revisit clause and ADR-011's `evidence_refs` note: append-only as **Firestore's** guarantee rather than our discipline (one immutable document per version at `beliefs/belief-{entity}/versions/{n}`, `create()` and never written again); links pointing **backwards only** with `superseded_by` derived on read (storing the backlink would mean writing to a committed version after the fact, so "intact" would mean "intact plus one field we added"); **no `current_version` pointer** (a pointer buys a failure window — a crash between the version write and the pointer write strands the belief at a version no later commit can extend); the version document written **last**, after the root and each `evidence/{id}`, since evidence nothing references is harmless while a version citing missing provenance is a belief that lies; and evidence normalised to `evidence/{id}`, `create()`-if-absent, which is what gives item 13's novelty check one place to read instead of N copies across versions.
   - **The verify line is asserted as *byte-identical*.** `tests/test_beliefs.py` snapshots v1's stored dict and compares it after v2 lands; the live script re-reads it out of Firestore and does the same. A test also asserts the write order rather than trusting the comment.
   - **A re-affirmation commits; a status flip is refused.** Same status with new evidence supersedes at the 0.50 door it already passed. A *different* status needs 0.70 **plus** §6.3's different-source-class rule, both item 14's, so it answers `REJECT("FLIP_UNSUPPORTED")` carrying the arithmetic. Shipping 0.70 without §6.3 would be strictly weaker than either document — one sensor could set and clear its own alarm.
   - **§2.2's stage order is unchanged: the registry is read before the store.** An agent whose standing has not been checked does not get a read of institutional memory performed on its behalf. The cost is that a stage-2 refusal reports `belief.version = 1` and no `supersedes`, because the engine never learned what was at stake; `decision.reason` says why it got no further.
   - **The decay clock is written now** (`half_life_days`, `expires_at`, `on_expiry`) though the Sweeper that consumes it is Phase 9's — three constants at commit time, so beliefs committed in between are not a backfill problem.
   - **No new span shape and no new attribute key.** `belief.supersedes` was defined in item 2 and item 12 is its first emitter; it carries the predecessor's **version number**, so the signature narrowed from `str | None` to `int | None` — the only telemetry change, and §8.1 lists no attribute types, so it needed no edit.
   - **Mutation-checked three ways**: swallowing `AlreadyExists` on the version write instead of raising `VersionConflict`, returning the oldest version from `current()`, and writing the version document before its evidence. Each turned exactly one test red; 271 pass on restore.
   - **Verified live.** `scripts/verify_belief_store.py` commits v1 and v2 against real Firestore through the real pipeline, asserts v1 unchanged and linked, the derived backlinks, both evidence documents, the refused flip, and that a direct re-append of v1 raises `VersionConflict` — then deletes everything it wrote in a `try/finally`. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60`, trace `292ade4d64980a4f3044bb878ec8f9d4`, baseline restored.
   - **Also in this item, unrelated to the store:** `AGENTS.md` and `.cursor/rules/provenance.mdc` are deleted and `CLAUDE.md`'s "Keeping the instruction files in sync" section with them. The project is worked in Claude Code alone; a copy frozen at whatever item last synced it is worse than no copy.
13. ~~Computed confidence (noisy-OR, §4.3) + the mechanical novelty check. — verify: ARCHITECTURE §10 confidence rows — unverified-claim-only evidence yields 0.00; restating one source N times equals stating it once; duplicate `(source_id, observed_at)` is not new.~~
   — **done** (Aug 22): `provenance/beliefs.py` gained `evidence_id`, `read_evidence` and `novel`; `provenance/policy.py` wired §2.2 stage 3 and widened stage 4 to the accumulated evidence set; one line of `provenance/incident.py` fixed the evidence id. Offline halves in `tests/test_policy.py`, `tests/test_beliefs.py` and `tests/test_incident.py`; live half `scripts/verify_belief_store.py`, extended rather than duplicated.
   - **Design recorded in [`ADR-017`](./docs/adr/ADR-017-novelty-and-the-accumulated-evidence-set.md):** comparing `(source_id, observed_at)` **pairs** rather than ids (two callers assign ids by hand, so an id comparison would rest on a convention nothing enforces — `verify_belief_store.py` keeps hand-written ids deliberately, as the case proving the check compares pairs); a version resting on **everything it ever rested on**, without which §6.3's own legitimate-update example is unreachable; and `NO_NEW_EVIDENCE` as a terminating refusal, the one addition to `CommitReason`.
   - **§4.3's confidence was already built; the item is really stage 3.** `policy.confidence()` shipped with item 10 doing the noisy-OR, the per-source-class collapse and the age decay, and three of §10's four confidence assertions were already green. Only *"age decay is monotonic"* was missing, and it is now a ladder of eight ages rather than the single point a sign error inside the exponent can fit.
   - **A `novel_count` that gates nothing is the cosmetic version of the boundary.** It was on the `belief.commit` span from item 10 as `len(evidence)`. §6.3 states novelty as a condition on the claim and §2.2 orders it *before* confidence, which only means something if the stage can terminate. An evidence-free *first* claim is `BELOW_THRESHOLD` at 0.00 instead — ADR-002's arithmetic, the same door that refuses a bare `unverified_external_claim`; "nothing new" is a statement about a belief that already exists.
   - **A live defect found by building the reader.** `incident.py` derived the evidence id from `action.predicate_id()` — the success-predicate *text* — while `append()` writes evidence create-if-absent. Two incidents phrasing the predicate identically shared an id at different observation times, so the second write was discarded, the version cited the id twice, and the stored document kept the first run's `observed_at`. `confidence()` decays from `observed_at`, so the survivor aged every later observation by however long ago the first run was. The id is now content-addressed from the pair, which makes the collision unrepresentable rather than unlikely.
   - **`half_life_domain` appeared in §4.3's formula with no value in any document.** Now published as 30 days beside the weights it multiplies, since ADR-002's defence of these numbers is that they are inspectable and fixed. The per-domain table moves to item 21.
   - **No new span shape and no new attribute key.** `provenance.evidence.novel_count` was defined in item 2 and item 10 emitted a placeholder into it; this is its first honest value. `decision.reason` is a plain string on the span, so a new `CommitReason` is not a telemetry vocabulary change.
   - **Mutation-checked six ways:** comparing ids instead of pairs, confidence over the proposal alone, the gate firing on a first belief, the evidence id back to the predicate hash, a version citing only what it proposed, and an unresolvable citation reading as absent. The first two failed immediately; the third and fourth survived the first pass and are the reason two more tests exist. 282 pass on restore.
   - **Verified live.** `scripts/verify_belief_store.py` green end to end against real Firestore — v2 accumulating both ids, a re-cited pair refused `NO_NEW_EVIDENCE` with no v3 written, a flip still refused `FLIP_UNSUPPORTED` on novel evidence, and v3 committing on the same sensor at a later reading — scratch state deleted on exit. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60`, trace `2ba3a15a45745e3b45753fe0fd49a5c2`, baseline restored.
14. ~~Memory Policy Engine (the mirror pipeline, §2.2): standing check, domain authority, novelty, computed confidence vs thresholds (0.50 new / 0.70 + different-class for a flip), version + sign + COMMIT/REJECT. — verify: a same-source-class flip attempt above 0.70 is rejected; with a different class it commits.~~
   — **done** (Aug 22): `provenance/registry.py` gained `record_rejection()`; `provenance/policy.py` gained `FLIP_THRESHOLD`, `COUNTED_REJECTIONS` and §6.3's rule; one comment in `provenance/incident.py` was repointed. Offline halves in `tests/test_policy.py` and `tests/test_registry.py`; live half `scripts/verify_belief_store.py`, extended a third time rather than duplicated.
   - **Design recorded in [`ADR-018`](./docs/adr/ADR-018-the-conflict-rule-and-the-standing-counter.md):** §6.3's class set read off **the version in force** rather than the proposal (ADR-017 left this open; stage 3 already resolves those `evidence/{id}` documents, so it costs zero extra reads, and accumulation makes it the stricter reading); the two failures keeping separate names — `BELOW_THRESHOLD` self-describing now that `belief.threshold` emits `0.70`, and `FLIP_UNSUPPORTED` meaning exactly one thing; and only refusals that are statements about the agent's *evidence* costing standing. No new `CommitReason`, no new attribute key, no new span shape. ADR-016's revisit clause hoped `FLIP_UNSUPPORTED` would become unreachable; the honest resolution is the opposite.
   - **Deviation: the Memory Analyst is deferred to item 23.** The item's title said "Memory Analyst recommends; the engine decides", which over-promised relative to its own verify line — that line is entirely about the engine and is checked with scripted proposals, the posture item 8 used to prove denial-by-registry before any agent existed. Deriving a *status* is not a model's job here: the control loop already knows deterministically what a confirmed rollback teaches, which is why `incident.BELIEF_STATUS` is a constant. The Analyst's first structural need is item 23, where extracting a shared signature across ≥3 entity beliefs genuinely requires one.
   - **One source class cannot reach 0.70, which is what makes the class rule load-bearing.** The strongest base weight is 0.60 and §4.3 collapses a class to its least-decayed item. So a same-class flip *above* 0.70 — the verify line's first half — is only reachable because a version rests on everything it ever rested on (item 13): the chain carries two classes and the proposal adds a third reading of one of them. Read the class set off the *proposal* instead and that case flips to a commit; that is mutation check 1.
   - **The window is append-only and the counter write is best-effort.** `degraded_by_window()` filters by cutoff on read, so pruning buys nothing and would be a delete path in the one subsystem built on never deleting. A `RegistryError` during the counter write is suppressed: nothing was committed either way, so it costs a missed increment rather than a wrong answer, and raising would turn a correct refusal into an exception out of the incident's resolve node. The window is therefore a floor on how many rejections happened, never a ceiling. `NO_NEW_EVIDENCE` counts deliberately — without it, item 28's poisoner could repeat one unverifiable pair forever and never reach the counter.
   - **The live script degraded its own agent, and that became a check.** The three refusals items 12–13's half deliberately provokes are exactly §2.2 stage 6's three, so `sre-infra-agent` hit DEGRADED in the middle of verifying itself and the subsequent flip failed as `STANDING_NOT_GOOD`. Asserting that (the window's three reasons in order, then reinstating) is worth more than arranging to avoid it: it proves live that the write is real and that these three reasons are the ones that count.
   - **Mutation-checked eight ways:** the class set read off the proposal, `FLIP_THRESHOLD` at 0.50, the flip check ordered before the threshold check, the class rule reduced to "any novel item", `COUNTED_REJECTIONS` widened to infrastructure refusals, `degraded_by_window()` applied to the pre-append list (degrades on the fourth, not the third), a pruned window, and the counter write left to raise. All eight turn a test red. 296 pass on restore, up from 282.
   - **Verified live.** `scripts/verify_belief_store.py` green end to end against real Firestore — v4 accumulating a second class to 0.82, a same-class flip refused `FLIP_UNSUPPORTED` at 0.82 with no evidence document written, the counter's three entries asserted and the agent reinstated, v5 committing `CLEARED` at 0.91 with `threshold` stored as 0.70 and citing all five items, then three unverifiable claims at 0.00 driving DEGRADED and a fourth ordinary write refused `STANDING_NOT_GOOD` — scratch belief deleted and the agent restored to GOOD with an empty window on exit. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60`, trace `d6f14db35c154697ec0783b97cee6298`, baseline restored.
15. ~~Retraction as a first-class transition + audit flagging of every action previously authorized on the retracted belief. — verify: ARCHITECTURE §10 retraction row.~~
   — **done** (Aug 22): one new module, `provenance/audit.py` (the authorization ledger); `provenance/policy.py` gained `retract()`, two `CommitReason` values and §6.4's gate; `provenance/incident.py` writes a ledger record after an approving decision and fails closed if it cannot. Offline halves `tests/test_audit.py` plus new cases in `tests/test_policy.py` and `tests/test_incident.py`; live half `scripts/verify_belief_store.py`, extended a fourth time rather than duplicated.
   - **Design recorded in [`ADR-019`](./docs/adr/ADR-019-retraction-and-the-audit-ledger.md):** why the ledger had to exist at all (nothing in the repo linked an action to a belief) and why the `target == entity` heuristic was rejected as falsifying the claim rather than proving it; why §6.4 is the rule for a retraction rather than an addition to §6.3 (§6.3 *delegates* the disproven case, and applying both would make a sensor's own correction impossible); why 0.50 over the **disproving evidence alone** is the only reading where every clause means something; flag-before-append and failing closed on both sides; and why `RETRACTION_UNSUPPORTED` joins `COUNTED_REJECTIONS` while `NOTHING_TO_RETRACT` does not.
   - **The honest cost of building the link before its producer.** `recall()` is item 16's and returns `()`, so every record a real incident writes cites nothing and a real retraction flags nothing. The mechanism is proven with hand-seeded records — item 8's posture for denial-by-registry, item 14's for the conflict rule.
   - **The arithmetic, stated once because it is the bullet.** At 0.50 over disproving-only: `verified_system_observation` 0.60 ✓, `third_party_audit` 0.55 ✓, `contractual_record` 0.50 ✓, `agent_inference` 0.15 ✗, `unverified_external_claim` 0.00 ✗ — the poisoner fails on both rules rather than one. **The two rules are independent, and one live case proves it:** `third_party_audit` at 0.55 clears the door and still fails the class test against a belief established at 0.60. That case is in the live script and in `test_a_strong_enough_number_still_fails_the_class_test`; without it, a suite where the threshold did all the work would pass.
   - **Nothing subtracts — which answers ADR-017's and ADR-018's revisit clauses with "confirmed", not an exception.** The `RETRACTED` version cites the accumulated set plus the disproving items, and no class leaves the flip-comparison set. So re-asserting a retracted status is an ordinary flip needing 0.70 *and* a class the whole chain does not carry: two sensors cannot retract and reinstate a belief by trading turns. ADR-016's third clause — is `RETRACTED` a status or a transition — resolves as *both*, describing different things: the outcome is what the engine decided, the status is what the store holds.
   - **The one new module keeps the belief store's contract literal.** `flag()` is an `update()`, and `beliefs.py`'s entire promise is that nothing in it modifies a committed document. An authorization record is not a belief, so the update lives in `audit.py` and "nothing under `provenance/` modifies or deletes a belief version" stays true without an exception attached. `tests/test_registry.py`'s fake store grew a `where`/`stream` pair — the first query support it has ever needed.
   - **No new span shape and no new attribute key** — the streak since item 12 holds. `BeliefOutcome` has admitted `RETRACT` since item 2 and this is its first emitter; `decision.reason` is a plain string, so two new `CommitReason` values are not a telemetry change.
   - **Mutation-checked eight ways:** the strength baseline read off the proposal, retraction confidence over the accumulated set, the door raised to 0.70, the flag moved after the append, `NOTHING_TO_RETRACT` counted against standing, the ledger query widened to flag every record, §6.3's flip rule left applying to retractions, and the incident's ledger write made best-effort. All eight turn a test red. 326 pass on restore, up from 296.
   - **Verified live.** `scripts/verify_belief_store.py` green end to end against real Firestore — a `third_party_audit` retraction refused `RETRACTION_UNSUPPORTED` *at 0.55, above the door*, with no v6 and no evidence document written; a `verified_system_observation` retraction committing v6 as `RETRACTED` at 0.60 against threshold 0.50, citing all six items; the ledger record citing this belief flagged at v5 while the control record citing another is untouched; a second retraction refused `NOTHING_TO_RETRACT` at no cost to standing — scratch belief, both ledger records and the agent's GOOD/empty-window state all restored on exit. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60`, trace `a181050a3f191877ed38a664b2dc505e`, baseline restored and no ledger record left behind.
16. ~~Recall: exact-key entity reads + the Vertex AI embedding index over belief statements — index nominates IDs only; the store resolves currency and drops RETRACTED/UNKNOWN(stale). — verify: a RETRACTED belief that is the closest embedding match is never handed to the Orchestrator.~~
   — **done** (Aug 22): one new module, `provenance/recall.py`; `provenance/beliefs.py` gained §3.2's `statement`, the root-document copy of it, `class_statements()` and `belief_id_for()`; `provenance/policy.py` gained `UNKNOWN`; `provenance/incident.py` lost its stub and calls the module; one new attribute key. Offline halves `tests/test_recall.py` plus new cases in `tests/test_beliefs.py`, `tests/test_incident.py` and `tests/test_telemetry_schema.py`; live half the new `scripts/verify_recall.py`.
   - **Design recorded in [`ADR-020`](./docs/adr/ADR-020-recall-as-built.md)**, which implements ADR-005 without revisiting it: no stored vector and no deployed index endpoint (a cost decision before a design one — Vector Search bills per node-hour); the statement copied onto the belief **root document**, which is what makes ADR-005's blindness structural rather than a convention; `recall.nominated_ids` published beside the survivors; the ledger citing **entity ids only**, making §6.2's advisory cap a property of a type; the two reads failing independently; and what `SIMILARITY_FLOOR = 0.55` was measured against — including that the same statements score **0.696 against a deliberately absurd cafeteria query**, so the floor ranks statements within one query and does not judge whether the query was sensible. The first draft of the live script tried to prove the floor with an unrelated query and would have enshrined the opposite conclusion.
   - **The one new attribute key ends the streak since item 12, deliberately** and under item 2's condition (module, §8.1 and `tests/test_telemetry_schema.py` in one commit). Still five span shapes.
   - **`UNKNOWN` is defined and filtered now, though item 29 is what writes it.** Recall drops `RETRACTED` and `UNKNOWN` together, so the Sweeper has to write a status and not also remember to fix a read path. A read path that learns about a status only when something starts writing it is one release of stale beliefs informing diagnoses.
   - **The offline half injects an embedder on purpose.** With a real one, "this belief is the closest match" would be a model's opinion, and a run where the retracted belief happened not to rank first would pass while proving nothing. Hand-picked vectors make the premise a fact offline; `scripts/verify_recall.py` is where a real model has to agree, and it asserts the **ranking before the drop** for exactly that reason.
   - **Mutation-checked six ways:** the `RETRACTED` filter removed, the `UNKNOWN` filter removed, the ledger citing class beliefs, the index resolving a version to read its statement (34 red), the statement kept off the root document, and an unreachable index nominating everything. All six turn a test red. 348 pass on restore, up from 326. Mutation check 3 — citing `belief_ids` instead of `entity_ids` — needed its incident test written *before* the mutation failed, which is how the gap was found.
   - **Verified live.** `scripts/verify_recall.py` green end to end against real Firestore and a real `text-embedding-005`: the RETRACTED belief ranked first among nominations, was recorded as nominated, and appeared in neither what recall handed over nor the text an agent is shown; the weaker current class belief survived, which is what proves the drop was specific rather than recall returning nothing; the entity belief came back by exact key carrying no statement; the other-domain belief fell under the floor and was never nominated — all four scratch beliefs deleted on exit. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60`, trace `20112d7f984829ebc7dd9b1900639e27`, baseline restored. Incident #1 still recalls nothing and cites nothing, which is honest: it is the fleet's first encounter with the service, and item 18 is the beat where memory pays.
17. ~~Seed the `SUP-042` AT_RISK belief + belief inspector UI (evidence, arithmetic, supersession chain, decay clock). — verify: the inspector shows the computed confidence breakdown for the seeded belief.~~
   — **done** (Aug 23): `provenance/policy.py` gained `Contribution` and `contributions()`, with `confidence()` folded onto it; `provenance/app.py` gained `GET /belief/{entity}`, the first route here that reads Firestore; `provenance/web/index.html` filled §8.2's third surface. Two new scripts, `scripts/seed_belief.py` and `scripts/verify_belief_inspector.py`. Offline halves in `tests/test_policy.py` and `tests/test_app.py`.
   - **Design recorded in [`ADR-021`](./docs/adr/ADR-021-the-belief-inspector.md)**, which amends ADR-015 and answers the ADR-019 revisit clause naming this item: the span stream *structurally* cannot serve this surface (§8.2 asks for evidence, arithmetic, a chain and a decay clock — all content, which §8.1 keeps off spans, so no sixth shape could close the gap), so the UI has a second data source and ADR-015's "one stream" claim carries a stated exception; live Firestore rather than a snapshot, because items 28 and 29 both write to this belief; and §4.3 getting exactly **one** implementation in `contributions()`, since a breakdown that can disagree with the number it explains is worse than none. Tested as `Π(1 − w) == 1 − confidence()` over a table — the components-must-sum rule `set_risk()` already enforces, applied to a product.
   - **Two versions, and the flip is real.** `FLAGGED` **0.575** at T−8d, superseded by `AT_RISK` **0.770** at T. v2 clears `FLIP_THRESHOLD` *and* rests on `third_party_audit`, a class the chain did not carry — the first time §6.3's rule runs on the demo's own belief rather than a scratch fixture. The eight-day gap is load-bearing: each version's evidence is fresh at its own commit, so v1 clears 0.50; back-date the pair twelve days instead and v1 scores 0.4495 and is refused `BELOW_THRESHOLD`.
   - **§3.2's canonical figure does not compute, and now says so.** It shows `conf 0.71` on one evidence item and `conf 0.94` on three. Neither is reachable: the strongest base weight is 0.60 and the formula collapses a class to its least-decayed item, so one item caps at **0.60** and all five classes fresh cap at **0.9235**. The figure is kept — inherited verbatim from the spec — with an as-built note giving the real numbers. ADR-002 defends these weights as inspectable and fixed; a canonical example that fails inspection undercuts that more than an admitted approximation does.
   - **Create-if-absent with no `--reset`, a sharper version of `seed_registry.py`'s reason.** That script refuses to reset because standing is never quietly forgiven. Here the stakes are the demo's closing shot: items 27 and 28 attack this belief and the point is that it survived, so a re-run rewriting a poisoned-then-defended chain would erase the only thing that beat proves. It refuses outright rather than skipping quietly.
   - **`flagged_by` is deliberately not rendered**, which answers ADR-019's revisit clause with "not yet". `SUP-042` is never retracted and no `authorizations/{id}` cites it, so the field is an empty array on every load — a surface wired for a case it cannot show is untested code shaped like a feature. Item 31's approval cards are the first with a flagged action to display, and the "should the flag emit a span" question is a §8.1 change belonging with that surface.
   - **Mutation-checked six ways:** the per-class `max()` collapse dropped, a row per evidence item rather than per class, `confidence()` reading `base` instead of the decayed `weight`, an unreadable store answering 404 instead of 503, v2's flip proposed on a class already in the set (refused at 0.5033, below the door), and v1's evidence back-dated twelve days (refused at 0.4495). The first survived on the first pass — `contributions()` was tested with the fresh item last, where "strongest wins" and "last wins" agree — and the test now passes both orderings. 358 tests pass, up from 348.
   - **Verified live.** `scripts/seed_belief.py` green against real Firestore, committing v1 at 0.5750 and v2 at 0.7698 through the real pipeline, with `suppliers/SUP-042` untouched and still carrying no status; the re-run guard then refuses as designed. `scripts/verify_belief_inspector.py` green: the breakdown multiplies back to the number beside it, every version's stored confidence still recomputes from its own citations at its own commit time, the backlink is derived rather than stored, `expires_at` is `committed_at + 30d`, and an unknown entity is 404. The rendered surface was exercised against the live payload in a DOM harness — the total rendered equals the route's `confidence_now`, and two of three rows carry an eight-day age. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60` (trace `5864ac5e2d58574cac42f8077718ac9b`), and `SUP-042` — scope `ENTITY`, so carrying no statement — is correctly absent from the recall index.
18. ~~**Incident #2 — the fleet remembers**: same service, similar deviation; the Orchestrator recalls the prior belief before diagnosis completes and prioritizes the config-regression hypothesis. — verify: the recall event appears in the trace before the domain agent's first hypothesis.~~
   — **done** (Aug 23): **no change under `provenance/` at all** — one flag on `scripts/verify_incident_one.py` (`--remember`), two cases in `tests/test_incident.py`, and nothing else. Item 8's posture, for item 8's reason: every mechanism this item names already shipped, so item 18 is the proof rather than the build. No ADR, because item 18 rejects no alternative ADR-015 and ADR-020 have not already recorded.
   - **The verify line was already structurally true, and completely untested.** `recall.recall()` is awaited in `run_incident()` *before* `build_graph()`, so memory is resolved before the Orchestrator agent is constructed, and its two facts ride as attributes on every `reasoning.chain` span from `classification` onward. What was missing was a run in which `beliefs/belief-inventory-api` exists when the incident wakes — `refuse_if_dirty()` refuses precisely that state — and any assertion anywhere that spans are *ordered*. There was none in the suite before this item.
   - **No sixth span shape, and the ordering assertion is stronger than the event the line asks for.** A `provenance.recall.event` would buy one duration and cost §8.1 a shape, its tests and its documentation in the same commit. Instead: the `classification` span carries `provenance.recall.belief_ids` containing the belief *and* starts before the `diagnosis` span. An attribute set at span-open time on a span that opened earlier is not a weaker fact than an event — it is the same fact without a shape.
   - **The Orchestrator's prompt was deliberately not touched, which closes item 16's deferral with a reasoned no.** Item 16 recorded that "item 18 owns the prompt work". The answer is that there is none to do: §5.3 and ADR-013 split the Orchestrator's three verbs so that only *classify* is reasoning, and feeding memory into a **routing** prompt would let a belief bias which domain owns a deviation — the one decision in the loop with no downstream check. §6.6's "hands the Orchestrator the governed object" is already satisfied by `incident.py` doing the lookup on its behalf.
   - **The item does not claim memory *caused* the hypothesis, and says so.** `sre_infra.py` has carried its own config-regression hint since item 9, so incident #2 would select `config_regression` with or without a recalled belief — the claim is unfalsifiable as the line words it. The script asserts what is provable (recall arrived first, the diagnosis named config, the belief superseded, the ledger's cold/warm contrast) and leaves causation to item 32's `--memory-disabled` A/B. Removing the hint to make the claim falsifiable was rejected: incident #1 is a cold case and would lose its only hint, putting the verified 10/10 at risk and contaminating items 19, 20 and 32, which all run this path.
   - **v2 commits at 0.60, not higher, and that is §4.3 working.** Incident #2's belief is a re-affirmation, not a flip: same status, same `verified_system_observation` class, and the formula collapses a class to its least-decayed item. Memory accumulated **evidence** — v2 cites both items and carries `supersedes: 1` — without accumulating certainty. Anyone expecting the number to climb is expecting the noisy-OR to double-count one sensor, which is the thing it exists not to do.
   - **The teardown split is the item's one real piece of engineering.** `restore()` became `restore_service()` (the three service fields and the switch) plus the two deletes. Between the runs only the first is called, so the fixture goes back to a spiked v42 while the belief and the first ledger record survive — `inject()` alone cannot do it, because it never writes `current_config_version`, which run 1's rollback moved to v41. One `refuse_if_dirty` and one `restore` span both incidents, in a single outer `try/finally`: if run 2 raises, run 1's belief still has to be torn down.
   - **The ledger's cold/warm contrast is the half that lives in stored state.** Incident #1's `authorizations/{id}` cites `[]`; incident #2's cites `["belief-inventory-api"]`. Asserting both is what makes it evidence — "some record cites the belief" would also pass on a fleet that cited it every time, which is what a broken cold case looks like. Item 16's control-record lesson, applied to a real incident instead of a fixture.
   - **Mutation-checked five ways**: `_seed_state()` handed an empty `Recalled` (the ordering test goes red); the `start_time` comparison reversed (red); the two runs given one `now`, collapsing §2.2's `(source_id, observed_at)` novelty pair (`REJECT`/`NO_NEW_EVIDENCE`, red); a v2 belief span checked with `expect_supersedes=None` and a v1 span checked with `"1"` (both red, both directions); the ledger contrast weakened to accept `[[belief], [belief]]` and `[[], []]` (both correctly rejected). A sixth is proven by the two live runs together rather than by a patch: `--runs 2` calls the **full** `restore()` between runs and commits v1 twice, `--remember` calls `restore_service()` and commits v2 — which is exactly the mutation and its control, observed. `--remember --runs 2` is refused. 360 tests pass, up from 358.
   - **Verified live.** `scripts/verify_incident_one.py --remember` green end to end (trace `5e769c328d1905f1b20cc44e4d4bd2ca`): both incidents `RESOLVED`, the second's `classification` span carrying `belief-inventory-api` and opening before `diagnosis`, `selected_hypothesis` `config_regression`, the belief `COMMIT` at v2 / 0.60 / `supersedes 1`, and the ledger showing `[]` then `["belief-inventory-api"]` — everything deleted and the fixture back to v42 on exit. The regression run matters as much: `--runs 2` still reaches `RESOLVED` twice with a **v1** belief each time and no `supersedes` on either span (trace `603a4493f510f2ae210e55b57dd0a17a`), so extracting `_one_incident()` out of `run()` cost the item-9/10/11/11.5 paths nothing. `scripts/verify_belief_store.py` is green end to end after the split, showing `REJECT/INSUFFICIENT_FOR_FLIP at 0.60` costing nothing and three `REJECT/BELOW_THRESHOLD at 0.00` driving the counter 1 → 2 → 3 → DEGRADED. `SUP-042` untouched throughout.

**Phase 5 — Verification honesty (three-valued outcomes, exercised not designed)**
19. ~~`REFUTED` and `INCONCLUSIVE` paths: negative beliefs written on confirmed refutation; nothing written on ambiguity; verification errors/timeouts treated as INCONCLUSIVE. — verify: fault-inject a failed rollback → REFUTED → negative belief; force ambiguity → INCONCLUSIVE → no write.~~
   — **done** (Aug 23): **Phase 5 opens, and verification stops being three-valued only in the type system.** Four lines in the `execute` node, one field on `ExecutionResult`, a two-entry dict in `provenance/incident.py`, two flags on `scripts/inject_fault.py`, and the new `scripts/verify_refuted.py`. **`provenance/policy.py` is unchanged apart from one refusal split** — that is the item's main finding, not an omission. Offline halves in `tests/test_incident.py` and `tests/test_executor.py`.
   - **Design recorded in [`ADR-022`](./docs/adr/ADR-022-the-refuted-and-inconclusive-paths.md)**, which answers the open fork in ADR-019's revisit clause and the `REFUTED` half of ADR-014's: a refutation **commits and does not retract** (§6.4 is for a belief that turns out wrong, and `CONFIG_REGRESSION_PRONE` is not wrong when a rollback fails — retracting would erase a belief on evidence that does not bear on it *and* flag every action authorized on it, a false alarm manufactured by conflating two claims); the status being `ROLLBACK_INEFFECTIVE` rather than the tempting `NOT_CONFIG_REGRESSION_PRONE`, which asserts more than a re-read can support; and ambiguity being forced by making verification **not happen**, because against a predicate reading "below 0.015" a competent model answers `REFUTED` to an error rate of 0.19, so a half-fixed post-state would flip between outcomes. ADR-014's clause independently predicted the shape ("a second `_resolve` branch, not a reshape") and that is what shipped.
   - **The one change to `policy.py`: `BELOW_THRESHOLD` carried two incompatible meanings.** §3.4's counter sits downstream of exactly the distinction the name was eliding: "your evidence could not support a belief at all" — the poisoner's `unverified_external_claim` at 0.00 — must cost standing, and "your evidence was good enough for a new belief and could not overturn one" must not, or an SRE agent degrades for correctly reporting that its own remediation failed. The refusal now splits on `NEW_BELIEF_THRESHOLD`: **`INSUFFICIENT_FOR_FLIP`** above it, new and deliberately **not** in `COUNTED_REJECTIONS`, and `BELOW_THRESHOLD` below it, unchanged and still counted. The frozenset itself is untouched at four reasons. `FLIP_UNSUPPORTED` needs no equivalent — an incident-written belief only rests on `verified_system_observation`, which caps at 0.60, so a warm refutation always meets the number before reaching the class check. Rejected alternatives: a "do not count this" flag from the control loop (the engine would decide by *who is asking*, which §1.1 property 2 forbids) and having the loop read `current()` and skip the commit (it would pre-empt the engine and erase the refusal from the trace).
   - **The warm-chain refusal is §6.3 working, and it has a test rather than a paragraph.** One belief chain per entity, so on a service already carrying the confirmed status the negative belief is a status *flip*: 0.70 plus a source class the chain does not carry, and one fresh `verified_system_observation` is 0.60 in a class already present. Refused on the number, confirmed version stands. Neither item 19 nor item 20 reaches this — both run cold — so it exists only in `test_a_refutation_cannot_flip_a_confirmed_belief_on_its_own`.
   - **One live script changed with it, and the change is an improvement.** `scripts/verify_belief_store.py` used to observe that the three refusals it provokes were "exactly §2.2 stage 6's three", so `sre-infra-agent` degraded in the middle of its own verification. One of those three is now uncounted, so it asserts two window entries, a standing of `GOOD`, and that `INSUFFICIENT_FOR_FLIP` is *absent* — the carve-out proven live. It still clears the window before continuing, because item 15's counted `RETRACTION_UNSUPPORTED` would otherwise be a third. §3.4's "third counted refusal → DEGRADED" is unaffected and still proven by `standing_checks()`.
   - **§7.2's table is a dict, not a branch.** `_LEARNS_FROM` maps `CONFIRMED`/`REFUTED` to the status each teaches and **omits `INCONCLUSIVE` entirely** rather than mapping it to `None`. "No partial credit" is therefore the shape of the data: committing on ambiguity means adding a key, which is a mutation the suite catches three ways.
   - **The switch is read *after* `read_state()`, and that ordering is the whole claim.** Read before execution, nothing would have run and "no belief was written" would be explained by an incident that stopped early — a green check proving nothing. Read after, the rollback lands, the service is genuinely repaired, and the only missing thing is the confirmation. `verify_refuted.py` asserts the repair (v41, rate back to nominal) precisely so the absent belief has one explanation.
   - **Two halves in one script, because either alone is weak evidence.** "Ambiguity wrote nothing" passes on a fleet whose memory is broken; "refutation wrote a belief" passes on a fleet that commits on anything. Item 16's control-record lesson again. The script **imports its teardown from `verify_incident_one.py`** rather than copying it — a deliberate break from the standalone-script convention, because these two are the only things writing this fixture and two restore paths over one fixture drift. That import forced a real fix: `restore_service()` and `refuse_if_dirty()` knew only about `error_rate_spike` and now cover **all three** switches.
   - **No sixth span shape and no new attribute key** — the streak since item 12 holds. `belief_written=true` on a span whose outcome is `REFUTED` is a value the item-2 vocabulary already admits; `telemetry._ERROR_OUTCOMES` correctly keeps `REFUTED` and correctly omits `INCONCLUSIVE`. `attempt` stays 1 — item 20 is what moves it.
   - **Mutation-checked eight ways:** `REFUTED` writing `BELIEF_STATUS` (2 red); `REFUTED` ending `RESOLVED` (1); the ambiguity switch read *before* execution (1); the switch ignored so the graph always verifies (1); `INCONCLUSIVE` added to `_LEARNS_FROM` (3); `REFUTED` dropped from `telemetry._ERROR_OUTCOMES` (2); and the carve-out boundary moved **down** to 0.0, which would exempt the poisoner (1), and **up** to `FLIP_THRESHOLD`, which would exempt nothing (2) — both directions pinned. A ninth, dropping the `flip and` guard from that ternary, is correctly green: inside `conf < threshold` a non-flip has threshold 0.50, so the guard is redundant and kept only for readability. 364 tests pass on restore, up from 360. *(A stale draft of this block reading "six ways … 363 tests" sat in item 13's slot until the record was reconciled against git — item 20's "up from 364" and item 14's "up from 282" fix both counts.)*
   - **Verified live, both halves and the regression.** `scripts/verify_refuted.py` green end to end: the REFUTED half (trace `f11db76a468be2e1b237beb33704998d`) executed v42 → v41, left the rate at 0.38, returned `REFUTED` against the predicate `62a76c98690305a4` declared before execution, and committed `belief-inventory-api` v1 `ROLLBACK_INEFFECTIVE` at 0.60; the INCONCLUSIVE half (trace `e2f5479f74b8a694427614582d4860c9`) rolled back *successfully* to v41 at 0.01, returned `INCONCLUSIVE`, wrote no belief document at all, and emitted five fewer spans — the Verification Agent was never invoked. Baseline confirmed restored: v42, 0.01, healthy, all three switches off, no belief, no ledger record. `scripts/inject_fault.py --rollback-fails` / `--ambiguous` / bare / `--clear` each read both documents back and exit 0, an unknown flag exits 1, and an unnamed switch is written *off* rather than left set. `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at 0.60 and `1+1+0+0 = 2` (trace `e51b7d46f88ecfdac5a5debead29a564`), so widening the shared teardown cost items 9–11.5 and 18 nothing. `SUP-042` untouched throughout.
20. ~~Bounded retry owned by the control loop: one re-plan with the refutation as input, then mandatory escalation. — verify: two consecutive REFUTED outcomes escalate; no third attempt occurs anywhere in the trace.~~
   — **done** (Aug 24): **one edge, and three sentences found by running it.** `resolve` becomes a routing node, the graph gains `(resolve, {"REPLAN": planner_agent, "DONE": halt})`, and `attempt` on the verification span finally exceeds 1. Every document in the repo has claimed this guarantee since the spec; this is the edge behind it. **`provenance/policy.py` is unchanged**, which is item 19's finding holding a second time. Offline halves in `tests/test_incident.py`; live half `scripts/verify_refuted.py`, extended rather than duplicated.
   - **Design recorded in [`ADR-023`](./docs/adr/ADR-023-the-bounded-retry.md)**, which answers the revisit clause in ADR-022 and ADR-013's "does not implement item 20" note: **two budgets rather than one** (`scratch.refuted_attempts` against `REFUTED_RETRY_BUDGET = 1`, beside item 9's `malformed_attempts` — collapsing them would mean a Planner that fumbled a field once loses the fleet's only chance to actually fix the service; one incident may spend both, three Planner calls, still bounded, and `test_the_two_retry_budgets_are_independent` says so), the constant living in `incident.py` rather than beside `action.MALFORMED_RETRY_BUDGET` (`action.py` is about what an Action *is*, and a remediation that ran and failed is not a schema fact), and the per-attempt observation stamp below.
   - **ADR-022's prediction held, and the v2 was unreachable as written.** The retry's commit is a re-affirmation of `ROLLBACK_INEFFECTIVE` at the 0.50 door, superseding the v1 at the same 0.60 — evidence accumulating without certainty accumulating, item 18's lesson inside one incident. But `_commit_belief()` stamped `observed_at` from the incident's frozen `now`, so both attempts produced an identical `(source_id, observed_at)` pair and §2.2 refused the second `NO_NEW_EVIDENCE` — **a counted rejection**, so every bounded-retry incident would have charged `sre-infra-agent` standing for honestly reporting that its own remediation failed twice. Three inside 24h is DEGRADED. That is the pathology item 19 split `INSUFFICIENT_FOR_FLIP` off `BELOW_THRESHOLD` to prevent, arriving by a different door. Each attempt now stamps its own observation time when `read_state()` returns.
   - **That fix is invisible offline, and the split is stated rather than hidden.** `beliefs.TIMESTAMP` has second resolution and a `FakeLlm` finishes both attempts inside one second, so offline the second commit is *correctly* refused — the same property item 18's supersession test already documented. **The offline suite proves the bound; `scripts/verify_refuted.py` proves the v2.** Reverting the stamp leaves all 370 tests green and turns that script red, which was checked by doing it rather than by reasoning about it. Widening `TIMESTAMP` to milliseconds (it rewrites a stored format `seed_belief.py`'s SUP-042 chain and the inspector both read) and a monkeypatchable clock (a seam that exists only for tests) were the alternatives.
   - **Three prompt sentences, all found live, and the history is the interesting part.** *One:* told only that its remediation had been refuted, the Planner concluded the config was innocent and proposed `DISABLE_COMPLIANCE_CHECKS(inventory-api)` — a supplier-scoped action aimed at a service. `action.validate()` rejected it twice and the incident escalated on the **malformed** budget having never made its second attempt: §7.1's first bullet working perfectly and item 20 starved by it. `known_action_classes` now renders each class with the entity kind it acts on, off `tools.TOOLS`. *Two:* that alone was not enough — a model that wants a different class resolves the conflict by keeping the target — so the retry text states the target's kind as a fact, read via `tools.tool_for()`. *Three, and sharper:* handing the Planner the rate its failed attempt left behind invites it to declare success against it. Told the error rate was 0.38 it declared "below 0.40", and its own refutation verified `CONFIRMED` and the incident closed `RESOLVED`. Item 11.5 gave the predicate a **floor**; the retry needed a **ceiling**. Both bounds say one thing: a predicate must be satisfiable by success and unsatisfiable by the failure it is meant to clear.
   - **No sixth span shape and no new attribute key.** `provenance.verification.attempt` was defined in item 2 and pinned to 1 since item 10; this moves it. "No third attempt anywhere in the trace" is proven by **counting** — two verification spans numbered 1 and 2, two authorization spans, two planning chains, nothing at 3. Four counts rather than one, because two verifications alone would also be produced by a loop that re-verified one execution without going back through the gateway.
   - **Two ledger records, and that is correct.** The retry goes back through `gateway.authorize()` — there is no path around it and item 20 built none — so two actions really were authorized and §6.4 has to be able to flag either. `INCONCLUSIVE` is never retried (`== "REFUTED"`, not `!= "CONFIRMED"`): §7.2 gives ambiguity its own row, and a test holds the difference.
   - **No fourth fault switch.** The successful retry — attempt 1 REFUTED, attempt 2 CONFIRMED — is an offline test and nothing live, because `rollback_fails` is a switch and not a coin. A "fails once" switch would cost a field on the frozen `FaultSwitch`, a flag on `inject_fault.py`, seeded documents and a teardown widening, to demonstrate a branch the offline suite already pins.
   - **Mutation-checked seven ways:** budget 2 (the "never asked a third time" test red), budget 0 (the happy-retry test red), the counter incremented before the span opens (`attempt` reads 2,3 — red), the retry condition widened to `!= "CONFIRMED"` (the INCONCLUSIVE test red), `resolve` routing DONE unconditionally (both retry tests red), the trace UI kept on `.find()` (the DOM harness shows attempt 1 twice), and the per-attempt `observed_at` reverted — **green offline and red live**, `REJECT/NO_NEW_EVIDENCE` at confidence 0.00, which is the one mutation only the live script can hold and the reason it exists. 370 tests pass, up from 364.
   - **Verified live.** `scripts/verify_refuted.py --refuted` green end to end (trace `16c1591ca4aed16682cbb75d374cd36f`): two attempts, both `REFUTED` against predicates declared before each execution, `belief-inventory-api` v2 `ROLLBACK_INEFFECTIVE` at 0.60 superseding v1 and citing both observations, two ledger records, `ESCALATED`, and the trace numbering the attempts 1 and 2 with nothing at 3. `--inconclusive` unchanged and green (trace `29b28c3a2c3650c7154fad4c91b3c3d1`). All four regressions green: `verify_incident_one.py` still `RESOLVED` at `1+1+0+0 = 2` with one belief at 0.60 (trace `cf7a897f6fb426ab8eccb7f9df2ceb6d`), `--remember` still v1 → v2 with the cold/warm ledger contrast (trace `1a437ffbe1ddc927d05d5b426a97d709`), and `verify_belief_store.py` green end to end, so item 28's poisoning arc is provably untouched by a change that lives next to `COUNTED_REJECTIONS`. `SUP-042` untouched throughout.

**Phase 6 — Second domain (generality proven with a number)**
21. ~~Supply-Chain Agent + its registry entry, running the same incident loop. Also the first item with two domains writing beliefs, so §4.3's `half_life_domain` becomes a table here rather than the single published number item 13 left.~~ — verify: a supplier-disruption trigger routes, diagnoses, and proposes through the identical control plane.
   — **done** (Aug 24): `provenance/agents/supply_chain.py` and `scripts/verify_supply_chain.py` are the two new files; a typed `Domain` entry and changed lines across eight existing modules are the honest cost, every one named in [`ADR-024`](./docs/adr/ADR-024-the-second-domain.md) rather than engineered around, and measured by item 22. **384 tests pass, up from 370.** Verified live: `scripts/verify_supply_chain.py` green end to end (trace `2630440d62a8fba2405ee6510931b2e5`).
   - **Design recorded in ADR-024** — the five things the loop was quietly single-domain or service-shaped about: the graph building one domain node (nodes *and* the routing edge map now comprehended out of `DOMAINS`); session state seeded from `company.service()` unconditionally (every domain's `seed_state()` now runs on every incident, because state is seeded *before* the Orchestrator classifies and there is no domain to key on yet); `route`'s new fail-closed check that the classified domain acts on the target's kind (§7.3), which is what makes that merge safe by construction; `planner.py` naming config versions and an error rate directly (now one `{planner_context}` slot, with **item 11.5's floor and item 20's ceiling moved into `sre_infra.py` unchanged in wording**); and `recall.query_text()` calling every target a service, fixed via one new `company.described()`. `Diagnosis` also moved to `_reasoning.py`: `hand_off` reads its five fields for whichever agent ran, so it is a contract between the domain layer and the Planner, not a fact about infrastructure.
   - **The registry entry already existed, so the item's title over-promised by half.** `supply-chain-agent` has been seeded since item 5 with `memory_domains=("supply-chain",)` and `tool_scope=()`, and `seed_belief.py` has been committing `SUP-042`'s chain as it since item 17. It needs no tool scope: §2.1 has the Planner emit the Action. `scripts/seed_registry.py` is untouched.
   - **The incident ends `HELD` at 11, and that is the shape rather than a shortfall.** `DISABLE_COMPLIANCE_CHECKS` scores `4 + 2 + 2 + 3` against a tier-1 supplier. The verify line is "routes, diagnoses, and **proposes**", and a hold satisfies it exactly — **§4.2's second worked example reached by a fleet** rather than asserted by a table-driven test, for the first time. **Consequence: no belief is written**, so the item's "two domains writing beliefs" clause is not what happened; nothing executed, so nothing was verified, so §7.2 permits nothing to be learned. Two domains have nonetheless had beliefs since item 17 (`SUP-042` is `supply-chain`), which is what makes the half-life table meaningful even though no *incident* in that domain writes one.
   - **`half_life_domain` is a real per-domain lookup whose two values are both 30 days.** An unpublished domain raises rather than defaulting, and a test asserts every domain a registry record may write in has an entry. Because both values are equal, a test patches one entry to prove the lookup is real. Answers ADR-017's and ADR-018's revisit clauses.
   - **`trigger_signal` gained `compliance_lapse` under item 2's condition** — module, §8.1 and `tests/test_telemetry_schema.py` in one commit. A lapsed certification reported as an error rate is an untrue fact on a span. **No sixth span shape and no new attribute key**; the streak since item 16 holds.
   - **The verify script mutates nothing — the first since item 8 with no teardown — and guards the opposite.** What it asserts is that `SUP-042`'s belief chain is byte-identical before and after, because this is the one script that runs an incident against the entity items 27 and 28 attack. It also checks recall handed over the AT_RISK belief *before* waking the fleet, so "diagnoses against prior belief" is a checked premise rather than a four-minutes-late trace finding.
   - **Two defects found while building, both in the script rather than the product.** The first run reported trace id `000…0` — `telemetry.configure_tracing()` was never called, so the tracer was a no-op and the read-back polled a nonexistent trace for the full four minutes. The second needed `force_flush()` before the read-back, or the poll races `BatchSpanProcessor`'s queue.
   - **All incident regressions green**, which is what says the moved Planner block, the multi-node graph and the half-life table cost the first domain nothing: `verify_incident_one.py` still `RESOLVED` at `1+1+0+0 = 2` with one belief at 0.60 (trace `dc1b46bcd511372a645286213b8e8579`); `--remember` still v1 → v2 (trace `16416f5a48490624e32e8a4791ad8ab9`); `verify_refuted.py` both halves (traces `a1f53647d81b270aa059224a3605b3e3` and `b440149939fc13c4a9e63d8f3efc80ad`); and `verify_belief_inspector.py` green against a local service running the new code, `SUP-042` still 0.575 / 0.770 with a 30-day expiry.
   - **`scripts/verify_belief_store.py` could not be run and was not forced.** It refuses unless `sre-infra-agent` starts `GOOD` with an **empty** rejection window, and the live record carried one `NO_NEW_EVIDENCE` entry stamped `2026-08-24T15:24:39Z` — three hours before this item's first commit, so it predates this work. §3.4 gives standing no automatic restoration path and clearing the window is a human act, so it was left alone. Nothing in this item touches `COUNTED_REJECTIONS`, `commit()`'s refusal paths or the standing counter.
22. ~~Instrument the cost: report lines changed *outside* the domain agent file and registry entry. Target: **zero** control-plane changes (gateway, risk table, Policy Engine, Sweeper, orchestrator).~~ — verify: the line-count report is committed; if the number isn't small, the control plane isn't general and the spec is wrong — better to find out now.
   — **done** (Aug 24): **the number is not small, and that is the finding.** One new file, [`docs/generality-report.md`](./docs/generality-report.md) — the first non-ADR document in the repo — plus one sentence each in `ARCHITECTURE.md` §5.4, `README.md` and `ADR-024`'s cost paragraph. **Nothing under `provenance/`, `tests/` or `scripts/` was touched**, so items 9–21's live evidence stands unaltered and no verify script needed re-running. No ADR: the methodology decisions are in the report's own Method section, where a reader looking for them will be.
   - **The report is the deliverable — every number, the per-file table, the reconciliation, the method and the third-domain prediction live there and are not restated here.** The headline: **114 lines in the agent file, 417 touched and 207 behavioural outside it**, and **≈10 lines** is what a third domain has to beat.
   - **§18's promised sentence is false in two places, in opposite directions.** The registry entry cost **zero** lines, not "one registry entry" — `supply-chain-agent` has been seeded since item 5. And `provenance/policy.py`, the Memory Policy Engine, is **named in §18 as a must-be-zero and changed** when §4.3's `half_life_domain` became a per-domain lookup. The narrower claim survives and is the one the submission should make: no *decision path* in the Policy Engine moved — §2.2's pipeline, both thresholds, §6.3's rule, `COUNTED_REJECTIONS`, every refusal path and `retract()` are untouched.
   - **`ADR-024`'s "roughly forty changed lines" was out by a factor of five**, and that sentence now points at the report instead of carrying a number. The estimate was written from the *list of files*, which was itself short by one — it names eight where there are nine, omitting `_reasoning.py`. Repointing rather than restating means the two documents cannot drift.
   - **`agents/orchestrator.py` is genuinely zero, and `incident.py` is reported beside §18's five rather than inside them.** §18's "orchestrator" is the Orchestrator agent of §5.3, which did not change. But `incident.py` builds the graph, sets `ctx.route` and merges the seeders, and its 160 touched lines are the largest number in the report; folding them into an undifferentiated "adaptation cost" total would have been technically §18-compliant and dishonest. The **Sweeper row reads `n/a`, not `0`** — a component nobody has written cannot be evidence that it did not have to change.
   - **The comment/behavioural split parses each file with `ast` and `tokenize`, not a `^\+\s*#` grep**, which under-counted by more than half (86 against 223 across `provenance/`) because multi-line docstring bodies do not match it. **Prompt text inside a string literal is counted as behaviour, not comment** — it is what the model is told. The reconciliation rule is `telemetry.set_risk()`'s components-must-sum rule applied to a diff: the columns add to the raw total exactly, so the itemization cannot quietly lose lines.
   - **No `scripts/generality_report.py`.** The verify line asks for a committed report, not a runnable check; move detection is already git's, and the comment/behavioural rule is twenty lines of `ast.walk` that would need maintaining in order to be rerun once, when a third domain arrives. The two git commands that produce every raw number are quoted in the report instead.
   - **The spec was not edited.** It is treated as a frozen input everywhere else in this repo; §18's sentence is corrected in the report and in `ARCHITECTURE.md` §5.4, which is where as-built notes go. `THREAT_MODEL.md` was not touched either — a measurement is not a security property.
23. ~~Class-belief proposal path, and the Memory Analyst's first structural need (deferred here from item 14): ≥3 entity beliefs sharing a structural signature → Analyst proposes; the Analyst recommends, the Policy Engine decides; confidence capped at 0.75 and below the weakest constituent; hard `ADVISORY ONLY` — may reorder investigation, may never authorize an action or evidence an entity-belief commit.~~ — verify: an attempt to cite a class belief as commit evidence is rejected by the Policy Engine.
   — **done** (Aug 25): one new agent file (`provenance/agents/memory_analyst.py`), two new scripts (`scripts/seed_class_belief.py`, `scripts/verify_class_belief.py`), one field on `BeliefVersion`, three constants and two `CommitReason` values in `policy.py`, a fourth registry record and two fixture services. **`incident.py` was not touched at all.**
   - **Design recorded in [`ADR-025`](./docs/adr/ADR-025-class-beliefs-and-the-advisory-cap.md):** the cap becoming a **refusal** rather than a label (two of §6.2's three enforcement points already existed via `recall.Recalled`'s typing from item 16; the third had nothing, because `Evidence` has no belief field, so no proposal could cite a class belief and no code could decline one — and a guarantee nothing can violate is an absence of a feature); `min(§4.3, CLASS_CAP 0.75, weakest − CLASS_MARGIN 0.05)` with the weakest recomputed **live** rather than read off a stored confidence; the shared status being what makes "structural signature" mechanical; and the evidence being **derived** from the constituents inside §2.2 rather than proposed, with a caller passing `evidence` on a CLASS commit getting a `ValueError` rather than a signed `REJECT` — `commit()`'s callers are our own code, so that is a bug, and a bug should not look like a governance decision in the audit stream.
   - **The control test is the load-bearing one.** An *entity* belief cited the same way still commits, without which "refuses class beliefs" and "refuses anything belief-shaped" pass identically.
   - **`COUNTED_REJECTIONS` goes to five, and the new pair splits on items 14 and 15's test.** `CLASS_BELIEF_NOT_EVIDENCE` counts — a statement about the evidence the agent brought, the memory-side twin of the poisoning attempt `BELOW_THRESHOLD` catches. `INSUFFICIENT_CONSTITUENTS` does not — how many entity beliefs exist is a fact about the store's population, `NOTHING_TO_RETRACT`'s reason. Item 28's poisoning arithmetic is untouched. Three constituents at 0.60 each land the class belief at exactly **0.55**.
   - **Two defects found live, both in what the Analyst was shown.** Told only that three tier-2 services hold `CONFIG_REGRESSION_PRONE`, run 1 generalized over "a high number of runtime dependencies" — a property nothing gave it. The clause forbidding unshown terms produced run 2's "having 'API' in the description is correlated with…", a real shared attribute and a spurious class. Both are the same shortage: three rows differing only by name is not enough to generalize from. The fix is that the seeder now states **in code** what the status records — that it is committed only after a rollback executed and verification CONFIRMED the deviation was gone — which is `sre_infra.seed_state()`'s `planner_context` posture. Run 3 wrote *"For a tier2 service, a change to its configuration is correlated with a subsequent error-rate deviation."* Items 11.5 and 20 found and fixed their defects the same way: one clause, upstream, in the prompt.
   - **Two more tier-2 services, because there were not three entity beliefs to generalize.** `belief-inventory-api` is created and deleted by every `verify_incident_one.py` teardown; `pricing-api` must stay belief-free or item 24 proves nothing; `checkout-api` was the only one left. `orders-api` and `search-api` extend the rationale `checkout-api` already carried. Cost: two dataclass entries, two comprehended fault switches, `seed_firestore.py`'s count 26 → 30, and one test assertion.
   - **No sixth span shape and no new attribute key.** `telemetry.reasoning_chain()` takes `step` as a plain `str`, so `step="generalization"` needed no vocabulary edit; `belief.scope` has admitted `CLASS` since item 2 and this is its first non-ENTITY emitter. `_finish()` now reports the evidence the pipeline **measured** rather than what the caller passed, or a class belief's span would claim it cited nothing while `novel_count` said otherwise.
   - **The domain agents' prompts were deliberately not touched.** `Recalled.summary()` has rendered class beliefs with an `[ADVISORY ONLY]` marker into `{recall_summary}` since item 16 and `sre_infra.py` already interpolates it. Whether a recalled class belief actually *moves* a hypothesis is item 24's verify line, and item 18's lesson was not to claim causation no A/B has measured.
   - **The cold incident still cites `[]` in the ledger even though a class belief now matches it, which is §6.2's type-level cap proven on a real incident.** Before item 23 the store held no class belief, so the guarantee was true vacuously — `recall.nominate()` returned before spending anything. `belief-service.tier2` matches an `inventory-api` error-rate query as readily as a `pricing-api` one, so `--remember`'s first incident is the first run in which recall genuinely hands a class belief to a domain agent and the ledger still records nothing resting on it.
   - **Verified live.** `seed_class_belief.py` green — three constituents at 0.6000, `belief-service.tier2` at 0.5500 — and `verify_class_belief.py` green end to end: the cap re-derived from first principles, a real `text-embedding-005` nominating it for a `pricing-api` deviation on an entity with **zero** entity beliefs, the refusal writing no version and no evidence document and costing exactly one window entry, `INSUFFICIENT_CONSTITUENTS` costing none, and the chain byte-identical on exit.
   - **Every incident regression green with a class belief in the store:** `verify_incident_one.py` `RESOLVED` at `1+1+0+0 = 2` with one belief at 0.60 (trace `1cd4d1247f8e58a810b6c7280f949d1c`); `--remember` v1 → v2 with `classification` carrying the belief before `diagnosis` (trace `7fe6f7734efe6c8c3b63875f7733cfbf`); `verify_refuted.py` both halves (traces `4da5189bb39290309446bec3fac097d4` and `d9a21edb4e8792aaebbd4b1891314ea4`); `verify_supply_chain.py` `HELD` at `4+2+2+3 = 11` with `SUP-042` byte-identical (trace `51987600080ea11e234fe0395c57690c`). 393 tests pass, up from 384.
   - **`scripts/verify_belief_store.py` ran, for the first time since item 20**, and is green end to end with five reasons in `COUNTED_REJECTIONS` rather than four: the three `BELOW_THRESHOLD` refusals still drive the window 1 → 2 → 3 → DEGRADED and the fourth write is still refused `STANDING_NOT_GOOD`, so item 28's poisoning arc is untouched by the new member. It was unblocked by clearing one `NO_NEW_EVIDENCE` window entry stamped `2026-08-24T15:24:39Z` — a human act under §3.4, and by then **24.1 hours old**, so past `REJECTION_WINDOW_HOURS`. The script refuses on a non-empty window regardless of age, deliberately, because its own teardown writes `rejection_window: []`.
24. ~~**Incident #3 — the fleet generalizes**: a deviation on `pricing-api`, never handled, empty entity memory; the class belief fires via the recall index and prioritizes the config-deploy hypothesis.~~ — verify: the trace shows the class-belief nomination on an entity with zero entity beliefs. *(The single most important thirty seconds of the video.)*
   — **done** (Aug 25): one new script (`scripts/verify_incident_three.py`), one flag on `scripts/inject_fault.py` (`--target`), two cases in `tests/test_incident.py`. **Nothing under `provenance/` changed at all** — items 8, 18 and 22's posture, and for their reason: every mechanism item 24 names was already built and already tested, so item 24 is the live proof rather than new code.
   - **Design recorded in [`ADR-026`](./docs/adr/ADR-026-incident-three-and-the-cold-entity.md)**, which answers ADR-025's revisit clause: why `ESCALATED` is the correct outcome rather than a shortfall (`pricing-api` has `known_good_version=None` on purpose, so the gateway approves at `1+1+0+0 = 2` and `executor.execute()` then refuses — §7.3 fail-closed, already built; **do not "fix" this with a node that halts before the Planner**, which would put a fact about infrastructure remediation into `incident.py`, where §5.4's generality claim says domain knowledge must not go, to tidy a trace that is already accurate); why the causation claim is exactly one step stronger than item 18's and no more; and why `--runs N` measures the flake rate before the demo depends on it.
   - **The causation claim, stated precisely.** Item 18 declined to claim memory caused the hypothesis because `sre_infra.py` carries its own config-regression hint. Here that hint's precondition — "the deployed config version is ahead of the last known-good one" — is **false**: both are `unknown`. So `config_regression` on this fixture cannot have come from the hint. It is still not a measurement, and item 32's `--memory-disabled` A/B is still where the number comes from.
   - **Nothing verifying is what keeps `pricing-api` belief-free.** §7.2 permits learning only from an outcome verification could settle, and nothing executed. So the premise that makes this an entity with empty memory is maintained by the same mechanism that stops the incident, rather than by a teardown remembering to delete something. The script asserts it anyway — `restore()` returns a failure count and checks `beliefs/belief-pricing-api` *before* deleting anything, because a belief there would mean the loop committed on an unverified action.
   - **The per-run tally reads `telemetry.BUFFER`** — the *same span objects* Cloud Trace gets (item 11), not a copy — so ten runs do not pay ten four-minute polls; the durable read-back runs on the last one only, item 11.5's reason again.
   - **A local teardown, and that is not a reversal of item 19.** `verify_refuted.py` imports its teardown from `verify_incident_one.py` because two restore paths over **one** fixture drift. This is a different fixture: `pricing-api`'s switch, service document and ledger records are touched by nothing else. What is imported is the target-agnostic half — `attribute()`, `load_private_key()`, `read_back()` and `SWITCHES`, that last one precisely so a teardown cannot come to know about fewer §9 switches than exist. Parameterising incident #1's helpers was the alternative and would have put items 9, 10, 11, 11.5, 18 and 20 back on the regression list to prove a script-only change cost nothing.
   - **`--target` on `inject_fault.py`, defaulting to `inventory-api`.** The demo's third beat needs the spike on `pricing-api` on camera and the operator should reach it with the command they already use. Every existing invocation is unchanged in effect; the id is validated through `company.service()`, so a typo cannot write a `fault_injection` document for a service that does not exist.
   - **The cosmetic artifact, stated rather than fixed.** `sre_infra.seed_state()` renders both config versions as `"unknown"` for a service with no history, so a success predicate naming a version would read `is unknown`. It never executes. Fixing it is behavioural lines in `sre_infra.py`, one of item 22's five counted files, which would put every incident path back on the regression list. Measured over ten runs: **five of ten** predicates named the version as `unknown`, so the artifact is frequent rather than incidental — the kind of thing one run cannot tell you, and the reason `--runs` exists. It costs nothing because the executor refuses first, and every one of the ten named an error-rate threshold of 0.015 or 0.02 against the 0.01 nominal, so item 11.5's floor held on a cold entity too.
   - **No sixth span shape and no new attribute key.** §6.2 gained an "as built" note and §10 gained one row, **`Generalization`** — deliberately not folded into the neighbouring `Generality` row, which is item 22's line-count claim and a different thing.
   - **Verified live.** One run green end to end (trace `bb1389423de3d840ca044801e9bb761e`): `ESCALATED`, `ROLLBACK_CONFIG(pricing-api)` approved at `1+1+0+0 = 2`, `belief-service.tier2` nominated and handed over with `belief-pricing-api` nowhere, three reasoning chains and **zero** verification and belief spans, `belief-service.tier2` byte-identical. Then **`--runs 10`: 10/10 named `config_regression`** (trace `18f697445679c8400b97194d0c4f7aee`), every run identical, baseline restored after all eleven incidents. `--target` exercised in both directions, with an unknown id and a missing value both exiting 1. 395 tests pass, up from 393. Both regressions green: `verify_class_belief.py` with the chain byte-identical, and `verify_incident_one.py` still `RESOLVED` at `1+1+0+0 = 2` with one belief at 0.60 (trace `8754e36727770923b52db8666908d370`).

**Phase 8 — Security beats (filters leak, the boundary holds)**
25. ~~Model Armor templates wired on all ingest (injection/jailbreak + Sensitive Data Protection), verdicts logged to Cloud Logging.~~ — verify: a blunt payload is blocked and logged; a crafted business-text payload clears the threshold (both narrated honestly — see THREAT_MODEL).
   — **done** (Aug 25): one new module (`provenance/ingest.py`), two new scripts (`scripts/setup_model_armor.py`, `scripts/verify_model_armor.py`), `tests/test_ingest.py`, one runtime dependency, one API in `scripts/gcp_setup.sh`.
   - **Design recorded in [`ADR-027`](./docs/adr/ADR-027-model-armor-on-ingest.md):** one template at a threshold committed to **before** it was measured; verdicts logged by the service itself (`log_sanitize_operations`) rather than restated by us, so there are not two sources that can disagree about one event and `google-cloud-logging` stays out of the image; **no span** — the item says verdicts are *logged*, never traced, and §5.1 makes Model Armor a filter rather than one of the decisions the vocabulary carries, which also keeps item 26's `verify:` line cheap; failing closed (`ScreeningUnavailable` on an API error, on `invocation_result=FAILURE`, and never a `Verdict | None`); and deliberately not wiring it to a caller that does not exist yet.
   - **Deviation, and it is the shape of the whole item: "wired on all ingest" wires to nothing, because there is no untrusted free-text ingest in this repo.** `incident.Trigger` is a validated entity id, a `Literal` signal, a float and a timestamp; every string that reaches a prompt comes from the frozen entity model or from our own agents, and `POST /trigger`'s body is those same four fields. The untrusted-content path arrives with item 26's sanitizer and its consumer with item 27's arc. Adding a `raw_content` field now would be a field nothing reads for a whole item — the speculative shape `CLAUDE.md` §2 forbids. So item 25 builds the mechanism and proves it live, which is exactly what its own `verify:` line asks for.
   - **The measurement is the item's real output, and it came out the way §10 predicted.** Against the live template: the blunt payload is **blocked** on `pi_and_jailbreak` at `HIGH`, and §10's crafted supplier-alert payload **clears** (`NO_MATCH_FOUND`). So item 27's arc survives as scripted and `THREAT_MODEL.md`'s bypass row is true rather than hopeful. **HIGH was fixed before either payload was screened** — spec §10 published the threshold long before anyone measured it, so it is a prior commitment and not a number fitted to the answer. Had the crafted payload been caught, the recorded response was to re-script item 27 and rewrite the threat row, never to lower the confidence level; `scripts/verify_model_armor.py` says so in the failure message itself.
   - **The log read-back carries a per-run nonce**, so what it finds is *this* run's verdict rather than a previous run's entry inside the same freshness window — item 2's read-back precedent with item 16's control-record lesson applied. Mutation-checked live: pointing the read at a nonce nothing screened exits 1 with the right message, which is what says the check is not passing vacuously. **Mutation-checked three ways offline:** fail-open on an API error, SDP's match read at the top level like every other filter (it nests one deeper, so the PII half would silently report nothing), and `FAILURE` treated as a clean pass — all three red.
   - **Known limit, dated and not fixable from here.** Model Armor's own log entries carry a warning that filter version `v1` moves to `LEGACY` on **2026-09-01** — after the Aug 31 deadline but inside the Oct 1 judging window — and `google-cloud-modelarmor==0.7.1` exposes no filter-version knob. The recorded demo is unaffected; a judge re-running the verify script in September could in principle see a different verdict on the crafted payload.
   - **A pre-existing flake in the offline suite, found by item 25's merge and fixed here.** `test_a_refuted_verification_writes_the_negative_belief` asserted `REJECT/NO_NEW_EVIDENCE` on the retry's commit, which is only true when both attempts land inside the same wall-clock second. Item 20 stamps each attempt with its own read time and `beliefs.TIMESTAMP` has second resolution, so on a loaded CI runner the two straddle a second and the retry *correctly* commits v2 — the assertion was an artifact of machine speed rather than a property. It went red on the merge commit having been green on the branch, which is exactly what a race looks like. Fixed with a `FrozenClock` monkeypatched over `incident.datetime` for that one test — the same move the two explicit `now` values already make in `test_a_second_incident_supersedes_the_belief_the_first_one_wrote`. **No production code changed.** Reproduced deterministically before the fix by advancing the clock 0.6s per reading, and that same harness is green after it.
   - **Verified live.** `setup_model_armor.py` creates the template and reads back `ENABLED / HIGH`, `sdp basic ENABLED`, `log_sanitize_operations True`; a second run is a no-op and exits 0. `verify_model_armor.py` green end to end: blunt blocked on `pi_and_jailbreak`, crafted clears, and the logged entry found carrying `filterMatchState MATCH_FOUND`, `MODEL_ARMOR_SANITIZATION_VERDICT_BLOCK`, *"The prompt violated Prompt Injection and Jailbreak filters"* and `confidenceLevel HIGH`. `./scripts/gcp_setup.sh` re-run is idempotent and exits 0. **404 tests pass, up from 395.** The one regression worth running is the dependency's: `google-cloud-modelarmor` installs as a single wheel with **no movement in `google-adk`'s pins**, and `verify_incident_one.py` still reaches `RESOLVED` at `1+1+0+0 = 2` with one belief at 0.60 (trace `3ef7d24357af40032c2a46a18efab34e`), baseline restored.
   - **Cost.** Model Armor is free to 2M tokens/month standalone and bills per call, never per hour, so it adds nothing idle to the $300 ceiling. Single-digit screening calls and no model calls.
26. ~~Gemma 4 sanitizer deployed in isolation (Vertex AI Model Garden): untrusted content → typed facts, PII tokenized, output is data never authority. — verify: raw inbound text never appears in any frontier-model prompt in the trace.~~
   — **done** (Aug 25): `provenance/sanitizer.py` and `scripts/verify_sanitizer.py` are the two new files; `Trigger.raw_content`, one shared `sanitized_facts` state key and one instruction line in each of the two domain agents are the wiring. **439 tests pass, up from 404.** Design in [`ADR-028`](./docs/adr/ADR-028-the-gemma-sanitizer-as-built.md); [`ADR-006`](./docs/adr/ADR-006-gemma-sanitizer-isolation.md) gained an as-built pointer because the probe overruled its serving model.
   - **This item gave item 25's `screen()` its caller.** Item 25 built Model Armor and recorded that "wired on all ingest" wired to nothing; `run_incident()` now screens `raw_content` and raises `ingest.ContentBlocked` on a match, then sanitizes. Both happen **before the incident span opens**, so §7.3's "ingest halts" leaves no incident id, no span and no record — which is what *halts* has to mean. No new `IncidentOutcome`, no §8.1 amendment for it.
   - **Four probe findings, and three of them changed the build.** ADR-006's "Gemma 4 (E4B/12B, served via Vertex AI Model Garden)" was written at design time and never probed. (1) `gemma-4-26b-a4b-it-maas` serves on the **`global`** endpoint through the `google.genai` client `recall.py` already uses — **serverless, so nothing is deployed and nothing bills while idle**. (2) It is global-only: a `us-central1` call answers `FAILED_PRECONDITION`. (3) **`responseSchema` is ignored** — probed with a two-field schema it returned one invented field and neither declared one, so `sanitizer._parse()` is the entire type guarantee and no schema is sent. (4) PII tokenization works when prompted. `pyproject.toml` is **untouched**: `google-genai` already arrives via `google-adk`, so unlike item 25 there was no dependency regression to run.
   - **The Gemma cost trap is deleted rather than managed.** `CLAUDE.md` named a deployed endpoint at $1–4/hr *while idle* as the single largest threat to the $300 credit, with a deploy-only-while-recording discipline. MaaS bills per token, so there is no `deploy_gemma.sh`, no `undeploy_gemma.sh`, no setup step before a rehearsal and nothing that can be left running overnight. The live trap is removed from `CLAUDE.md`; a 429 note replaces it.
   - **Disclosed demo risk: `gemma-4-26b-a4b-it-maas` is `PUBLIC_PREVIEW` on shared capacity and answered `429 "The request queue is full."` on roughly one call in two.** Every probe cleared inside two attempts. `SANITIZE_ATTEMPTS = 4` with exponential backoff absorbs it, on 429 **only**; everything else — including a malformed extraction — raises on the first call, because retrying a bad shape is resampling until the model agrees. If a rehearsal ever dies on `queue full after 4 attempts`, that is capacity: re-run, do not raise the bound.
   - **Finding — the first live run failed, and it is the most useful thing this item produced.** Told to "list every token you used", Gemma tokenized the statement correctly (`[PERSON_1]`, `[EMAIL_1]`, `[PHONE_1]`) and then listed in `pii_tokens` the **values it had replaced** — a name, an email and a phone number, verbatim. The one field whose job was to prove the PII was gone was the field carrying it, and it travelled into the seeded prompt state and out onto ADK's own `call_llm` spans; `verify_sanitizer.py` caught all nine leaks. Fixed at **two** layers, because the prompt alone would have made the system prompt the boundary ADR-006 refuses to let it be: the prompt now names placeholders explicitly, and `_parse()` enforces `PLACEHOLDER` — a token that is not a placeholder *is* PII. The refusal names a count and never the values.
   - **Mutation-checked seven ways:** retrying on any error rather than 429 only, the parser tolerating extra keys, the raw payload seeded instead of the fact, the blocked verdict ignored, the extraction put on the span, the sanitizer called when there is no raw content, and `pii_tokens` accepted without the placeholder check. All seven turn a test red. 439 pass on restore.
   - **Verified live.** `scripts/verify_sanitizer.py` green end to end (trace `36e4b5a0a263e1d19fe72671d95d6b01`): the crafted payload clears Model Armor, Gemma reduces it to *"The sender reports that a supplier certification system has migrated and provides contact information for [PERSON_1] via [EMAIL_1] or [PHONE_1]"* with all three placeholders listed, the incident ends `HELD`, the seeded prompt state carries the fact and none of the payload, `SUP-042`'s chain is byte-identical, and **every attribute of all 23 spans is free of the raw payload** — the scan covers ADK's own spans, not only our five shapes. The trace shows **four** reasoning steps where item 21's shows three, which is the sanitizer being countable in the audit stream. All three regressions green: `verify_model_armor.py` unchanged (blunt blocked on `pi_and_jailbreak`, crafted clears, verdict read back from Cloud Logging), `verify_supply_chain.py` still `HELD` at `4 + 2 + 2 + 3 = 11` (trace `40b094c3ea882629d1aa26bd7e37b6c8`), and `verify_incident_one.py` still `RESOLVED` at `1 + 1 + 0 + 0 = 2` with one belief at 0.60 (trace `f225a47911f7ef9ae1ad6f0e00741905`), baseline restored.
   - **Cost.** No hourly resource and nothing new in the Cloud Run image. One Gemma call per incident carrying raw content, capped at four attempts, on ~150-token inputs; the whole item was single-digit cents. ADR-006's $1–4/hr idle risk does not materialize.
27. Injection arc end to end: the crafted payload leaks through both filters, the Supply-Chain Agent proposes `DISABLE_COMPLIANCE_CHECKS(SUP-042)`, the Planner types it honestly, the gateway scores **11** → HOLD. — verify: ARCHITECTURE §10 injection-arc row; the hold cites the risk arithmetic, not the payload.
28. Poisoning arc + standing: unverifiable "Supplier X is cleared" → weight 0.00 → rejected; three attempts inside the rolling window → **DEGRADED**, visible live on the registry panel; the agent's next ordinary low-risk proposal now requires human approval; SUP-042 still AT_RISK. — verify: ARCHITECTURE §10 poisoning + standing rows.

**Phase 9 — Staleness Sweeper (the second long-running async behaviour)**

29. Sweeper running continuously: on expiry, re-verify (CONFIRMED → refreshed version, decay reset; REFUTED → retraction path) or downgrade to `UNKNOWN(reason=stale)` — excluded from recall and confidence, never deleted. — verify: ARCHITECTURE §10 sweeper row, plus the downgrade visible in the belief inspector.

**Phase 10 — Human approval path (the Unlikely Hero surface)**

30. Approval queue with park/resume: a held incident parks via the ADK Task API, survives minutes of waiting, and resumes on approve/deny; nothing auto-approves on timeout. — verify: an incident parks ≥5 minutes and resumes cleanly; denial is signed into the ledger.
31. The plain-language approval card for the store operations manager: what the fleet wants to do, why, the component-by-component risk arithmetic, approve/deny — generated from the risk table, never from a model. — verify: a non-engineer can read the card for the score-11 action and articulate why it was held.

**Phase 11 — Counterfactual instrumentation (measured, not estimated)**

32. `--memory-disabled` A/B on incident #2: wall-clock, tool calls, tokens, hypotheses evaluated before the correct one. Full unedited side-by-side run recorded for the repo and blog post; the video shows only the result table. — verify: the A/B table is reproducible from the committed run artifacts.

**Phase 12 — Bonus artifacts (+1.0 max on a 5-point base — the margin in a tight field)**

33. Blog post (the determinism boundary, ARCHITECTURE §4, essentially already written) published on dev.to/Medium with the required disclosure line, embedding the full unedited A/B run. — verify: live URL.
34. Social post on X or LinkedIn with **#AllThingsAgenticHackathon**, linking video and repo. — verify: live URL.
35. Gemma 4 integration documented in the submission write-up as the additional Google AI model (+0.2 — one honest model beats three ornamental ones). — verify: the write-up section exists and matches what's deployed.

**Phase 13 — Cold-visitor test**

36. The hosted Cloud Run URL works for a first-time judge: approval queue included, credentials and step-by-step testing instructions in the README, verified valid through **October 1**. — verify: someone who has never seen the project completes the approval flow using only the README.

**Phase 14 — Rehearsal + submission**

37. Rehearse the full incident arc end to end, timed to **3:40** (rules evaluate only the first 4:00). Test several injection-payload variants so whichever Model Armor verdict occurs on camera is narrated honestly. — verify: a timed dry run lands at ≤3:40 with every beat intact, including the budgeted on-screen Cloud Run console + live service URL shot.
38. Record and publish the video (YouTube/Vimeo, public); finalize the repo (architecture diagram, reproducible spin-up instructions; if private, shared with testing@devpost.com and cloudhackathons@google.com); submit with the pre-existing-code disclosure table (README) verbatim in the submission text. — verify: the Devpost submission is complete before **Aug 31, 5:00 PM PDT**.

---

## Beyond the hackathon — documented, not built

Named here so they're deliberate deferrals, not gaps discovered by a judge:

- **Class-belief statistical rigor.** Generalizing from three observations is thin. The advisory-only cap and sub-constituent confidence ceiling bound the damage (a wrong class belief costs investigation order, never authorization); a production version needs a real minimum-support and validation policy. Trigger: any use beyond hypothesis ordering.
- **Verification against uncontrolled systems.** The synthetic environment means verification cannot fail the way production fails; the three-valued architecture is exactly what production needs, but predicates against real telemetry (and their flakiness) are unbuilt. Trigger: first non-synthetic deployment.
- **Standing restoration workflow.** Reinstatement is "explicit human action" — a real deployment needs an audited review flow for it, not a console flag-flip. Trigger: first real multi-team operation.
- **Multi-approver / escalation tiers.** One store-ops persona owns the queue; production wants routing, delegation, and out-of-office fallback. Trigger: more than one approver exists.
- **Memory domains beyond two.** The generality number (item 22) is the evidence the control plane scales; actually onboarding domains 3–N (with their own status vocabularies and half-lives) is future work. Trigger: demand.
