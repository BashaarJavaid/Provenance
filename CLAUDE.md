# CLAUDE.md

Project-specific context and instructions for Provenance, merged with a set of general behavioral guidelines (sections 1-4 below, adapted from [andrej-karpathy-skills/CLAUDE.md](https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md)) aimed at reducing common LLM coding mistakes: unstated assumptions, speculative complexity, unrelated edits, and vague success criteria.

**Tradeoff:** these guidelines bias toward caution over speed. For trivial tasks, use judgment.

---

## Project

Provenance — a fleet of agents that detects, diagnoses, fixes, and verifies enterprise incidents, and writes what it confirmed into a governed institutional memory. The claim: **an LLM never decides what the organization does, and never decides what the organization believes.** Full pitch in `README.md`. Hackathon deadline: **August 31, 2026, 5:00 PM PDT** (All Things Agentic Hackathon, Fortified Enterprise Fleet track).

## Where things live

- `README.md` — what this is, the architecture diagram, tech stack, pre-existing-code disclosure. Read this first.
- `ARCHITECTURE.md` — both decision pipelines, canonical typed objects, the determinism boundary, memory design, failure modes, observability, testing strategy, deployment. Load this when working on the gateway, the Memory Policy Engine, or any core loop logic.
- `THREAT_MODEL.md` — what's protected against, what isn't, and the assumptions the design rests on. Load this when working on anything security-relevant (gateway, risk table, memory writes, sanitizer, standing).
- `docs/adr/` — one file per architecture decision, including why several common alternatives weren't chosen. Load the specific ADR relevant to the component being touched, not all of them by default.
- `ROADMAP.md` — the fourteen-phase build order as a living checklist. Check this at the start of a session to see what's next; update it as items complete.
- `self-healing-enterprise-project-spec (1).md` — the original spec: demo script (§13), synthetic-company details (§12), submission logistics (§21). Load for demo/submission work.

Don't load `ARCHITECTURE.md`, `THREAT_MODEL.md`, or the ADRs in full for unrelated tasks (e.g. a pure UI tweak or demo-recording step) — pull in only the file relevant to the current task.

## Conventions

The four load-bearing properties from `ARCHITECTURE.md` §1.1 are hard rules, not guidelines — a change that violates one is wrong no matter how convenient:

- **No direct path from any reasoning agent to a state-mutating action.** The gateway is the only path. Don't add a second one "for testing" that could ship.
- **The memory write path mirrors the action path.** Probabilistic recommends, deterministic decides — for beliefs as for actions. The Memory Analyst never commits; the Policy Engine never reasons.
- **No LLM-generated number is an input to a deterministic decision.** Confidence comes from the published noisy-OR formula (`ARCHITECTURE.md` §4.3); risk comes from the lookup table (§4.2). If a change makes a model-asserted number decisive anywhere, it has crossed the determinism boundary — reject it.
- **The registry is read at request time, not at boot.** Don't cache standing.

Further conventions:

- Python 3.12, Google ADK 2.0, GCP (Cloud Run, Firestore, Vertex AI). Async throughout. No `localStorage`/browser-storage patterns in the backend.
- Agents emit **typed objects only**: the Planner emits the canonical typed Action (`ARCHITECTURE.md` §3.1), never free-form text; the Analyst emits typed evidence and recommendations. Don't invent new response shapes — reuse the four canonical objects in §3.
- Verification is three-valued (`CONFIRMED` / `REFUTED` / `INCONCLUSIVE`) and memory learns **only** from confirmed outcomes. Never write a belief on `INCONCLUSIVE` — no partial credit.
- Class beliefs are **advisory only**: they may reorder hypotheses; they may never authorize an action or serve as evidence for an entity-belief commit.
- Beliefs are append-only: supersession and retraction, never overwrite, never delete.
- Fail-closed is the default posture for any subsystem failure that would silently weaken a guarantee (`ARCHITECTURE.md` §7.3). If unsure whether something should fail open or closed, it's closed.
- No ML/LLM-based risk scoring — a deliberate constraint (see `docs/adr/ADR-003`), not a gap to fill in later.
- PortunusMCP is consumed strictly as a library dependency (identity, RBAC/ABAC, ECDSA signing). All track-facing authorization logic — risk table, registry standing, typed-action fields, hold/resume — is new code in this repo, and must stay visibly so in the commit history (contest disclosure requirement; see `README.md`).

## Commands

Built so far; this section grows as ROADMAP phases land (update to reality as commands are created):

- Local dev setup: `python3.12 -m venv .venv && .venv/bin/pip install -e ".[dev]" && .venv/bin/pip install --no-deps portunusmcp==0.1.0` — the second install is mandatory and cannot be folded into `pyproject.toml`; see ROADMAP item 0.5. `pip check` exits non-zero in this venv by design; CI must not gate on it.
- `.venv/bin/pytest` — tests
- `.venv/bin/ruff check .` — lint
- `.venv/bin/ruff format --check .` — formatting (fix with `ruff format .`)
- `.venv/bin/mypy provenance/` — strict type-check
- `./scripts/set_budget.sh` — the $300 billing budget with 50/90/100% alerts; idempotent (already run)
- `./scripts/gcp_setup.sh` — GCP project, APIs (incl. Cloud Trace and Cloud Run), the Cloud Build and Firestore (`roles/datastore.user`) IAM grants, Firestore, and a live Gemini access probe; idempotent. `PROJECT_ID` / `REGION` override the defaults (`provenance-hackathon`, `us-central1`)
- `./scripts/deploy.sh` — the one-command Cloud Run deploy; builds from the repo Dockerfile and curls `/health` on the deployed URL, exiting non-zero if it isn't 200. `PROJECT_ID` / `REGION` / `SERVICE` override the defaults. Live: https://provenance-808273007560.us-central1.run.app
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/emit_trace_samples.py` — emits one span of each shape and reads the trace back from Cloud Trace; the item-2 `verify:` check. Needs credentials, so it is not in CI; allow up to ~4 minutes for indexing
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/seed_firestore.py` — seeds the synthetic company into Firestore and reads all 26 documents back; the item-4 `verify:` check. Create-if-absent by default so a re-run never clobbers state a demo take changed; `--reset` restores the baseline. Needs credentials, so it is not in CI
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/seed_registry.py` — writes the three agent registry records, generating each agent's keypair once and printing the private half once. Create-if-absent and deliberately **no `--reset`**: a re-run must never rewrite a stored `DEGRADED` back to `GOOD`. `--rotate <agent-id>` is the one path to a new key. Needs credentials, so it is not in CI
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/verify_registry.py` — reads standing, flips it, re-reads through the same client in the same process, and restores; the item-5 `verify:` check. Exits non-zero if the flip isn't visible. Needs credentials, so it is not in CI
- `PROVENANCE_PLANNER_KEY="$(cat planner.pem)" GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/verify_gateway.py` — authorizes §4.2's two worked examples against the live registry, asserts 2/APPROVE and 11/HOLD, denies an unregistered id, verifies all three signatures, and reads the spans back from Cloud Trace asserting the components sum; the item-7 `verify:` check. Mutates nothing. The private key is an env var because `seed_registry.py` prints each once and stores it nowhere — `--rotate` mints a new one. Needs credentials, so it is not in CI
- `PROVENANCE_PLANNER_KEY="$(cat planner.pem)" GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/verify_denial_by_registry.py` — authorizes §4.2's score-2 rollback three times while flipping only the planner's stored `standing` (GOOD → APPROVE, SUSPENDED → DENY, DEGRADED → HOLD), then reads all three spans back from Cloud Trace asserting `stage="registry"` and the standing that caused each; the item-8 `verify:` check. **The one script that writes to live state** — the flips sit in a `try/finally` that restores the original standing on any exit path, including Ctrl-C. Refuses to run unless the record starts at `GOOD`. Needs credentials, so it is not in CI
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/inject_fault.py` — flips `fault_injection/inventory-api.error_rate_spike` and raises the service's `error_rate` to the spec's 0.38; `--clear` restores baseline. Reads both documents back. Needs credentials, so it is not in CI
- `PROVENANCE_PLANNER_KEY="$(cat ~/planner.pem)" GOOGLE_CLOUD_PROJECT=provenance-hackathon GOOGLE_GENAI_USE_VERTEXAI=1 GOOGLE_CLOUD_LOCATION=global .venv/bin/python scripts/verify_incident_one.py` — the item-9, **item-10, item-11 and item-11.5** `verify:` check, one script because each is incident #1 going further: injects the fault, wakes the fleet against live Firestore and live Gemini, asserts exactly one typed `ROLLBACK_CONFIG(inventory-api)` scoring `1+1+0+0 = 2` and auto-approved, that the rollback executed (v42 → v41, error rate dropped), that verification returned `CONFIRMED` against the pre-declared predicate, and that one belief committed at exactly `0.60` — then reads the incident/reasoning/decision/verification/belief spans back from Cloud Trace, asserting the verification span's `predicate_id` equals the one the incident span carried *before* execution. **Writes live state** — the injection and the rollback sit in a `try/finally` that restores `error_rate`, `healthy`, `current_config_version` and deletes the belief on any exit path; the script refuses to run if the fault switch is already on **or** if `beliefs/belief-inventory-api` already exists (since item 12 the restore deletes the whole chain: the version subcollection, the `evidence/{id}` documents those versions cite, and the root document). Set `PROVENANCE_SERVICE_URL` + `PROVENANCE_TRIGGER_TOKEN` to also run incident #1 a second time **through the deployed service** — 403 without the header, 200 with, then `GET /trace` unauthenticated asserting the span shapes that outcome implies, the risk components summing, and no content-bearing attribute key. **That half writes live state too** and takes the same posture: `refuse_if_dirty()`, `inject()`, `try/finally` `restore()`. Its one limit is that the incident runs in the *deployed* process, so a Ctrl-C mid-trigger restores locally while the remote rollback can still land after — it warns rather than pretending. **`--runs N` runs the incident N consecutive times** — item 11.5's line is 10 — tallying how many reached `RESOLVED` and printing every `success_predicate` with the lowest threshold any of them named. Each run injects and restores around itself, which keeps the runs independent: without the restore, run 2 would supersede run 1's belief rather than commit the v1 this script asserts (before item 12 it was refused outright as `SUPERSESSION_UNSUPPORTED`). The loop finishes all N even after a failure (one refuted run in ten and seven in ten are different findings) and the Cloud Trace read-back runs on the last one only, since items 9–11 already proved the span shapes and each read-back polls up to four minutes. `--runs 1` is the default and behaves exactly as before. Costs three `gemini-2.5-pro` calls plus one `gemini-3.5-flash` per run, ~60s each. Needs credentials, so it is not in CI
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/verify_belief_store.py` — the item-12, **item-13, item-14 and item-15** `verify:` check: commits v1–v3 of a **scratch** belief (`verify-belief-store`, in no incident) through the real `policy.commit()` against live Firestore, asserting v1's document is byte-identical after the write that supersedes it, that v2 carries `supersedes: 1` and cites both items, that backlinks are derived rather than stored, that re-citing evidence unchanged is refused `NO_NEW_EVIDENCE`, that a one-class flip at 0.60 is refused `BELOW_THRESHOLD` against the 0.70 door, and that re-appending v1 raises `VersionConflict`. Then **item 14's half**: v4 adds a second source class to reach 0.82; a flip on a class already in the set is refused `FLIP_UNSUPPORTED` *above* 0.70 with no evidence document written; the three refusals the script has by then provoked are asserted to have degraded `sre-infra-agent` (the window's three reasons, in order) before it reinstates the agent; v5 commits the flip on a genuinely new class at 0.91, storing `threshold` as 0.70 and citing all five items; then three unverifiable claims about a second scratch entity drive the counter 1 → 2 → 3 and DEGRADED, and a fourth ordinary write is refused `STANDING_NOT_GOOD`. Its evidence ids stay **hand-written** deliberately — that is the case proving novelty compares pairs and not ids. Then **item 15's half**, between the other two because the last one takes the agent's authority away: two authorization records are seeded, one citing this belief and one citing another; a `third_party_audit` retraction is refused `RETRACTION_UNSUPPORTED` *at 0.55, above the 0.50 door*, writing no v6 and no evidence document; a `verified_system_observation` retraction commits v6 as `RETRACTED` at 0.60 against threshold 0.50, citing all six items; exactly one of the two ledger records is flagged, at v5; and a second retraction is refused `NOTHING_TO_RETRACT` at no cost to standing. The control record is the point of that half — without it, "flag every action authorized on this belief" and "flag everything" pass the same check. **Writes live state, including the registry**, and restores everything in a `try/finally` on any exit path including Ctrl-C: the scratch belief and both ledger records are deleted and the agent goes back to `GOOD` with an empty window. Refuses to run if its scratch belief exists, if either ledger record exists, **or** if the agent does not start at `GOOD` with an empty window. Costs no model calls. Needs credentials, so it is not in CI
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon GOOGLE_GENAI_USE_VERTEXAI=1 .venv/bin/python scripts/verify_recall.py` — the item-16 `verify:` check: seeds four scratch beliefs (one ENTITY, and three CLASS whose statements are written to score high / middling / low against one incident query), then asserts that a **real** `text-embedding-005` ranks the `RETRACTED` one first, that recall records it as nominated and hands it to nobody, that the weaker current class belief *did* survive (without which the drop could be recall returning nothing), that the entity belief came back by exact key carrying no statement, and that the other-domain belief fell under `SIMILARITY_FLOOR` and was never nominated. **Writes live state** — all four beliefs and their evidence are deleted in a `try/finally` on any exit path including Ctrl-C — and refuses to run if any of them already exists. Costs two embedding calls and no reasoning-model calls. Needs credentials, so it is not in CI
- `GOOGLE_CLOUD_PROJECT=provenance-hackathon .venv/bin/python scripts/seed_belief.py` — writes the `SUP-042` AT_RISK belief: two versions through the real `policy.commit()` pipeline as `supply-chain-agent`, `FLAGGED` at **0.575** (`contractual_record` + `agent_inference`, T−8d) superseded by `AT_RISK` at **0.770** (adding `third_party_audit`, T), then reads the whole chain back and re-derives the arithmetic from the stored citations. **Writes permanent live state**, create-if-absent and deliberately **no `--reset`** — items 27/28 attack this belief and the demo's closing shot is that it survived, so it refuses outright if the belief already exists rather than rewriting a poisoned-then-defended chain. Needs credentials, so it is not in CI
- `PROVENANCE_SERVICE_URL=… .venv/bin/python scripts/verify_belief_inspector.py` — the item-17 `verify:` check: GETs `/belief/SUP-042` with no token and asserts the breakdown multiplies back to the number beside it, that every version's stored confidence still recomputes from its own citations at its own `committed_at`, that `superseded_by` was derived rather than stored, that the flip rests on a source class the chain did not carry, that `expires_at` is `committed_at + 30d`, and that an unknown entity is 404 and not 503. **Mutates nothing** and needs no cloud credentials of its own — it talks HTTP. Defaults to `http://127.0.0.1:8000`; `PROVENANCE_SERVICE_URL` points it at the deployed service. It re-implements §4.3 from first principles rather than importing `policy.contributions()`, so it is not asserting that a function equals itself
- `GET /belief/{entity}` on the deployed service — the belief inspector's read side, unauthenticated by design; serves the whole chain, the evidence every version cites, and §4.3 recomputed as of now. The **first route that reads Firestore**
- `GET /trace` on the deployed service (or a local `uvicorn provenance.app:app`) — the trace UI's read side, unauthenticated by design; serves the whole in-process span buffer as JSON, which is what `provenance/web/index.html` polls once a second
- CI (`.github/workflows/ci.yml`) runs lint, format, type-check, and tests on push/PR — no cloud credentials

Note that `./scripts/deploy.sh` now **requires** `PROVENANCE_PLANNER_KEY` and `PROVENANCE_TRIGGER_TOKEN` and refuses to run without them; it passes the whole environment as a `--env-vars-file` because a PEM is multi-line, and checks that the deployed `/trigger` answers 403 unauthenticated. Since item 11 it deploys at **`--max-instances=1`** (was 3): the trace UI's span buffer lives in the process that ran the incident, so the blocking trigger and the polls beside it have to reach the same instance. `--min-instances=0` is unchanged.

Still to come:

- Fault-injection for the other two switches — `rollback_fails` and `verification_ambiguous` (Phase 5, items 19–20). `scripts/inject_fault.py` covers only `error_rate_spike` today
- `--memory-disabled` counterfactual A/B runner (Phase 11)

## Cost ceiling

The GCP project runs on a **$300 free-trial credit and must not exceed it**, and the hosted demo has to stay alive through **October 1** judging. Trial credit also expires ~90 days after activation, so unspent credit is not banked. Treat the ceiling as a design constraint, not something to audit afterwards — billing data lags up to a day, so by the time a number looks wrong the money is already gone.

- **Before adding any paid resource** — a deployed model endpoint, a Cloud Run service with `min-instances > 0`, a scheduled job, anything with a GPU — state what it costs per hour and whether it bills **while idle**. Idle-billing resources are the only things that can realistically drain the credit.
- **The single largest risk is the Gemma 4 sanitizer** (`docs/adr/ADR-006`, Phase 8). A dedicated Vertex endpoint bills by the hour whether or not it serves a request — order $1–4/hr, which is the entire credit in under two weeks of being left up. Deploy it only while that beat is being built or recorded, and undeploy immediately after. Never leave it running overnight.
- **Cloud Run stays `min-instances=0`** with a low `max-instances`. Scale-to-zero is the default posture; an always-warm instance is a deliberate, justified exception, not a convenience.
- **Token spend is not the risk; loops are.** A Gemini call costs cents; an agent looping on one costs whatever it can reach. The bounded retry (Phase 5, item 20) and the escalation path are cost controls as much as correctness ones.
- **The backstop is a budget, not a habit.** `scripts/set_budget.sh` configures a $300 Cloud Billing budget with alerts at 50/90/100%. Alerts notify, they do not stop spend — the four rules above are the actual guardrail.

## Current phase

**Phases 1, 2 and 3 are complete and Phase 4 is open — items 1–17 are done (Aug 23); item 18 (incident #2 — the fleet remembers: the Orchestrator recalls the prior belief before diagnosis completes) is next.** The repo has `pyproject.toml` (package `provenance/`; runtime deps are `google-adk==2.7.1`, `cryptography`, `opentelemetry-exporter-gcp-trace`, `fastapi`, `uvicorn` and `google-cloud-firestore`), ruff / mypy-strict / pytest config, GitHub Actions CI that deliberately does not gate on `pip check`, and `tests/test_portunus_surface.py` guarding the consumed Portunus surface. ADK is now used, not just pinned: `provenance/agents/` and `provenance/incident.py` arrived in item 9, and item 10 appended two nodes and a fourth agent to the same graph. Item 11 rendered the whole thing: the deployed shell's first two §8.2 surfaces are live. On the cloud side, `provenance-hackathon` exists with `aiplatform` + `firestore` + `cloudtrace` + `run` + `cloudbuild` + `artifactregistry` enabled and a Native-mode Firestore database in `us-central1`. **Gemini 3.5 Pro is not served to this project** (probed, 404 on every variant and endpoint), so the four reasoning roles run on `gemini-2.5-pro` and verification on `gemini-3.5-flash`; note that Gemini 3.x lives on the **`global`** endpoint, not a regional one.

**The trace schema (item 2) is now a contract to build against, not a convention.** `provenance/telemetry.py` defines four span shapes — `provenance.authorization.decision`, `provenance.belief.commit`, `provenance.verification.outcome`, `provenance.reasoning.chain` — documented in `ARCHITECTURE.md` §8.1. When a component from any later phase needs to emit, **use these helpers; do not call the OTel tracer directly and do not invent a fifth shape** without changing §8.1 and the tests together. Three rules the module enforces and `tests/test_telemetry_schema.py` guards: span attributes carry **identifiers, hashes, enums and numbers only — never content** (no payload text, prompt, model output or rationale prose); an emitted risk score must equal the sum of its four components; and a span that exits without recording an outcome is marked `ERROR`. Cloud **Logging** export is deliberately not wired — only Trace.

**The service is live (item 3):** https://provenance-808273007560.us-central1.run.app — one Cloud Run service, `provenance/app.py` (plain FastAPI) serving `GET /health` and `GET /` (the shell at `provenance/web/index.html`, one static file, no build step). `docs/adr/ADR-008` records why one service rather than §11's eventual four, why our own Dockerfile rather than `adk deploy cloud_run`, and why no UI framework. Three things to know before touching it: **use `/health`, never `/healthz`** — Cloud Run's frontend swallows that path and answers with its own 404 before the request reaches the container; the **Dockerfile's two-step install must stay two steps in that order** (`pip install .` then `pip install --no-deps portunusmcp==0.1.0`), same constraint as CI; and the service runs at `min-instances=0`, which is the posture the cost ceiling requires — do not raise it for convenience. Later items fill the shell's six labelled regions rather than restructuring it.

**The synthetic company is seeded (item 4).** `provenance/synthetic/company.py` holds the cast as frozen dataclasses; `scripts/seed_firestore.py` writes it to Firestore and reads every document back. Cymbal Home & Garden: `inventory-api` / `pricing-api` / `checkout-api` (all tier2), suppliers `SUP-042` (**tier1**) / `SUP-017` / `SUP-093`, approver Dana Ruiz, a `fault_injection/{target_id}` switch per service, and a small retail base. Four things to know before touching it: **`inventory-api` is tier2 and `SUP-042` is tier1** — that is the only route §4.2's worked examples have to the frozen scores 2 and 11, and `tests/test_synthetic_company.py` asserts both by name; **no entity document carries a status** — `SUP-042` becomes AT_RISK through the belief store in item 17, never as a seeded field, and a test rejects any `status`/`confidence`/`belief` field on the entity dataclasses; the seed is **create-if-absent** so a re-run cannot clobber mid-rehearsal state, and its read-back therefore content-checks only what that run wrote (`--reset` rewrites and checks everything); and `google-cloud-firestore` moved from **dev** to a runtime dependency in item 5, when `provenance/registry.py` became the first importer inside the package. Schema reasoning in `docs/adr/ADR-009`.

**The agent registry is live (item 5) — Phase 2's authorization spine begins here.** `provenance/registry.py` holds §3.4's record, the async read API, and `set_standing()`, the single writer; `scripts/seed_registry.py` writes the three agents (`sre-infra-agent`/`infrastructure`, `supply-chain-agent`/`supply-chain`, `remediation-planner` with §4.2's two action classes and no memory domain) and `scripts/verify_registry.py` performs the live flip. Five things to know before touching it: **`get_agent()` reads Firestore on every call and must stay that way** — §1.1's fourth property, and both the offline test and the live script fail the moment anything memoizes; **no function returns `Agent | None`** — `RegistryUnavailable` / `AgentNotRegistered` / `RegistryError` are what item 7 catches and maps to `DENY(stage="registry")`, and a test rejects any optional-`Agent` return type; the **stored `standing` is authoritative and never recomputed from `rejection_window`**, because `SUSPENDED` isn't derivable and reinstatement needs a field a human can set; the rolling window is now a number — **3 rejections inside 24 hours** (`REJECTION_THRESHOLD` / `REJECTION_WINDOW_HOURS`), with `degraded_by_window()` shipped for item 14 but deliberately not called on read; and the seeder has **no `--reset`** and skips existing records whole, so re-seeding can neither forgive a DEGRADED agent nor rotate a key item 7 has minted. Schema reasoning in `docs/adr/ADR-010`.

**The typed Action and its validation are live (item 6) — §2.1 stage 1, the gateway's front door.** `provenance/tools.py` is the tool registry; `provenance/action.py` holds §3.1's eight-field Action, `validate()`, the `ActionError` hierarchy and `outcome_for()`; `tests/test_action.py` is the entire `verify:` line — item 6 is the **first item with no live script**, because it touches no cloud service at all. Seven things to know before touching it: the **tool registry is a frozen constant, not Firestore, and that is deliberate** — §1.1's request-time rule exists because standing changes mid-run, and nothing analogous is true of a tool, so `TOOLS` holds two entries with four fields each (`action_class`, `target_kind`, authoritative `reversible`, authoritative `blast_radius`) and **no `base[action_class]`**, which stays in item 7's table with the other three components; **`validate()` takes `object`, not `dict`** — §10 names free-form text as a rejection case and it is only testable if a bare string can be handed in, so a `str`, `None`, a list and a wrong-keyed dict all raise; **nothing returns an optional `Action` or `Tool`**, guarded by the same reflection test item 5 used, because a forgotten `if action:` at stage 1 means the gateway scores something never validated and no later stage catches it; **the tool registry is authoritative over the Planner** for `reversible` and `blast_radius`, and the entity model over `target_tier`, so a Planner misdeclaring any of the three raises `FieldMismatch` (§3.1's "not vibes"); **there is no `params` field and §3.1 stays at eight** — item 10's executor reads `known_good_version` off the entity model rather than trusting a Planner-supplied version, and a test asserts its absence; `outcome_for()` is **stateless** (`MALFORMED_RETRY_BUDGET = 1`) because §7.1 gives the count to the control loop, not the agent; and `validate()` is **sync**, the one deliberate exception to async-throughout, since it reads two frozen tuples and does no I/O. Also added: `company.service(id)` / `company.supplier(id)` (raising `KeyError`) and `telemetry.TargetKind` — **no span shape changed**. Schema reasoning in `docs/adr/ADR-011`.


**The Agent Gateway is live (item 7) — §2.1's pipeline, and the only path to a state-mutating action.** Three modules: `provenance/risk.py` (§4.2's table), `provenance/credentials.py` (stage 2's signed assertion), `provenance/gateway.py` (the pipeline, whose `authorize()` is its single entry point). `scripts/verify_gateway.py` is the live half. Schema reasoning in `docs/adr/ADR-012`. Ten things to know before touching it:

- **`authorize()` takes `object`, not an `Action`, and that is load-bearing.** It runs `action.validate()` itself as stage 1, which is what makes `DENY(stage="schema")` reachable and what guarantees nothing reaches the risk table unvalidated — §1.1 property 1 made structural rather than documentary. **Every terminal outcome is a returned `Decision`, never a raised exception**; a test asserts `authorize` is the module's only public coroutine, because a second door is the whole security story.
- **The agent signs its own credential.** §2.1 said "minted by the registry **and** verified against the agent's registered `public_key`", which cannot both be true — ADR-010 stores no private halves. Resolved in favour of: the registry *issues and registers* the keypair (`--rotate` is the one path to a new one), the agent holds the private half and signs. `CREDENTIAL_TTL_SECONDS = 300`.
- **The registry read physically precedes the identity check**, because the public key stage 2 verifies against is a field on the record stage 3 fetches. The recorded `stage=` is still §2.1's. Do not "fix" the ordering.
- **`proposed_by` is checked against the credential**, and a credential for a superseded `agent_version` is denied. Without the first, an authenticated agent could present its own valid credential beside an action attributed to somebody else; without the second, `--rotate` revokes nothing.
- **RBAC and ABAC are two checks and must stay two.** Tool scope is a plain `in` (that is what role-based means); the standing rule goes through Portunus `abac.compile_condition`/`evaluate`, whose grammar has no `in` operator. A test asserts the compiled `abac.Condition` object, so removing the dependency fails the build rather than silently falsifying ADR-004's disclosure.
- **A DEGRADED hold is scored; a SUSPENDED denial is not.** DEGRADED returns `HOLD, stage="registry", reason="STANDING_DEGRADED"` carrying the full §4.2 arithmetic, because §3.4's "regardless of risk score" only means something if the score exists and item 31's card renders it. A denial carries no score.
- **The `authorization.decision` span's fields below `agent.{id,version}` are now optional** — the one amendment to item 2's contract, made with §8.1 and `tests/test_telemetry_schema.py` in the same commit. Pre-standing denials had no way into the audit stream otherwise. **Absent means omitted, never emitted empty**, and out-of-vocabulary values still raise. Still four shapes; no new attribute key.
- **`risk.BASE` is the only home of `base[action_class]`** (`ROLLBACK_CONFIG` 1, `DISABLE_COMPLIANCE_CHECKS` 4 — fixed by §4.2's worked examples). A test asserts its key set equals `tools.TOOLS`'s action classes, so a third tool cannot ship without a base score. **`risk.band()` never returns `DENY`**: every denial comes from who is asking, never from the score.
- **`Decision` is ours, not Portunus's**, typed with `telemetry.AuthOutcome` / `AuthStage` so the object and the span cannot drift — this closes item 0.5's open question. Its `subject` field (`agent@version|action_class|target`) is signed, so a signature cannot be lifted from one action onto another.
- **The gateway's signing key is ephemeral per process** — a marked `ponytail:` shortcut with Secret Manager as the named upgrade path. Decisions verify against `gateway.public_key_pem()` from the same run, not across a Cloud Run restart; `THREAT_MODEL.md` states this rather than letting "signed" imply otherwise.

Two pieces of infrastructure arrived with it: `[[tool.mypy.overrides]] module = "services.*"` in `pyproject.toml` (portunusmcp ships no `py.typed`; scoped to that one distribution, everything we write stays `--strict`), and `tests/conftest.py` holding the one global `TracerProvider` — OpenTelemetry allows it to be set once, and with two span-emitting test modules the second `set_tracer_provider()` was being ignored, silently starving that module's exporter.

**Registry state note:** `remediation-planner` is at **`v3`** — items 7 and 8 each ran `seed_registry.py --rotate` once, because each printed private half is shown exactly once and stored nowhere (ADR-010). Standing and `rejection_window` survive a rotation; only the key and version move. **Read `agent.version` off the record rather than hardcoding it** — `scripts/verify_gateway.py` and `scripts/verify_denial_by_registry.py` both do, which is why a rotation costs them nothing. A proposal's `proposed_by` must match the stored version or the gateway denies at `stage="identity"` — that is the version-binding check working.


**Denial-by-registry is proven live (item 8) — Phase 2 closes here.** One new file, `scripts/verify_denial_by_registry.py`, and **no change under `provenance/` at all**: item 7 had already built every mechanism item 8 names, so item 8 is the live proof, not new code. It authorizes §4.2's score-2 `ROLLBACK_CONFIG(inventory-api)` three times against the live registry while the only thing that changes between runs is the stored `standing` — `GOOD` → APPROVE at 2, `SUSPENDED` → DENY unscored, `DEGRADED` → HOLD *carrying* the 2 — then reads all three spans back from Cloud Trace. Four things to know before touching it: **"the signed ledger" is the span stream, not a UI** — §8.2's ledger surface is item 11's and item 8 renders nothing, so the `verify:` line is checked by asserting `decision.stage`, `decision.reason`, `agent.standing` and a non-empty `decision.signature` on what actually landed in Cloud Trace; **the GOOD control run is load-bearing**, because without it "denied" and "held" could be caused by something about the action rather than by the registry entry; **the flips sit in a real `try/finally`**, a deliberate departure from `scripts/verify_registry.py`'s unconditional restore line, because `seed_registry.py` has no `--reset` and a crash between the flip and the restore would strand the fleet's only tool-scoped agent as `SUSPENDED` with no path back but a hand-written `set_standing` — exercised with a simulated Ctrl-C, not assumed; and the script **refuses to run against a non-GOOD record**, since the control would prove nothing and the restore would cement the stranded value. No ADR and no new offline tests: item 8 rejects no alternative ADR-012 has not recorded, and item 7's three DEGRADED/SUSPENDED tests already are the offline half.

**The fleet runs (item 9) — Phase 3 opens, and the first agent code in the repo.** `provenance/models.py` holds the per-role model strings; `provenance/incident.py` holds the `Trigger`, the ADK graph and the control loop; `provenance/agents/` holds the three `LlmAgent`s (`orchestrator.py`, `sre_infra.py`, `planner.py`) and `_reasoning.py`; `provenance/app.py` gained `POST /trigger`; `provenance/telemetry.py` gained a fifth span shape. Live half `scripts/verify_incident_one.py`, offline half `tests/test_incident.py`. Schema reasoning in `docs/adr/ADR-013`. Twelve things to know before touching it:

- **The trigger stream is `POST /trigger`, not a listener, and it is guarded.** §5.3 says only "wake-on-event" and names no mechanism; a Firestore listener needs an always-warm instance the cost ceiling forbids. The route requires a header matching `PROVENANCE_TRIGGER_TOKEN` and **fails closed when the variable is unset** — the service is public by design, and an open endpoint that spends model tokens is a loop a stranger gets to run. Do not "simplify" the guard away for local convenience.
- **The graph is ADK's real Graph Runtime and the re-plan is a real edge.** `google.adk.workflow.Workflow`, with `LlmAgent` as a `BaseNode` and `ctx.route` selecting conditional edges. §7.1's one malformed re-plan is `validate → planner`; the escalation is a terminal branch. The count lives on the loop's scratch object, never on an agent — `action.outcome_for()` finally has its caller.
- **Agents are built per incident, never at import.** `_reasoning.py` keeps per-invocation span state on the agent object, so module-level singletons would share it across concurrent requests. This is also what lets tests inject a fake `BaseLlm`.
- **ADK invokes callbacks by keyword** (`callback_context=`, `llm_response=`) even though the type alias is declared positionally. The parameter names in `_reasoning.py` are load-bearing; renaming them to `ctx` fails at runtime, not at type-check time.
- **Session state carries only strings and numbers.** Agent instructions interpolate `{state_key}`, so every key an instruction names must be seeded. The `Decision` and the `Action` deliberately do **not** go through state — they live on `_Scratch`, because round-tripping them through JSON would mean the object a caller inspects is not the object the gateway signed.
- **`DOMAINS` maps a domain to `(agent id, scope sentence)` and both halves matter.** Naming a domain is not a vocabulary a model can aim at: with the bare word `infrastructure`, the Orchestrator classified a service error-rate spike as something else and the incident ended `UNROUTABLE` (observed live). Item 21 adds one entry here and one agent file, and nothing else — which is exactly what item 22 measures.
- **The Orchestrator holds no registry record.** §3.4 records authority and it has none; its span carries `orchestrator@v1`. Do not seed a fourth agent to make the trace tidy.
- **The fifth span shape landed under item 2's condition** — module, `ARCHITECTURE.md` §8.1 and `tests/test_telemetry_schema.py` in one commit. `provenance.incident` opens with the three wake-time facts and takes the rest through the recorder; `HELD` is **not** an error status, `DENIED`/`ESCALATED`/`UNROUTABLE` are.
- **`action.predicate_id()` is `sha256(success_predicate)[:16]`, and it is on the incident span before anything executes.** That is what makes "pre-declared" checkable: item 10's verification span carries the same id. Do not switch it to a per-incident id — two runs of the same predicate must hash alike.
- **The Planner's private key is `PROVENANCE_PLANNER_KEY`, marked `ponytail:` with Secret Manager as the upgrade path.** `deploy.sh` requires it and passes the environment as a `--env-vars-file` (mode 600, removed on every exit path) because a PEM is multi-line. `THREAT_MODEL.md` states what this costs rather than letting it be implied.
- **Item 9 executes nothing.** It ends at the signed decision. Item 10 *appends* execute and verify nodes to this graph; it should not need to reshape the existing ones.
- **Three sequential `gemini-2.5-pro` calls take ~60s.** Measured, not estimated. If §13's pacing needs it faster, the fix is one line in `provenance/models.py` — that is what the file is for.

**Incident #1 finishes and the fleet learns (item 10) — the executor, the Verification Agent, and the first belief.** `provenance/executor.py`, `provenance/policy.py` (a deliberate *stub* Policy Engine), `provenance/agents/verification.py`; two nodes and three edges appended to `provenance/incident.py`; `RESOLVED` on `telemetry.IncidentOutcome`. Live half: `scripts/verify_incident_one.py`, **extended not duplicated**. Offline halves: `tests/test_executor.py`, `tests/test_policy.py`, plus new cases in `tests/test_incident.py`. Schema reasoning in `docs/adr/ADR-014`. Ten things to know before touching it:

- **`executor.execute()` re-checks the decision; it does not trust the branch that called it.** It verifies the signature against `gateway.public_key_pem()`, requires `decision.outcome in executor.APPROVING`, and requires `decision.subject` to end in `|{action_class}|{target}`. That last check is the **first consumer of item 7's subject binding** — without it a signed APPROVE for a cheap rollback could be presented beside an expensive action. Do not "simplify" it away on the grounds that the graph only routes approvals here: the point is that §1.1 property 1 stops being a property of the edge list.
- **`known_good_version` comes off `company.service(target)`, never off the Action.** This is the moment ADR-011 removed `params` for. `tests/test_executor.py` hands the Planner a predicate naming a different version and asserts v41 still lands.
- **The `rollback_fails` switch is read inside `execute()`, every time.** ADR-009 reason 3. A failed rollback still *deploys* the version and leaves the rate spiked — a rollback that skipped its own write would make item 19's REFUTED a fact about the executor rather than about the remediation.
- **`provenance/policy.py` is a stub and refuses rather than approximating.** It runs the §2.2 stages that were already free — request-time registry read (standing `GOOD` **and** the domain in `memory_domains`), `confidence()` as §4.3's noisy-OR over **distinct source classes**, the `0.50` threshold, sign, `create` — and answers `REJECT("SUPERSESSION_UNSUPPORTED")` when v1 exists. **It could not write a v2** — item 12 gave it the versioned store, and that refusal is gone. Absent by design: novelty, the §6.3 conflict rule, `RETRACT`, an `evidence/{id}` collection, the standing-counter write.
- **`resolve` emits `verification.outcome`, not the Verification Agent.** `belief_written` is unknown until the commit has been attempted, so `belief.commit` **nests inside** the verification span and a test asserts the parent span id. Do not move the emit into the agent.
- **A node parameter's name is the session-state key ADK resolves it from.** `resolve(ctx, verification)` must be called `verification` because that is `verification.OUTPUT_KEY` — same rule as `validate(ctx, proposal)`. It deliberately shadows the module of the same name, which is why `verification_agent` is bound earlier in `build_graph`.
- **ADK re-raises a failed node out of the root workflow**, so `run_incident` wraps the runner loop: a Verification Agent that errors would otherwise leave an *executed* action with no verification span, reading as an incident nobody checked. §7.3 says that case is `INCONCLUSIVE`, and the **control loop** emits it — the same rule as §7.1's retry count.
- **`RESOLVED` is a sixth `IncidentOutcome`, added under item 2's condition** (module, `ARCHITECTURE.md` §8.1 and `tests/test_telemetry_schema.py` in one commit). After item 10, an incident ending at `AUTHORIZED` means something executed and was never verified. **No sixth span shape and no new attribute key** — `verification.outcome` and `belief.commit` were defined in item 2 and this is their first emitter.
- **The executor emits no span.** Execution is not a decision; every decision about it was made and signed upstream. What came of it reaches the trace through the verification span that judges it.
- **`policy.py` has its own per-process signing key**, the same `ponytail:` debt as the gateway's with the same Secret Manager upgrade path. A *stored* belief's signature is therefore checkable only while the writing process lives — `THREAT_MODEL.md` states that rather than letting "signed" imply more.

**Model access, as verified in item 9:** `gemini-2.5-pro` serves from **both** `us-central1` and `global` for this project; `models.LOCATION` defaults to `global` so one endpoint covers every role including item 10's `gemini-3.5-flash`. ADK needs `GOOGLE_GENAI_USE_VERTEXAI=1`, `GOOGLE_CLOUD_PROJECT` and `GOOGLE_CLOUD_LOCATION`. The Cloud Run runtime service account already reaches Vertex — no IAM change was needed.

**The trace UI is live (item 11) — Phase 3 closes here, and the fleet is finally visible.** Three files under `provenance/`: a `_SpanBuffer` and `GET /trace`'s serializer in `telemetry.py`, the route in `app.py`, and the two filled surfaces in `web/index.html`. Offline halves in `tests/test_app.py` and `tests/test_telemetry_schema.py`; live half is `scripts/verify_incident_one.py`, extended a second time rather than duplicated. Schema reasoning in `docs/adr/ADR-015`, which also answers ADR-008's revisit clause. Ten things to know before touching it:

- **The UI reads the same span objects Cloud Trace gets, not a copy.** `telemetry.BUFFER` is a second `SpanProcessor` on the same provider, holding a bounded deque of live spans, so §8's "the UI reads that same stream" is literal. Cloud Trace read-back is the rejected alternative and the one thing that cannot work: item 2 measured 15s–2min indexing lag on top of `BatchSpanProcessor`'s batching, and the item says *as it runs*. Cloud Trace stays the durable record; this is the live one.
- **Capture is on `on_start`, not `on_end`, and that is load-bearing.** A `reasoning.chain` span stays open for the whole model call (~20s per Pro call), so on-end capture leaves the panel blank and then paints a finished row — §8.2 says "current state". An SDK span's attributes are readable while it is recording and `end_time` is `None` until it closes, so `snapshot()` reads current state at poll time and one entry serves both. Moving capture to `on_end` turns three tests red.
- **The buffer filters by span name, and has to.** ADK auto-instruments ~20 spans per incident onto the same provider; `on_start` keeps only the `provenance.` prefix (§8.1's stated purpose for it). They still reach Cloud Trace through the other processor. Without the filter the polled payload quadruples and `BUFFER_SIZE` means a quarter of what it says.
- **`configure_tracing()` is no longer a no-op without a project.** It always builds the provider and attaches `BUFFER`; only the Cloud Trace exporter is conditional. **The return value narrows to mean exactly "Cloud Trace export is wired"** — what `/health`'s `tracing` field always reported. Without this the UI is blank locally and in CI and the item has no offline half.
- **`tests/conftest.py` attaches `BUFFER` too**, for the reason that file exists: `TestClient` is used as a context manager so the lifespan runs, and OTel ignores the second `set_tracer_provider()`. Without that line `/trace` is empty under test and every endpoint case passes vacuously.
- **`GET /trace` is unauthenticated on purpose and must stay that way.** The verify line says a *cold* browser; item 36 says a judge with a README. A read spends nothing, which is the entire reason `/trigger` is guarded and this is not. §8.1's redaction rule is what bounds it, now checked three times — at emit, on the serialized payload, and on the deployed bytes. `THREAT_MODEL.md` states what is published rather than letting "read-only" imply harmlessness.
- **`POST /trigger` was not touched.** It still blocks for the whole ~60s incident and still returns item 9's body; the UI just does not await the fetch before polling. Backgrounding it would change a documented shape and break the live script. Likewise `/trace` serves the **whole buffer** each poll, not an `?after=` delta — a span mutates after it is first served, so a cursor needs all of the snapshot's de-duplication plus a counter.
- **`scripts/deploy.sh` is at `--max-instances=1`** because the buffer is per process. `--min-instances=0` is unchanged, so this lowers the cost ceiling. Do not raise it for concurrency the demo does not have — it is also why a Firestore span-mirror was rejected: one flag solves that problem for free.
- **The Executor gets no row in the fleet view, deliberately.** It emits no span (ADR-014); what came of it reaches the trace through the `verification.outcome` span that judges it. A surface that inferred an executor row would assert state the audit stream cannot prove. The other four regions keep their placeholders, and `tests/test_app.py` still pins all six names as literal strings — **do not rename an `<h2>`**.
- **No sixth span shape and no new attribute key.** Everything both surfaces render was defined in item 2 or item 9; `ARCHITECTURE.md` §8.1 is unchanged and only §8.2 gained an "as built" note. If a later surface needs a field that is not on a span, that is a §8.1 change with the module and the tests in the same commit — not a second data source for the UI.

**The predicate floor (item 11.5) — the Planner is told what healthy looks like.** A defect found live, not designed: one run in three declared *"less than 1%"* against a service whose nominal error rate is exactly `0.01`, and `REFUTED` was the honest answer even though the rollback had worked. Two lines of behaviour change — `nominal_error_rate` (+ a percent form) in `_seed_state()`, and a clause on the Planner's `success_predicate` bullet. Six things to know before touching it:

- **The nominal comes off the frozen fixture, never off the trigger.** `executor.execute()` writes `company.service(target).error_rate` back verbatim on a successful rollback, so the number the Planner is told is byte-identical to the number the rollback produces. Seeding `trigger.observed_value` instead would hand it the *spiked* rate — the offline test's store holds 0.38 precisely so that regression is red.
- **Both units, because the defect was a units collision.** The prompt says `0.01 (1%)`. The model translated `0.01` into "1%" on its own and could not see it had landed on the baseline; showing both removes the translation step.
- **Verification was not touched, and must not be.** §7.2's three-valued rule working is the whole reason the fleet is trustworthy, and loosening the honesty gate to accept a boundary miss trades the property Phase 5 exists to demonstrate for a greener demo. **§3.1 stays at eight fields and `success_predicate` stays free text** — a typed threshold is exactly the `params` ADR-011 removed.
- **A predicate can fail two ways, and the ten-run verify found the second.** *Unsatisfiable* is the floor. *Uncheckable* is a reference to a fact the judge is not given: one run wrote "matches the last known-good version" and the Verification Agent — shown the deployed version but never told what known-good means — answered `INCONCLUSIVE`. Hence the second clause: name values **literally** (`"is v41"`). Both defects are the Planner's and both fixes are one sentence.
- **"Do not carry a version number in any field" now reads "Outside the success predicate…".** The old sentence contradicted the literal-values clause. ADR-011's intent is intact: the executor still reads `known_good_version` off the entity model, and a version in the predicate is something to be *checked*, never obeyed.
- **The threshold check is assert-only and lives in the script.** `scripts/verify_incident_one.py` scrapes the rates a sentence names and fails on any at or below nominal; nothing parsed is a `WARN`, not a failure. A regex over natural language on the *production* path would spend §7.1's single retry budget on something that is not a schema failure — that is why the fix upstream is a prompt clause and the check downstream is a script.

Verified live at **10/10 `RESOLVED`** via `--runs 10` (trace `c7e4a8a0c8ef59df4edc14efeb3f4e09`), lowest threshold named `0.015` against the `0.01` nominal, baseline restored after all twenty-two incidents the item spent.

**The versioned belief store is live (item 12) — Phase 4 opens, and the Policy Engine stops refusing.** One new module, `provenance/beliefs.py`: §3.3's `Evidence` (moved out of `policy.py`), the `BeliefVersion` document, and `append`/`current`/`history`. `policy.py` keeps §2.2's pipeline and now writes a real superseding version. Offline half `tests/test_beliefs.py`, live half `scripts/verify_belief_store.py`. Schema reasoning in `docs/adr/ADR-016`, which also answers ADR-014's revisit clause and ADR-011's `evidence_refs` note. Ten things to know before touching it:

- **Append-only is Firestore's guarantee here, not our discipline.** One immutable document per version at `beliefs/belief-{entity}/versions/{n}`, written with `create()` and never written again — **nothing under `provenance/` modifies or deletes a version**, and that is the property the whole memory design rests on. The test asserts v1's stored dict is *byte-identical* after v2 lands; do not add a `set()`, an `update()` or a delete path to this module. Tearing down a fixture belongs in a script (`verify_incident_one.py` says why); retracting one is §6.4 and item 15's.
- **Links point backwards only.** `supersedes` sits on the newer version; `superseded_by` is **derived on read** (version *n* is superseded iff *n+1* exists). Storing the backlink means writing to a committed version after the fact, which is both a second failure mode and a fact that can disagree with the chain. §3.2's history block is a *view* — `history()` is what produces it.
- **There is no `current_version` pointer, deliberately.** `current()` walks `versions/1, 2, …` until a read misses, so the newest existing version *is* the current one. A pointer costs a crash window that strands a belief at a version no later commit can extend; a transaction closes it at the price of transaction support in the test fake. Two or three reads per belief is not a cost worth engineering against — do not "optimise" a pointer back in.
- **`append()` writes the version document last**, after the root document and after every `evidence/{id}` it cites. A crash can leave evidence nothing references yet, which is harmless; a version citing evidence the store cannot produce is a belief that lies. A test asserts the order, so reordering the writes fails the build.
- **Evidence is normalised to `evidence/{id}` and cited by id**, `create()`-if-absent so re-citing an item is a no-op rather than a rewrite. This is what gives item 13's `(source_id, observed_at)` novelty check one place to read. `Evidence`, `payload_hash()` and `TIMESTAMP` all live in `beliefs.py` now — `incident.py` builds its one evidence item from there.
- **A re-affirmation commits; a status flip is refused.** Same status with new evidence supersedes at the 0.50 door. A *different* status answers `REJECT("FLIP_UNSUPPORTED")` carrying the arithmetic, because §4.3's 0.70 is inseparable from §6.3's different-source-class rule and both are item 14's. **Do not ship 0.70 without §6.3** — at 0.70 alone one sensor can set and clear its own alarm, which is strictly weaker than either document. `SUPERSESSION_UNSUPPORTED` is gone from `CommitReason`; `FLIP_UNSUPPORTED` and `VERSION_CONFLICT` replace it.
- **§2.2's stage order is unchanged: the registry is read before the store.** An agent whose standing has not been checked does not get a read of institutional memory performed on its behalf, so a stage-2 refusal reports `belief.version = 1` and no `supersedes` — `decision.reason` is what says the write never reached the store. `tests/test_policy.py`'s fail-closed registry test is what pins the ordering; a "fix" that reads the store first turns it red.
- **No function returns `BeliefVersion | None`** — `BeliefNotFound` / `BeliefStoreUnavailable` / `VersionConflict` are what `policy.py` catches, and a reflection test rejects any optional-`BeliefVersion` return type. §7.3 again: "the store was unreadable" and "there is nothing here" must not look alike, or a v1 gets committed over a belief that already exists.
- **The decay clock is written at commit** (`half_life_days`, `expires_at`, `on_expiry`) though the Sweeper that consumes it is Phase 9's — three constants, no branch, so beliefs written in between are not a backfill problem.
- **No new span shape and no new attribute key.** `belief.supersedes` was defined in item 2 and this is its first emitter; it carries the predecessor's **version number**, so the signature narrowed to `int | None`. It is emitted whenever a predecessor was found — on the refusal to flip one as much as on the version that supersedes it — and omitted entirely for a first belief. `ARCHITECTURE.md` §8.1 needed no edit; §6.7 gained an "as built" note.

Verified live: `scripts/verify_belief_store.py` green end to end against real Firestore, and `scripts/verify_incident_one.py` still reaches `RESOLVED` with one belief at exactly `0.60` (trace `292ade4d64980a4f3044bb878ec8f9d4`), baseline restored. **`AGENTS.md` and `.cursor/rules/provenance.mdc` were deleted in this item** — this file is the single source of instructions now, and there is nothing left to keep in sync.

**Novelty and the accumulated evidence set (item 13) — the Policy Engine's stage 3 stops being a comment.** `provenance/beliefs.py` gained `evidence_id` / `read_evidence` / `novel`; `provenance/policy.py` wired §2.2 stage 3 and widened stage 4; one line of `provenance/incident.py` fixed a live defect. Offline halves in `tests/test_policy.py`, `tests/test_beliefs.py` and `tests/test_incident.py`; live half `scripts/verify_belief_store.py`, extended a second time rather than duplicated. Schema reasoning in `docs/adr/ADR-017`. Nine things to know before touching it:

- **§4.3's confidence was already built — the item is really stage 3.** `policy.confidence()` has done the noisy-OR, the per-source-class collapse and the age decay since item 10, and three of §10's four confidence assertions were already green. Only *"age decay is monotonic"* was missing, and it is now a ladder of eight ages: a sign error inside the exponent fits any single-point test.
- **`REJECT("NO_NEW_EVIDENCE")` is the one addition to `CommitReason`, and it fires only against an existing belief.** §6.3 states novelty as a condition on the claim and §2.2 orders it *before* confidence, which only means something if the stage can terminate. An evidence-free **first** claim is a different refusal — `BELOW_THRESHOLD` at 0.00, ADR-002's arithmetic, the same door that refuses a bare `unverified_external_claim`. Do not collapse the two: "nothing new" is a statement about a belief that already exists, and a test pins the distinction because dropping the `previous is not None` guard is otherwise invisible.
- **A superseding version cites its predecessor's evidence plus the novel items, and confidence computes over that union.** §3.2 renders `#17 ev-[118]` → `#42 ev-[118,140,141]`; §6.3's legitimate-update case is **0.71** accumulated against 0.55 for the new item alone, and item 14's flip door is 0.70. Proposed-only evidence would leave §6.3's own worked example unreachable. `confidence()` needed **no change** — its per-class `max` already prefers the fresher item, which is what the spec's *"same-source re-confirmation raises confidence via decay-reset"* means arithmetically.
- **Novelty compares `(source_id, observed_at)` pairs, not ids, and that is deliberate.** With content-addressed ids the two agree and id-membership would cost zero reads. It would still be wrong: two callers assign ids by hand, so the guarantee would rest on a convention nothing enforces. `scripts/verify_belief_store.py` keeps its hand-written ids **on purpose** — that is the case proving the check compares pairs.
- **The evidence id is content-addressed now, and that fixed a live defect.** `incident.py` derived it from `action.predicate_id()` — the success-predicate *text* — while `append()` writes evidence create-if-absent. Two incidents phrasing the predicate identically shared an id at different observation times: the second write was discarded, the version cited the id twice, and the stored document kept the first run's `observed_at`. `confidence()` decays from `observed_at`, so the survivor aged every later observation by however long ago the first run was. `action.predicate_id()` keeps its other caller — the incident span's pre-declared predicate — untouched.
- **`read_evidence()` raises on a citation the store cannot answer.** A shorter tuple would make a missing item look like something nobody ever observed, and the next proposal carrying it would be counted as new (§7.3). The reflection test in `tests/test_beliefs.py` now rejects an optional `Evidence` return the same way it rejects an optional `BeliefVersion`.
- **`beliefs.py` still has no delete and still writes the version document last.** Item 13 added three functions and changed none of `append`/`current`/`history`. The two item-12 tests that guard those properties are untouched and must stay that way.
- **No new span shape and no new attribute key.** `provenance.evidence.novel_count` was defined in item 2 and item 10 emitted `len(evidence)` into it as a placeholder; this is its first honest value. `evidence.ids` and `evidence.source_classes` stay **as proposed** while `novel_count` is what survived stage 3 — the gap between them is the audit trail. `decision.reason` is a plain string on the span, so a new `CommitReason` is not a telemetry vocabulary change and §8.1 needed no edit.
- **`half_life_domain` is published as 30 days in §4.3 and stays one number until item 21.** It appeared in the formula with no value in any document, which is a real hole in ADR-002's "inspectable and fixed" defence. A per-domain table today would be a dict with one key; item 21 is when a second domain first writes beliefs.

Also in this item: `ARCHITECTURE.md` §5.10's as-built note was still item 10's and named a `SUPERSESSION_UNSUPPORTED` that item 12 had already removed — rewritten to items 10/12/13, alongside new "as built" notes on §2.2 and §4.3.

Verified live: `scripts/verify_belief_store.py` green end to end against real Firestore with its scratch state deleted on exit, and `scripts/verify_incident_one.py` still reaching `RESOLVED` with one belief at exactly `0.60` (trace `2ba3a15a45745e3b45753fe0fd49a5c2`), baseline restored. Mutation-checked six ways; 282 tests pass.

**The conflict rule and the standing counter (item 14) — the Policy Engine stops refusing flips, and a rejection finally costs something.** `provenance/registry.py` gained `record_rejection()`; `provenance/policy.py` gained `FLIP_THRESHOLD`, `COUNTED_REJECTIONS` and §6.3's rule; one comment in `provenance/incident.py` was repointed at item 23. Offline halves in `tests/test_policy.py` and `tests/test_registry.py`; live half `scripts/verify_belief_store.py`, extended a third time rather than duplicated. Schema reasoning in `docs/adr/ADR-018`, which also answers the revisit clauses in ADR-010, ADR-016 and ADR-017. Ten things to know before touching it:

- **The Memory Analyst is not here; it is item 23's, and that is a deviation from item 14's title.** The title said "Memory Analyst recommends; the engine decides", which over-promised relative to its own verify line — that line is entirely about the engine and is checked with scripted proposals, the posture item 8 used to prove denial-by-registry before any agent existed. A *status* is not a model's job on this path: the control loop already knows deterministically what a confirmed rollback teaches, which is why `incident.BELIEF_STATUS` is still a constant. The sentence has moved to item 23's roadmap text and `incident.py`'s comment now points there. **Do not add an Analyst agent to close a gap item 14 did not leave.**
- **"The class that established the current status" is the classes the version in force rests on** — the `known` tuple §2.2 stage 3 already resolved via `beliefs.read_evidence()`, so the check costs zero additional reads. The rule is a set difference and nothing else: `{i.source_class for i in new} - {i.source_class for i in known}` non-empty. Because evidence accumulates, this is the **stricter** of the two available readings (every class that has ever supported the belief, not only the one that last changed it), which is the right side to err on. Do not "fix" it into a `history()` walk.
- **One source class can never reach 0.70, and that is what makes the class rule load-bearing.** The strongest base weight is 0.60 and §4.3 collapses a class to its least-decayed item. So a same-class flip *above* 0.70 — the verify line's first half — is only reachable because a version rests on everything it ever rested on (item 13): the chain carries two classes and the proposal adds a third reading of one of them. Read the class set off the *proposal* and that case silently becomes a commit.
- **Two failures, two names, and no new vocabulary.** `BELOW_THRESHOLD` carrying `threshold = 0.70` is the number; `FLIP_UNSUPPORTED` is the corroboration. `belief.threshold` was already on the span and on the stored version, so **no new `CommitReason`, no new attribute key, no fifth span shape** — `ARCHITECTURE.md` §8.1 needed no edit. `FLIP_UNSUPPORTED` now means exactly one thing where before it meant "any status difference, for reasons not yet built"; ADR-016's revisit clause expected it to become unreachable and the honest answer is the opposite.
- **`COUNTED_REJECTIONS` is three reasons and must stay three.** `BELOW_THRESHOLD`, `FLIP_UNSUPPORTED`, `NO_NEW_EVIDENCE` — §3.4 counts "rejected memory writes **lacking verifiable evidence**", not every rejection. `REGISTRY_UNAVAILABLE` / `STORE_UNAVAILABLE` / `VERSION_CONFLICT` are infrastructure and standing has **no automatic restoration path**, so counting them is a permanent penalty for someone else's outage. `STANDING_NOT_GOOD` / `DOMAIN_NOT_HELD` are already-refused authority. `NO_NEW_EVIDENCE` is in **deliberately**: without it a poisoner repeating one unverifiable pair never reaches the counter and item 28's DEGRADED never fires.
- **`registry.record_rejection()` takes the `Agent`, not an id, and `set_standing()` is still the only writer of standing.** The Policy Engine already holds the record from stage 2 — §1.1's fourth property is about reading standing *before deciding*, not reading it twice inside one decision. `registry.py` stays the only module that writes `agents/{id}`; *which* refusals count stays in `policy.py`, because that is a judgment about §2.2's pipeline and not about storage.
- **The window is append-only and never pruned**, and `degraded_by_window()` runs over the **appended** list — apply it to the pre-append window and the third rejection stops degrading, the fourth does. A consequence worth knowing: a human reinstating an agent must clear the window too, or it re-degrades on the next single rejection inside 24 hours. The live script clears both.
- **The counter write is best-effort and the refusal is not.** A `RegistryError` is suppressed in `commit()` with a `ponytail:` comment: nothing was committed either way, so it costs a missed increment rather than a wrong answer, and raising would turn a correct fail-closed refusal into an exception out of the incident's `resolve` node. The window is therefore a **floor** on how many rejections happened, never a ceiling.
- **The `belief.commit` span carries the standing that governed the decision, not the one it caused.** An agent that degrades on this very rejection still shows `GOOD` — it was GOOD when the proposal was evaluated, and the degradation is a consequence, not an input. `DEGRADED` becoming visible is the registry panel's surface (item 28).
- **The live script degrades its own agent, on purpose now.** The three refusals items 12–13's half provokes are exactly the three counted ones, so `sre-infra-agent` hits DEGRADED mid-verification. Rather than arrange around it, the script asserts the window's three reasons in order and then reinstates — which proves live that the write is real and that these three are what count. If you add a fourth counted refusal to that half, this assertion is what will tell you.

**Retraction and the audit ledger (item 15) — the Policy Engine's second door, and the first thing that links an action to a belief.** One new module, `provenance/audit.py`; `provenance/policy.py` gained `retract()`, `RETRACTED`, two `CommitReason` values and §6.4's gate; `provenance/incident.py` writes a ledger record after an approving decision. Offline halves `tests/test_audit.py` plus new cases in `tests/test_policy.py` and `tests/test_incident.py`; live half `scripts/verify_belief_store.py`, extended a fourth time rather than duplicated. Schema reasoning in `docs/adr/ADR-019`, which also answers the revisit clauses in ADR-016, ADR-017 and ADR-018. Ten things to know before touching it:

- **§6.4's third bullet had nothing to join against, and building that link is most of the item.** No `Action`, `Decision`, `ExecutionResult` or span carried a belief id — `Action.evidence_refs` holds *evidence* ids (§3.3) and `recall.belief_ids` on the reasoning span records what was nominated, not what authorized anything. So `authorizations/{id}` exists: one document per authorized action, citing whatever `recall()` returned, written by `incident.py`. **`recall()` is item 16's and returns `()`**, so every record a real incident writes cites nothing and a real retraction flags nothing — that is the honest state, stated in the module docstring, and the mechanism is proven with hand-seeded records exactly as items 8 and 14 proved theirs.
- **The heuristic alternative was rejected because it would falsify the claim, not prove it.** Flagging by `target == entity` inside the retracted version's window needs no new storage and flags actions that merely *touched* the same entity, cannot name the version in force, and cannot tell "this belief informed the plan" from "this incident was about this service". §6.4's weight is "knows *which* of its past decisions rested on the wrong thing"; that is the distinction the heuristic erases.
- **§6.4 replaces §6.3 for a retraction — it is not cumulative.** §6.3's three cases end with "*Disproven belief.* Retraction — see §6.4", which is a delegation. Applying the different-source-class rule too would make a same-class retraction impossible, and a sensor reporting that what it previously reported was wrong is the paradigm case §6.4 exists for. `flip` is now `not retracting and previous is not None and previous.status != status`; drop that first clause and eleven tests go red.
- **The door is 0.50 over the disproving evidence *alone*, and every word of that is load-bearing.** Over the accumulated set any threshold is free (that set already cleared one). At 0.70 over disproving-only the strongest single class is 0.60, so two classes would always be required and §6.4's "at least as strong" bullet would be dead text. At 0.50 the arithmetic *is* the bullet: 0.60 / 0.55 / 0.50 retract, 0.15 and 0.00 cannot. Removing the number entirely would leave the one path that *erases* memory as the only path with no arithmetic on it.
- **The two rules are independent, and one case proves it live:** `third_party_audit` at 0.55 clears the 0.50 door and still fails the class test against a belief established by `verified_system_observation` at 0.60. That is the first half of the live script's retraction section and `test_a_strong_enough_number_still_fails_the_class_test`. Without it a suite where the threshold does all the work would pass.
- **The strength baseline is `max(BASE_WEIGHT)` over the classes the version in force rests on** — item 14's reading of §6.3's near-identical phrase, and free because stage 3 already resolved that evidence. **Published base weights, never decayed ones**: §6.4 compares classes, and a time-dependent rule would let a belief drift in and out of reach by nothing but sitting there.
- **Nothing subtracts.** The `RETRACTED` version cites the accumulated set plus the disproving items, and no class leaves the flip-comparison set — ADR-017's and ADR-018's revisit clauses answered "confirmed" rather than with an exception. Consequence: re-asserting a retracted status is an ordinary flip needing 0.70 **and** a class the whole chain does not carry, so two sensors cannot retract and reinstate by trading turns. ADR-016's third question resolves as *both* — `RETRACT` is the outcome the engine decided, `RETRACTED` is the status the store holds.
- **Flag before append, and fail closed on both sides.** `beliefs.append()` writes evidence before the version citing it for one reason and this is the same one: a flag pointing at a retraction that never landed is a false alarm a human dismisses, while a retraction whose actions were never flagged is the failure §6.4 exists to prevent. An unwritable ledger is `REJECT`/`STORE_UNAVAILABLE` with **no version written**; in the incident, a failed ledger write routes `HALT` and ends `ESCALATED` rather than executing — the belief store is the same Firestore, so an incident that cannot write the ledger could not have committed its belief either. Do not make either one best-effort "like the standing counter": that write is best-effort because nothing was committed either way, which is not true here.
- **`COUNTED_REJECTIONS` is four now, and only one of the two new reasons joined.** `RETRACTION_UNSUPPORTED` is a statement about the agent's evidence (item 14's test). `NOTHING_TO_RETRACT` is about the store's state — counting it would let a typo or a race permanently degrade an agent, since standing has no automatic restoration path.
- **No new span shape and no new attribute key** — the streak since item 12 holds, and `ARCHITECTURE.md` §8.1 needed no edit. `BeliefOutcome` has admitted `RETRACT` since item 2 and this is its first emitter; `telemetry._ERROR_OUTCOMES` correctly omits it, because a retraction is a *successful* outcome. Two new `CommitReason` values are not a telemetry change (`decision.reason` is a plain string). **`flag()` is an `update()` and it lives in `audit.py`, never in `beliefs.py`** — that is what keeps "nothing under `provenance/` modifies or deletes a belief version" literally true. `tests/test_registry.py`'s fake store grew a `where`/`stream` pair, its first query support.

Verified live: `scripts/verify_belief_store.py` green end to end against real Firestore with everything restored on exit, and `scripts/verify_incident_one.py` still reaching `RESOLVED` with one belief at exactly `0.60` (trace `a181050a3f191877ed38a664b2dc505e`), baseline restored and no ledger record left behind. Mutation-checked eight ways; 326 tests pass, up from 296.

**Recall is live (item 16) — the index nominates, the belief store decides.** One new module, `provenance/recall.py`; `provenance/beliefs.py` gained §3.2's `statement`, its root-document copy, `class_statements()` and `belief_id_for()`; `provenance/policy.py` gained `UNKNOWN`; `provenance/incident.py`'s stub is gone. Offline half `tests/test_recall.py` plus cases in `tests/test_beliefs.py`, `tests/test_incident.py` and `tests/test_telemetry_schema.py`; live half the new `scripts/verify_recall.py`. Schema reasoning in `docs/adr/ADR-020`, which implements `ADR-005` without revisiting its division of labour. Ten things to know before touching it:

- **The `statement` lives on the belief root document as well as the version, and that is the load-bearing part.** ADR-005 says the index "never sees confidence, status, or currency"; the statement lives on the version, so an index reading `current()` would be touching the exact object it is forbidden to consult. `beliefs.class_statements()` streams **root documents** — `belief_id`, `entity`, `domain`, `scope`, `statement`, `created_at`, and no status and no confidence — so the blindness is structural, not a promise. `append()` writes the root create-if-absent, so the statement lands once at v1 and is never rewritten. Make `nominate()` resolve a version to read the statement and 34 tests go red.
- **Nothing stores a vector and no index endpoint is deployed.** Statements and the query are embedded together in one batched call per incident and ranked by cosine in Python. A Vertex AI Vector Search index bills per node-hour whether or not it serves a query — the Gemma sanitizer's trap — and against a corpus in single digits it buys latency nobody is short of. It also removes a failure mode: a stored vector can disagree with the statement it came from. `nominate()`'s signature does not change if Vector Search ever becomes worth it.
- **`nominate()` returns `tuple[str, ...]` and a test asserts that annotation.** The whole ADR-005 argument is that the index proposes and the store disposes; a `nominate()` that returned versions would be an index that had already read what it may not see.
- **The two reads fail independently, and only one goes quiet.** An unreachable embedding API raises `IndexUnavailable`, which `recall()` catches into zero class nominations — never all of them — while §6.1's exact-key read proceeds untouched, because a promise of a *mechanical* read must not acquire a dependency on a model being up. An unreadable **store** propagates: "the store was unreadable" and "the organization believes nothing" must not look alike (§7.3). A nominated id the store cannot produce is dropped rather than raised — a stale nomination is not a corrupt belief.
- **`recall.nominated_ids` is a new attribute key, the first since item 2 defined the vocabulary.** It shipped under item 2's condition — module, `ARCHITECTURE.md` §8.1 and `tests/test_telemetry_schema.py` in one commit — and still **five span shapes**. It earns the exception the way `evidence.ids` beside `novel_count` does: §6.6's guarantee is that a RETRACTED belief can be the closest match and reach nobody, and a span carrying only the survivors is indistinguishable from an index that found nothing. The gap between the two keys is the audit trail.
- **The ledger cites `entity_ids`, never `belief_ids`.** §6.2 caps a class belief as ADVISORY ONLY — it may reorder investigation and may never be the evidence that authorizes an action — and `authorizations/{id}.belief_ids` is the record of what an action rested on. A class belief there would make §6.4's retraction flag actions on grounds §6.2 says they could not have had. `Recalled` keeps the two sets apart rather than concatenating them so the cap is a property of the type, not a rule `incident.py` has to remember.
- **`UNKNOWN` is defined in `policy.py` and filtered now, though item 29's Sweeper is what writes it.** Recall drops `RETRACTED` and `UNKNOWN` together so item 29 has to write a status and not also remember to fix a read path.
- **`SIMILARITY_FLOOR = 0.55` was measured, and the measurement matters more than the number.** Against an `error_rate_spike on inventory-api` query, `text-embedding-005` scores infrastructure statements at 0.628–0.731 and an unrelated supply-chain one at 0.523. But the same statements score **0.696 against a nonsense cafeteria query** — the floor ranks statements *within one query* and does not judge whether the query was sensible. Do not read a score here as a confidence; nothing downstream does, which is what makes a weak instrument safe.
- **The offline tests inject the embedder and the live script does not, on purpose.** With a real model, "this belief is the closest match" is an opinion, and a run where the retracted one happened not to rank first would pass while proving nothing. Hand-picked vectors make the premise a fact offline; `scripts/verify_recall.py` asserts the **ranking before the drop** so the premise is checked live too. Its fourth scratch belief — the weaker but *current* class belief — is the control: without it, "the retracted one was dropped" and "recall returned nothing" pass the same check.
- **Recall is still a plain awaited call before `build_graph()`, not a graph node.** ADR-013 chose that shape and item 18's `verify:` line ("the recall event appears in the trace before the domain agent's first hypothesis") is already satisfied by it. `run_incident()` gained one keyword, `embed`, which exists for the same reason `model_orchestrator` does. **The Orchestrator's and SRE agent's instructions were not touched** — item 18 owns the prompt work; item 16's guarantee is asserted on recall's return value, on `{recall_summary}` and on the span.

Verified live: `scripts/verify_recall.py` green end to end against real Firestore and a real embedding model with all four scratch beliefs deleted on exit, and `scripts/verify_incident_one.py` still reaching `RESOLVED` with one belief at exactly `0.60` (trace `20112d7f984829ebc7dd9b1900639e27`), baseline restored. Incident #1 recalls nothing and cites nothing, which is honest — it is the fleet's first encounter with the service, and item 18 is where memory pays. Mutation-checked six ways; 348 tests pass, up from 326.

**The belief inspector is live and `SUP-042` is AT_RISK (item 17) — §8.2's money shot, and the UI's second data source.** `provenance/policy.py` gained `Contribution` / `contributions()`; `provenance/app.py` gained `GET /belief/{entity}`; `provenance/web/index.html` filled the third of six surfaces. Two new scripts, `scripts/seed_belief.py` and `scripts/verify_belief_inspector.py`; offline halves in `tests/test_policy.py` and `tests/test_app.py`. Schema reasoning in `docs/adr/ADR-021`, which amends ADR-015 and answers ADR-019's revisit clause. Ten things to know before touching it:

- **The span stream cannot serve this surface, and that is structural rather than an oversight.** §8.2 asks the inspector for evidence, the arithmetic behind a computed confidence, a supersession chain and a decay clock — every one of those is *content*, §8.1 keeps content off spans on purpose, and `tests/test_telemetry_schema.py` enforces it. So **do not "fix" this by adding a sixth span shape or a content-bearing attribute key**: that would trade the property that makes `/trace` safe to publish for one panel. The UI has two data sources now, and ADR-015 carries a stated exception instead of a quiet one.
- **The route reads live Firestore, and a baked JSON snapshot is the rejected alternative.** It needs no route logic and no request-time credentials, and it would be correct until item 28 — which attacks this exact belief — and item 29, whose §10 row wants the `UNKNOWN` downgrade "visible in the belief inspector". A snapshot would render the pre-attack chain during the attack, which is a surface asserting state the store does not hold.
- **`policy.contributions()` is the only implementation of §4.3, and `confidence()` is defined in terms of it.** One row per **distinct source class**, carrying `base`, `age_days` and the decayed `weight`. Computing the breakdown in `app.py` or in the browser would put the published formula in a second place — and a breakdown that can disagree with the number it explains is worse than no breakdown. The test is `Π(1 − w) == 1 − confidence()` over a table: `set_risk()`'s components-must-sum rule, applied to a product.
- **The route serves both confidences, labelled, and that is not redundancy.** The version document is immutable so its `confidence` is frozen at commit, while the evidence behind it keeps decaying. Stored-only makes the decay clock a date with no consequence; live-only makes the UI appear to contradict the append-only store it inspects. They differ by exactly `2^(-age/30)`, which is the clearest demonstration in the product that confidence is computed rather than asserted.
- **`BeliefNotFound → 404`, `BeliefStoreUnavailable → 503`, and they must never collapse into one code.** §7.3 on the read side: a 404 claims this entity has no beliefs, which a service cannot know while it cannot read the store. Concretely — item 28's closing shot is "`SUP-042` is still AT_RISK", and a route answering 404 when Firestore blinks renders that shot as the poisoning having worked. A test pins it.
- **The seeded chain's numbers come out of the formula and cannot be chosen.** `FLAGGED` **0.575** at T−8d → `AT_RISK` **0.770** at T. The eight-day gap is load-bearing, not decorative: each version's evidence is fresh at its own commit, so v1 clears the 0.50 door; back-date the pair twelve days and v1 scores **0.4495** and is refused `BELOW_THRESHOLD`. v2 is a genuine §6.3 flip — it clears 0.70 **and** adds `third_party_audit`, a class the chain did not carry — so **the third evidence item cannot be another reading of the first two**; a restatement is refused at 0.5033.
- **`scripts/seed_belief.py` has no `--reset` and refuses rather than skipping.** `seed_registry.py`'s posture for a sharper reason: items 27 and 28 attack this belief and the demo's point is that it survived, so a re-run rewriting a poisoned-then-defended chain would erase what those beats prove. `suppliers/SUP-042` is still untouched and still carries no status — ADR-009's rule held.
- **§3.2's canonical figure does not compute, and §3.2 now says so.** `conf 0.71` on one evidence item and `conf 0.94` on three are both unreachable: one class caps at **0.60**, all five fresh cap at **0.9235**. The figure is kept as written (it is verbatim from the spec) with an as-built note carrying the real numbers. If you find yourself trying to make the store produce 0.94, that is the direction ADR-009 already rejected.
- **`flagged_by` is deliberately not rendered, and that answers ADR-019's clause with "not yet".** `SUP-042` is never retracted and no `authorizations/{id}` cites it, so it would be an empty array on every load. Item 31's approval cards are the first surface with a flagged action to show, and whether `audit.flag()` should emit a span is a §8.1 change that belongs there. Likewise **no `statement` parameter was added to `policy.commit()`** — §3.2 gives an ENTITY belief none, and item 23 is what first needs one.
- **The inspector rides the 15s `/health` tick, not the 1s `/trace` tick.** A belief changes on the order of once per demo; 60× the reads buys nothing items 28 and 29 need. Also: the section is now `class="wide"`, and **the six `<h2>` literals are still pinned by `tests/test_app.py`** — do not rename one.

Verified live: `scripts/seed_belief.py` and `scripts/verify_belief_inspector.py` green against real Firestore, the rendered surface exercised against the live payload in a DOM harness (rendered total equals the route's `confidence_now`; two of three rows carry an 8-day age), and `scripts/verify_incident_one.py` still reaching `RESOLVED` with one belief at exactly `0.60` (trace `5864ac5e2d58574cac42f8077718ac9b`), baseline restored. `SUP-042` is scope `ENTITY` and carries no statement, so it is correctly absent from the recall index. Mutation-checked six ways; 358 tests pass, up from 348.

Alongside this sits the documentation suite (this file, `README.md`, `ARCHITECTURE.md`, `THREAT_MODEL.md`, `ROADMAP.md`, `docs/adr/`) and the original spec. The item-0.5 constraints all hold in the scaffold: the package is **`provenance/`** (a local `services/` would shadow the installed `portunusmcp`), `portunusmcp` installs as a **separate `--no-deps` step** and must never be added to `pyproject.toml`, and CI does not gate on `pip check`. See `ROADMAP.md` for the full build order; update this section as phases complete, recording what shipped and any deviations, the way each completed item gets a `— **done**:` note in the roadmap.

---

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

This project's design questions have largely been resolved in writing — the spec's §19 "Resolved design questions," `ARCHITECTURE.md`, and `docs/adr/`. If a design question comes up, check there first before guessing; if it's genuinely not covered, that's exactly the kind of thing to surface rather than silently decide.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked, and beyond what the current `ROADMAP.md` phase calls for. Don't pull forward a Phase 8 security beat while working on Phase 3.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested — e.g. don't turn the risk table into a pluggable scoring framework or the two memory domains into a generic domain-registration system; the generality claim is proven by the Phase 6 line count, not by speculative plumbing.
- No error handling for impossible scenarios — but do implement the fail-closed handling `ARCHITECTURE.md` §7.3 explicitly calls for; that's a stated requirement, not speculative robustness.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it — don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: every changed line should trace directly to the current task or `ROADMAP.md` item. This matters doubly here: the Phase 6 generality proof reports lines changed outside the domain agent, and the contest disclosure story depends on new work being cleanly visible in the history.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add the standing check" → "Write a test that flips an agent to DEGRADED mid-run and asserts its next low-risk proposal is held, then make it pass."
- "Implement the conflict rule" → "Write a test where a same-source-class flip above 0.70 is rejected and a different-class one commits, then make it pass."
- "Wire up the Sweeper" → "Expire a belief with no re-verification source and assert UNKNOWN(stale), excluded from recall, never deleted."

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification. `ARCHITECTURE.md` §10 (Testing Strategy) already defines the verification criteria for every core guarantee, and each `ROADMAP.md` item carries a `verify:` line — use those as the source of "verify: [check]" rather than inventing new success criteria per task.

---

**These guidelines are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, phase boundaries in `ROADMAP.md` stay respected, and clarifying questions come before implementation rather than after mistakes.
